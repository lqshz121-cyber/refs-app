import assert from 'node:assert/strict';import fs from 'node:fs';import React from 'react';import {renderToStaticMarkup} from 'react-dom/server';import {AuthoritativeExpensePanel} from '../src/authoritative-expense-panel.jsx';
const config={entityId:'11111111-1111-4111-8111-111111111111',periodId:'33333333-3333-4333-8333-333333333333',baseUrl:'https://accounting.example',getAccessToken:async()=> 'a'.repeat(48)},markup=renderToStaticMarkup(<AuthoritativeExpensePanel config={config} fetcher={async()=>{throw new Error('SSR must not fetch');}}/>);for(const token of ['Invoice expense review','PROPOSAL ONLY'])assert.match(markup,new RegExp(token,'i'));const source=fs.readFileSync('src/authoritative-expense-panel.jsx','utf8');for(const token of ['refreshAuthoritativeAiInvoiceExpenseEvidence','policy-backed expense proposal','No Draft, review, approval, or posting authority'])assert.match(source,new RegExp(token,'i'));assert.doesNotMatch(source,/localStorage|seed\.js|legacy-demo-app/i);assert.doesNotMatch(source,/[^\x00-\x7F]/);console.log('authoritative expense panel: authenticated evidence only');

import assert from 'node:assert/strict';import test from 'node:test';import {refreshAuthoritativeAiApInvoiceCutoffReviews} from '../src/accounting-api.js';
const id=n=>`${String(n).padStart(8,'0')}-0000-4000-8000-${String(n).padStart(12,'0')}`,hash=n=>`sha256:${String(n%10).repeat(64)}`,entityId=id(1),periodId=id(2),actions={can_create_draft:false,can_review:false,can_approve:false,can_post:false};
const finding={schema_version:'AI_AP_INVOICE_CUTOFF_FINDING_V1',finding_type:'AP_INVOICE_PRIOR_PERIOD_CUTOFF_RISK',risk_level:'HIGH',rule_id:'AI_AP_INVOICE_BUSINESS_DATE_VS_POSTED_PERIOD_V1',entity_id:entityId,accounting_period_id:periodId,current_period_code:'2026-08',invoice_period_id:id(3),invoice_period_code:'2026-07',invoice_period_status:'CLOSED',vendor_ref:'V-1',vendor_name:'Vendor',document_number:'INV-1',currency:'USD',gross_amount:'500.0000',invoice_business_date:'2026-07-29',accounting_date:'2026-08-03',source_trace:{business_document_id:id(4),source_document_id:id(5),source_payload_hash:hash(1),posted_journal_entry_id:id(6)},reason:'Prior-period invoice.',suggested_action:'Review cutoff.',confidence:0.99,owner_role:'CONTROLLER_REVIEW',due_basis:'BEFORE_PERIOD_CLOSE',required_human_fields:['goods_services_received_date','invoice_received_date','source_completeness','prior_period_close_assessment','materiality_assessment','account_mapping','cutoff_conclusion','resolution_reason'],action_flags:actions};
const data={schema_version:'AI_AP_INVOICE_CUTOFF_BATCH_V1',current_accounting_period_id:periodId,scanned_posted_invoice_count:1,finding_count:1,findings:[finding],action_flags:actions},config={baseUrl:'https://api.example.test',entityId,periodId,getAccessToken:async()=>'a'.repeat(48)};
test('reads authoritative cutoff evidence through a no-store GET',async()=>{let request;const result=await refreshAuthoritativeAiApInvoiceCutoffReviews({config,fetcher:async(url,init)=>{request={url,init};return {ok:true,json:async()=>({ok:true,data})};}});assert.equal(result.ok,true);assert.match(request.url,/ap-invoice-cutoff-reviews/);assert.equal(request.init.method,'GET');assert.equal(request.init.cache,'no-store');assert.equal(result.data.findings[0].action_flags.can_post,false);});
test('fails closed for extra trace fields or enabled accounting actions',async()=>{for(const changed of [{...data,findings:[{...finding,source_trace:{...finding.source_trace,token:'secret'}}]},{...data,action_flags:{...actions,can_post:true}}]){const result=await refreshAuthoritativeAiApInvoiceCutoffReviews({config,fetcher:async()=>({ok:true,json:async()=>({ok:true,data:changed})})});assert.equal(result.ok,false);assert.equal(result.code,'AI_AP_INVOICE_CUTOFF_PROTOCOL');}});

import assert from 'node:assert/strict';import {readRuleDetail,readRules} from '../src/rule-register.js';const id=n=>`${n.repeat(8)}-${n.repeat(4)}-4${n.repeat(3)}-8${n.repeat(3)}-${n.repeat(12)}`,entityId=id('2'),ruleId=id('3'),h='sha256:'+'a'.repeat(64),flags={can_create:false,can_edit:false,can_reorder:false,can_copy:false,can_enable:false,can_auto_categorize:false,can_auto_match:false,can_post:false},row={rule_id:ruleId,rule_kind:'BANK',rule_name:'Rule',family:'BANK',version:'1',lifecycle_revision:'0',priority:1,status:'APPROVED',scope_type:'ENTITY',scope_key:entityId,input_key_hash:h,snapshot_hash:h,configuration_state:'AVAILABLE',conditions:{x:'y'},actions:{account:'1'},effective_from:'2026-01-01T00:00:00Z',effective_to:null,created_by:'maker',approved_by:'reviewer',approved_at:'2026-01-02T00:00:00Z',retired_by:null,retired_at:null,retire_reason:null,usage_count:0,last_used_at:null,audit_event_ids:[],history:[],action_flags:flags},page={schema_version:'RULE_REGISTER_V1',entity_id:entityId,rule_kind:'BANK',status:'ALL',after_id:null,limit:25,read_at:'2026-02-01T00:00:00Z',rows:[row],next_id:null,action_flags:flags},config={baseUrl:'https://refs.example',tenantId:id('1'),entityId,periodId:id('9'),getAccessToken:async()=>'fixture-token-'.repeat(4)};let calls=[];const fetcher=async(url,options)=>{calls.push({url,options});return{ok:true,status:200,json:async()=>({ok:true,data:url.includes(`/${ruleId}`)?row:page})};};const list=await readRules({config,fetcher});assert.equal(list.ok,true);assert.match(calls[0].url,/kind=BANK&status=ALL&limit=25/);assert.equal(calls[0].options.cache,'no-store');const detail=await readRuleDetail({config,row,fetcher});assert.equal(detail.ok,true);assert.match(calls[1].url,new RegExp(`${ruleId}\\?kind=BANK$`));const drift=await readRuleDetail({config,row,fetcher:async()=>({ok:true,status:200,json:async()=>({ok:true,data:{...row,version:'2'}})})});assert.equal(drift.ok,false);console.log('rule register client: authenticated closed reads and stale detail guard verified');

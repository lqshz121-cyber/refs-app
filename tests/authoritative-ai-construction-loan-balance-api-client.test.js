import assert from 'node:assert/strict';import {refreshAuthoritativeAiConstructionLoanBalanceReviews} from '../src/accounting-api.js';
const id=n=>`${String(n).padStart(8,'0')}-0000-4000-8000-${String(n).padStart(12,'0')}`,hash=c=>`sha256:${c.repeat(64)}`,entityId=id(1),periodId=id(2),config={baseUrl:'https://api.example',entityId,periodId,getAccessToken:async()=>'controlled-token-value'},flags={can_create_draft:false,can_review:false,can_approve:false,can_post:false};
const finding={schema_version:'AI_CONSTRUCTION_LOAN_BALANCE_REVIEW_V1',finding_type:'LOAN_BALANCE_MISMATCH',risk_level:'HIGH',rule_id:'AI_LOAN_BALANCE_TIE_OUT_V1',entity_id:entityId,period_id:periodId,period_code:'2026-07',account_code:'291001',account_name:'Construction loan payable',loan_ref:'LOAN-100',currency:'USD',statement_date:'2026-07-31',lender_closing_balance:'100.0000',gl_closing_balance:'500.0000',difference_amount:'-400.0000',source_document_id:id(3),source_document_line_id:id(4),source_payload_hash:hash('a'),source_line_hash:hash('b'),journal_entry_ids:[id(5)],ledger_line_ids:[id(6)],gl_source_document_ids:[id(7)],loan_account_mapping_snapshot_id:id(8),loan_account_mapping_snapshot_hash:hash('c'),reason:'Lender and GL balances differ.',suggested_action:'Reconcile lender, bank, and ledger evidence before close.',required_human_fields:['lender_statement_review','draw_and_repayment_reconciliation','bank_trace_review','interest_fee_escrow_assessment','adjustment_decision'],policy_evidence:{setting_snapshot_id:id(9),setting_snapshot_hash:hash('d'),policy_version:1,minimum_difference:'0.0000'},action_flags:flags};
const fetcher=value=>async(url,options)=>{assert.match(url,/construction-loan-balance-review\?periodId=/);assert.equal(options.method,'GET');assert.equal(options.cache,'no-store');return {ok:true,json:async()=>({ok:true,data:{schema_version:'AI_CONSTRUCTION_LOAN_BALANCE_REVIEW_BATCH_V1',entity_id:entityId,scanned_gl_account_count:1,scanned_lender_statement_count:1,finding_count:1,findings:[value],action_flags:flags}})};};
const result=await refreshAuthoritativeAiConstructionLoanBalanceReviews({config,fetcher:fetcher(finding)});assert.equal(result.ok,true);assert.equal(result.rows[0].difference_amount,'-400.0000');assert.equal(result.rows[0].action_flags.can_post,false);
for(const changed of [{extra:'unsafe'},{entity_id:id(99)},{period_id:id(98)},{source_payload_hash:'sk-secret'},{journal_entry_ids:[]},{action_flags:{...flags,can_post:true}},{difference_amount:null}])assert.equal((await refreshAuthoritativeAiConstructionLoanBalanceReviews({config,fetcher:fetcher({...finding,...changed})})).ok,false);

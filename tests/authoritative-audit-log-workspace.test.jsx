import assert from'node:assert/strict';import test from'node:test';import React from'react';import{renderToStaticMarkup}from'react-dom/server';import{AuthoritativeAuditLogWorkspace}from'../src/authoritative-audit-log-workspace.jsx';import{navigationItemForRoute}from'../src/authoritative-navigation.js';
test('Audit Log is a discoverable immutable read workspace',()=>{assert.equal(navigationItemForRoute('audit-log').availability,'API_READ');const html=renderToStaticMarkup(<AuthoritativeAuditLogWorkspace config={{entityId:'22222222-2222-4222-8222-222222222222'}}/>);assert.match(html,/Audit Log/);assert.match(html,/Sensitive metadata and free-text reasons are excluded/);assert.match(html,/Apply/);assert.doesNotMatch(html,/Create|Approve|Post journal/);});

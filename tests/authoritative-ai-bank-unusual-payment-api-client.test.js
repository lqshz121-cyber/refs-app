import assert from 'node:assert/strict';
import {refreshAuthoritativeAiBankUnusualPayments} from '../src/accounting-api.js';
const id=n=>`${String(n).padStart(8,'0')}-1111-4111-8111-111111111111`,hash=`sha256:${'a'.repeat(64)}`,entityId=id(1),periodId=id(2),actions={can_create_draft:false,can_review:false,can_approve:false,can_post:false};
const policy={schema_version:'AI_BANK_UNUSUAL_PAYMENT_POLICY_V1',setting_snapshot_id:id(3),setting_snapshot_hash:hash,policy_version:1,minimum_absolute_payment:'1000.0000',weekend_days:[0,6],holiday_dates:['2026-07-04']};
const finding={schema_version:'AI_BANK_UNUSUAL_PAYMENT_FINDING_V1',finding_type:'BANK_PAYMENT_UNUSUAL_TIMING',risk_level:'HIGH',rule_ids:['BANK_PAYMENT_WEEKEND_DATE','BANK_PAYMENT_APPROVED_HOLIDAY_DATE'],entity_id:entityId,accounting_period_id:periodId,bank_account_ref:'111000-CASH',transaction_date:'2026-07-04',currency:'USD',amount:'-5000.0000',counterparty_name:'Example Vendor',bank_memo:'Emergency service payment',source_trace:{bank_source_id:id(10),source_document_id:id(11),source_document_line_id:id(12),source_payload_hash:hash,source_line_hash:hash,external_bank_line_id:'BANK-LINE-1'},reason:'A retained bank payment occurred on an approved weekend and holiday.',suggested_action:'Verify the payee, source support, approval, entity, urgency, and segregation before reconciliation close.',confidence:.94,owner_role:'CONTROLLER_REVIEW',due_basis:'BEFORE_BANK_RECONCILIATION_CLOSE',required_human_fields:['payee_identity','invoice_or_contract_support','payment_approval','business_urgency','preparer_approver_separation','valid_or_suspicious_conclusion','resolution_reason'],policy_evidence:policy,action_flags:actions};
const config={baseUrl:'https://accounting.example',entityId,periodId,getAccessToken:async()=> 'a'.repeat(48)};
let seen;const fetcher=async(url,options)=>(seen={url,options},{ok:true,status:200,json:async()=>({ok:true,data:{schema_version:'AI_BANK_UNUSUAL_PAYMENT_BATCH_V1',current_accounting_period_id:periodId,scanned_payment_count:1,finding_count:1,findings:[finding],action_flags:actions}})});
const result=await refreshAuthoritativeAiBankUnusualPayments({config,limit:25,fetcher});assert.equal(result.ok,true);assert.match(seen.url,/bank-unusual-payments\?periodId=/);assert.equal(seen.options.method,'GET');assert.equal(seen.options.cache,'no-store');assert.equal(seen.options.credentials,'include');assert.equal(result.data.findings[0].source_trace.source_document_line_id,id(12));assert.equal(result.data.findings[0].action_flags.can_post,false);
const unsafe=await refreshAuthoritativeAiBankUnusualPayments({config,fetcher:async()=>({ok:true,status:200,json:async()=>({ok:true,data:{schema_version:'AI_BANK_UNUSUAL_PAYMENT_BATCH_V1',current_accounting_period_id:periodId,scanned_payment_count:1,finding_count:1,findings:[{...finding,raw_payload:'secret'}],action_flags:actions}})})});assert.equal(unsafe.ok,false);assert.equal(unsafe.code,'AI_BANK_UNUSUAL_PAYMENT_PROTOCOL');
console.log('authoritative unusual bank-payment client: signed source, approved calendar, no accounting action');

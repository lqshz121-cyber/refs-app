import test from 'node:test';import assert from 'node:assert/strict';import {parseAuthoritativeSourceDocument} from '../src/accounting-api.js';
const id=n=>`${String(n).padStart(8,'0')}-1111-4111-8111-${String(n).padStart(12,'0')}`,tenantId=id(1),entityId=id(2),sourceDocumentId=id(3),periodId=id(4),hash='sha256:'+'a'.repeat(64),row={schema_version:'SOURCE_DOCUMENT_PARSE_RUN_V1',parse_run_id:id(5),tenant_id:tenantId,entity_id:entityId,source_document_id:sourceDocumentId,period_id:periodId,source_version:'v1',source_payload_hash:hash,parser_profile:'BANK_V1',status:'SUCCEEDED',row_count:2,missing_fields:[],parse_hash:hash,reason:'Controller requested a retained source parse.',created_at:'2026-09-13T00:00:00.000Z',idempotent:false,action_flags:{can_import:false,can_post:false}};
test('client retains only scope-bound source parse receipt',async()=>{let request;const r=await parseAuthoritativeSourceDocument({config:{baseUrl:'https://refs.example',tenantId,entityId,getAccessToken:async()=> 'a'.repeat(16)},sourceDocumentId,sourceVersion:'v1',expectedPayloadHash:hash,parserProfile:'BANK_V1',periodId,reason:row.reason,idempotencyKey:'source-parse-001',fetcher:async(url,options)=>(request={url,options},{status:201,ok:true,json:async()=>({ok:true,data:row})})});assert.equal(r.ok,true);assert.match(request.url,/source-documents/);assert.equal(request.options.method,'POST');assert.equal((await parseAuthoritativeSourceDocument({config:{tenantId,entityId},sourceDocumentId,sourceVersion:'v1',expectedPayloadHash:hash,parserProfile:'BANK_V1',periodId,reason:row.reason,idempotencyKey:'short'})).ok,false);});

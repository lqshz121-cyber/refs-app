import assert from 'node:assert/strict';import {refreshAuthoritativeAiPropertyRentRevenueReviews} from '../src/accounting-api.js';
const id=n=>`${String(n).padStart(8,'0')}-1111-4111-8111-${String(n).padStart(12,'0')}`,hash=n=>`sha256:${String(n).repeat(64).slice(0,64)}`,config={baseUrl:'https://accounting.example',entityId:id(1),periodId:id(2),getAccessToken:async()=>'controlled-token-value'};
const row={wbs_property_rent_source_admission_id:id(3),source_document_id:id(4),journal_entry_id:id(5),property_ref:'PROPERTY-1',unit_ref:'UNIT-101',lease_ref:'LEASE-1',tenant_ref:'TENANT-1',accounting_date:'2026-07-31',currency:'USD',expected_rent_amount:'500.0000',posted_revenue_amount:'100.0000',variance_amount:'400.0000',review_status:'POSTED_REVENUE_MISMATCH',rule_id:'AI_PROPERTY_RENT_POSTED_REVENUE_TIE_OUT_V1',risk_level:'HIGH',reason:'The source-bound rent charge differs from Posted revenue.',suggested_action:'Reconcile the source, mapping, Journal, and ledger evidence.',source_version:'snapshot:1:rent-1',receipt_hash:hash(1),source_evidence_hash:hash(2),mapping_snapshot_id:id(6),mapping_snapshot_hash:hash(3),created_at:'2026-08-01T00:00:00.000Z',can_create_draft:false,can_review:false,can_approve:false,can_post:false};
const run=payload=>refreshAuthoritativeAiPropertyRentRevenueReviews({config,limit:100,fetcher:async(url,options)=>{assert.equal(url,`https://accounting.example/api/v1/entities/${config.entityId}/ai/property-rent/revenue-review?periodId=${config.periodId}&limit=100`);assert.equal(options.method,'GET');assert.equal(options.cache,'no-store');assert.equal(options.headers.authorization,'Bearer controlled-token-value');return {ok:true,json:async()=>payload};}});
assert.equal((await run({ok:true,data:[row]})).ok,true);
for(const invalid of [{...row,can_post:true},{...row,variance_amount:'0.0000'},{...row,rule_id:'AI_PROPERTY_RENT_PERIOD_CUTOFF_V1'},{...row,receipt_hash:'secret'},{...row,raw_provider_payload:{credential:'forbidden'}}])assert.equal((await run({ok:true,data:[invalid]})).ok,false);
assert.equal((await run({ok:true,data:[row,row]})).ok,false);
console.log('authoritative AI Property Rent revenue review client: strict source-bound no-action DTO passed');

import assert from'node:assert/strict';import test from'node:test';import{refreshAuthoritativeAccountingSettings}from'../src/accounting-api.js';
const id=n=>`${String(n).padStart(8,'0')}-0000-4000-8000-${String(n).padStart(12,'0')}`,hash='sha256:'+'a'.repeat(64),config={baseUrl:'https://api.example.test',entityId:id(2),periodId:id(3),getAccessToken:async()=>'x'.repeat(32)};
const family=(name,index)=>({family:name,schema_version:`POLICY_${index}`,setting_snapshot_id:id(100+index),version:1,snapshot_hash:hash,approval_status:'APPROVED',approved_by:'controller',approved_at:'2026-08-28T00:00:00.000Z'}),families=['coa','vendor_treatment','project_property_cost_code','period_close_policy','tax','intercompany','materiality','approval_thresholds','report_mapping','loan_capitalization_policy'].map(family);
const data={schema_version:'AUTHORITATIVE_ACCOUNTING_SETTINGS_V1',scope:{tenant_id:id(1),entity_id:id(2),company_code:'WBPA',period_id:id(3),period_code:'2026-08',period_start:'2026-08-01',period_end:'2026-08-31',period_status:'OPEN',currency:'USD'},approval:{settings_snapshot_id:id(4),settings_version:1,settings_hash:hash,approval_status:'APPROVED',approved_by:'controller',approved_at:'2026-08-28T00:00:00.000Z'},families,period_close_policy:{allow_post:true,posting_lock:false,soft_lock:false,hard_lock:false,cutoff_date:'2026-08-31',accrual_cutoff_date:'2026-08-31',prepaid_boundary_date:'2026-08-31',reversal_policy:'NONE',prior_period_adjustment_policy:'BLOCKED',override_policy:'CONTROLLER_ONLY'},materiality:{currency:'USD',financial_statement_amount:'100.0000',budget_variance_amount:'100.0000',manual_je_amount:'100.0000',duplicate_amount:'100.0000',loan_difference_amount:'100.0000'},coverage:{active_posting_account_count:24,vendor_rule_count:1,dimension_rule_count:1,tax_code_count:1,intercompany_enabled:false,intercompany_counterparty_count:0,approval_level_count:4,classification_threshold_count:15,report_mapping_count:24,qualifying_loan_combination_count:1},action_flags:{can_create_draft:false,can_review:false,can_approve:false,can_post:false}};
test('browser reads exact approved settings with no-store',async()=>{let request;const result=await refreshAuthoritativeAccountingSettings({config,fetcher:async(url,init)=>(request={url,init},{ok:true,json:async()=>({ok:true,data})})});assert.equal(result.ok,true);assert.match(request.url,/accounting-settings\?periodId=/);assert.equal(request.init.cache,'no-store');for(const unsafe of [{...data,can_post:true},{...data,scope:{...data.scope,entity_id:id(9)}},{...data,action_flags:{...data.action_flags,can_post:true}},{...data,families:[...data.families,{...data.families[0]}]}])assert.equal((await refreshAuthoritativeAccountingSettings({config,fetcher:async()=>({ok:true,json:async()=>({ok:true,data:unsafe})})})).code,'ACCOUNTING_SETTINGS_PROTOCOL');});

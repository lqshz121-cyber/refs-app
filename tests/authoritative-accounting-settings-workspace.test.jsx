import assert from'node:assert/strict';import{readFileSync}from'node:fs';import test from'node:test';import React from'react';import{renderToStaticMarkup}from'react-dom/server';
import{AuthoritativeAccountingSettingsWorkspace}from'../src/authoritative-accounting-settings-workspace.jsx';import{navigationItemForRoute}from'../src/authoritative-navigation.js';
test('Accounting settings is a discoverable immutable read workspace',()=>{assert.equal(navigationItemForRoute('settings').availability,'API_READ');const html=renderToStaticMarkup(<AuthoritativeAccountingSettingsWorkspace config={{entityId:'22222222-2222-4222-8222-222222222222',periodId:'33333333-3333-4333-8333-333333333333'}}/>);assert.match(html,/Accounting settings/);assert.match(html,/approved for this company and period/);assert.doesNotMatch(html,/Save settings|Edit policy|Create Draft|Post journal/);});
test('Accounting settings renders approved calendar evidence rather than inferring cutoff days',()=>{const source=readFileSync('src/authoritative-accounting-settings-workspace.jsx','utf8');assert.match(source,/Business calendar/);assert.match(source,/Non-business dates/);assert.match(source,/Accrual cutoff/);assert.match(source,/Prepaid boundary/);});

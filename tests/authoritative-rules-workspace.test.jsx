import assert from 'node:assert/strict';import React from 'react';import {renderToStaticMarkup} from 'react-dom/server';import {AuthoritativeRulesWorkspace} from '../src/authoritative-rules-workspace.jsx';const markup=renderToStaticMarkup(<AuthoritativeRulesWorkspace config={{baseUrl:'https://fixture.example',entityId:'22222222-2222-4222-8222-222222222222'}}/>);assert.match(markup,/Rules/);assert.match(markup,/Read-only evidence/);assert.match(markup,/Bank rules/);assert.match(markup,/Integration rules/);assert.match(markup,/Loading Rules/);assert.doesNotMatch(markup,/New rule|Edit rule|Auto-post|Create Draft/);

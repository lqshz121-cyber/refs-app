import assert from 'node:assert/strict';import test from 'node:test';import {readFile} from 'node:fs/promises';
test('project loan-cost reader binds exact mappings, project dimensions, Posted period lines, and sources',async()=>{const up=await readFile(new URL('../db/migrations/240_ai_construction_loan_project_cost_read.sql',import.meta.url),'utf8');for(const token of ['refs_assert_ai_analysis_scope','CONSTRUCTION_LOAN_ACCOUNT_CLASSIFICATION','CWIP_ACCOUNT_CLASSIFICATION',"j.status='POSTED'","dimensions->>'project_ref'",'mapping_snapshot_hashes','source_document_ids','REVOKE ALL','GRANT EXECUTE'])assert.ok(up.includes(token),`missing ${token}`);assert.doesNotMatch(up,/INSERT INTO journal_entry|INSERT INTO ledger_line|UPDATE journal_entry/i);});

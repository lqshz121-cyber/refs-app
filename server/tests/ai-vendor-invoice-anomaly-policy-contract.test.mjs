import assert from 'node:assert/strict';import test from 'node:test';import {readFile} from 'node:fs/promises';
test('vendor anomaly threshold policy is approved, entity/period scoped, hash-bound and fail closed',async()=>{const sql=await readFile(new URL('../db/migrations/193_ai_vendor_invoice_anomaly_policy.sql',import.meta.url),'utf8');assert.match(sql,/family='AI_VENDOR_INVOICE_ANOMALY_POLICY'/);assert.match(sql,/status='APPROVED'/);assert.match(sql,/refs_assert_ai_analysis_scope/);assert.match(sql,/snapshot_hash IS DISTINCT FROM refs_jsonb_hash/);assert.match(sql,/minimum_history_periods.*BETWEEN 3 AND 24/s);assert.match(sql,/ratio_threshold_basis_points.*BETWEEN 15000 AND 100000/s);assert.match(sql,/AI_VENDOR_INVOICE_AMOUNT_ANOMALY_POLICY_V1/);assert.match(sql,/GRANT EXECUTE.*TO refs_app/s);});

import assert from 'node:assert/strict';import test from 'node:test';import {readFileSync} from 'node:fs';const read=p=>readFileSync(new URL(p,import.meta.url),'utf8'),up=read('../db/migrations/236_fixed_asset_register_evidence.sql'),server=read('../runtime/accounting-server.mjs');test('asset register is immutable source-bound and depreciation remains analysis-only',()=>{for(const token of ['CREATE TABLE fixed_asset_register_evidence','capitalization_proposal_id','placed_in_service_date','useful_life_months',"depreciation_method='STRAIGHT_LINE'","depreciation_convention='FULL_MONTH'",'register_evidence_hash','reject_mutation',"'AI.ANALYSIS.EXPLAIN'"])assert.ok(up.includes(token),`missing ${token}`);assert.doesNotMatch(up,/INSERT INTO journal_entry|INSERT INTO journal_line|INSERT INTO ledger_line/i);assert.match(server,/FIXED_ASSET_DEPRECIATION_SCHEDULE:fixedAssetDepreciationSchedule/);});

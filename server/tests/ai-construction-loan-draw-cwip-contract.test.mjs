import assert from 'node:assert/strict';import test from 'node:test';import {readFile} from 'node:fs/promises';
test('draw-to-CWIP review uses approved policy and one atomic, fail-closed population attestation',async()=>{const policy=await readFile(new URL('../db/migrations/247_ai_construction_loan_draw_cwip_policy.sql',import.meta.url),'utf8'),population=await readFile(new URL('../db/migrations/296_ai_construction_loan_cwip_population_attestation.sql',import.meta.url),'utf8'),server=await readFile(new URL('../runtime/accounting-server.mjs',import.meta.url),'utf8');for(const token of ['refs_assert_ai_analysis_scope','AI_CONSTRUCTION_LOAN_DRAW_CWIP_POLICY','AI_CONSTRUCTION_LOAN_DRAW_TO_CWIP_PERIOD_REVIEW_V1','refs_jsonb_hash','REVOKE ALL','GRANT EXECUTE'])assert.ok(policy.includes(token),`missing ${token}`);for(const token of ['refs_read_ai_construction_loan_cwip_population_attestation','AI_CONSTRUCTION_LOAN_CWIP_POPULATION_ATTESTATION_V1','refs_assert_ai_analysis_scope(p_tenant,p_entity)',"p.ledger_code='PRIMARY'",'current_candidate','current_exact','j.period_id=p.period_id AND l.period_id=p.period_id AND b.period_id=p.period_id','j.period_id=l.period_id','l.period_id=b.period_id',"hp.ledger_code='PRIMARY'",'j.currency::text=p.currency','l.currency::text=p.currency',"sl.link_type='JE_LINE_TO_LEDGER'",'sl.journal_line_id=l.journal_line_id','sl.posting_batch_id=l.posting_batch_id','sl.ledger_line_id=l.ledger_line_id','effective_mappings','UNION SELECT DISTINCT h.account_code FROM highest h','ZERO_CURRENT_PERIOD_ACTIVITY','current_activity_line_count','dense_rank()OVER','priority_rank=1',"e.loan_classification='NOT_CONSTRUCTION_LOAN'","e.cwip_classification='NOT_CWIP'",'explicit_non_target','non_target_rows','non_target_count','unclassified_rows','population_watermark','invalid_lineage_count','population_hash','LANGUAGE plpgsql STABLE'])assert.ok(population.includes(token),`missing ${token}`);assert.doesNotMatch(population,/refs_get_construction_loan_rollforward|refs_get_cwip_rollforward|refs_assert_ai_analysis_scope\(p_tenant,p_entity,p_period\)/);for(const token of ['getAiConstructionLoanCwipPopulationAttestation','analyzeAttestedConstructionLoanDrawCwip','CONSTRUCTION_LOAN_DRAW_CWIP:constructionLoanDrawCwip'])assert.ok(server.includes(token),`missing ${token}`);assert.doesNotMatch(`${policy}\n${population}`,/INSERT INTO journal_entry|INSERT INTO ledger_line|UPDATE journal_entry/i);});

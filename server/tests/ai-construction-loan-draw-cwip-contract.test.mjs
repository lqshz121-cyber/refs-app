import assert from 'node:assert/strict';import test from 'node:test';import {readFile} from 'node:fs/promises';
test('draw-to-CWIP review uses approved policy and existing Posted mapped rollforwards only',async()=>{const up=await readFile(new URL('../db/migrations/239_ai_construction_loan_draw_cwip_policy.sql',import.meta.url),'utf8'),server=await readFile(new URL('../runtime/accounting-server.mjs',import.meta.url),'utf8');for(const token of ['refs_assert_ai_analysis_scope','AI_CONSTRUCTION_LOAN_DRAW_CWIP_POLICY','AI_CONSTRUCTION_LOAN_DRAW_TO_CWIP_PERIOD_REVIEW_V1','refs_jsonb_hash','REVOKE ALL','GRANT EXECUTE'])assert.ok(up.includes(token),`missing ${token}`);for(const token of ['getConstructionLoanRollforward','getCwipRollforward','CONSTRUCTION_LOAN_DRAW_CWIP:constructionLoanDrawCwip'])assert.ok(server.includes(token),`missing ${token}`);assert.doesNotMatch(up,/INSERT INTO journal_entry|INSERT INTO ledger_line|UPDATE journal_entry/i);});

import assert from 'node:assert/strict';import test from 'node:test';import {readFileSync} from 'node:fs';const up=readFileSync(new URL('../db/migrations/243_ai_fixed_asset_impairment_posted_reconciliation.sql',import.meta.url),'utf8');test('impairment reconciliation reads only assessment-bound POSTED lines and remains write-free',()=>{for(const token of ["'AI.ANALYSIS.EXPLAIN'","l.dimensions->>'impairment_assessment_evidence_id'","j.status='POSTED'",'expected_impairment_loss','posted_impairment_expense','posted_accumulated_impairment'])assert.ok(up.includes(token),`missing ${token}`);assert.doesNotMatch(up,/INSERT INTO|UPDATE\s+|DELETE FROM/i);});

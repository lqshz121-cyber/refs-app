import assert from 'node:assert/strict';import test from 'node:test';import {readFileSync} from 'node:fs';const up=readFileSync(new URL('../db/migrations/238_ai_fixed_asset_posted_reconciliation.sql',import.meta.url),'utf8');test('asset reconciliation reads only asset-id-bound POSTED lines with analysis authority',()=>{for(const token of ["'AI.ANALYSIS.EXPLAIN'","l.dimensions->>'fixed_asset_register_evidence_id'","j.status='POSTED'",'expected_accumulated_depreciation','posted_accumulated_depreciation','journal_entry_ids','ledger_line_ids'])assert.ok(up.includes(token),`missing ${token}`);assert.doesNotMatch(up,/INSERT INTO|UPDATE\s+|DELETE FROM/i);});

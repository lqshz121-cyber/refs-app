import assert from 'node:assert/strict';import test from 'node:test';import {readFile} from 'node:fs/promises';
const up=await readFile(new URL('../db/migrations/199_ai_vendor_invoice_near_duplicate_policy.sql',import.meta.url),'utf8'),down=await readFile(new URL('../db/migrations/down/199_ai_vendor_invoice_near_duplicate_policy.sql',import.meta.url),'utf8');
test('near-duplicate policy is exact, approved, hash-bound, period-scoped, and fail closed',()=>{for(const token of ['refs_assert_ai_analysis_scope','accounting_period','AI_VENDOR_INVOICE_NEAR_DUPLICATE_POLICY','scope_type=\'ENTITY\'','status=\'APPROVED\'','match_count<>1','FOR SHARE','refs_jsonb_hash','AI_VENDOR_INVOICE_NEAR_DUPLICATE_POLICY_SNAPSHOT_V1','AI_VENDOR_INVOICE_NORMALIZED_ID_AMOUNT_DATE_V1','maximum_date_gap_days','maximum_amount_variance_basis_points','maximum_absolute_amount_variance','AI_VENDOR_INVOICE_NEAR_DUPLICATE_POLICY_V1'])assert.match(up,new RegExp(token));assert.match(down,/DROP FUNCTION refs_read_ai_vendor_invoice_near_duplicate_policy/);});

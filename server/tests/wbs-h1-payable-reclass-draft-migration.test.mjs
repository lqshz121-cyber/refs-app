import test from 'node:test';import assert from 'node:assert/strict';import {readFile} from 'node:fs/promises';
test('269 creates only a human WBS mapping Draft and retains exact lineage',async()=>{const up=await readFile(new URL('../db/migrations/269_wbs_h1_payable_reclass_draft.sql',import.meta.url),'utf8'),down=await readFile(new URL('../db/migrations/down/269_wbs_h1_payable_reclass_draft.sql',import.meta.url),'utf8');for(const token of ["'WBS.H1.PAYABLE.DRAFT'","'GL.JE.CREATE'",'refs_read_wbs_h1_payable_accounting_proposal','wbs_test_import_draft',"original.status<>'POSTED'",'VERIFIED_CLEAN','refs_create_manual_journal','WBS_H1_PAYABLE_RECLASS_DRAFT_CREATED',"'status','DRAFT'","'can_post',false",'reject_mutation','Refusing to drop retained WBS H1 Payable reclassification Draft evidence'])assert.match(up+'\n'+down,new RegExp(token.replace(/[.*+?^${}()|[\]\\]/g,'\\$&')));for(const forbidden of ['refs_transition_journal','refs_post_journal','INSERT INTO ledger_line','status\x27,\x27POSTED'])assert.doesNotMatch(up,new RegExp(forbidden));});

import assert from 'node:assert/strict';import test from 'node:test';import {readFileSync} from 'node:fs';const up=readFileSync(new URL('../db/migrations/241_ai_fixed_asset_post_disposal_depreciation.sql',import.meta.url),'utf8');test('reader uses reviewed disposal, exact date cutoff and asset-bound POSTED lines without writes',()=>{for(const token of ["'AI.ANALYSIS.EXPLAIN'","d.status='REVIEWED'","j.status='POSTED'","j.journal_date>d.disposal_date","l.dimensions->>'fixed_asset_register_evidence_id'",'disposal_evidence_hash'])assert.ok(up.includes(token),`missing ${token}`);assert.doesNotMatch(up,/INSERT INTO|UPDATE\s+|DELETE FROM/i);});

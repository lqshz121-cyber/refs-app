import assert from 'node:assert/strict';import fs from 'node:fs';import test from 'node:test';
const up=fs.readFileSync(new URL('../db/migrations/196_ai_vendor_invoice_frequency_anomaly_finding.sql',import.meta.url),'utf8'),down=fs.readFileSync(new URL('../db/migrations/down/196_ai_vendor_invoice_frequency_anomaly_finding.sql',import.meta.url),'utf8');
test('frequency findings and every source trace are append-only, exact retained evidence',()=>{for(const token of ['ai_vendor_invoice_frequency_anomaly_finding','ai_vendor_invoice_frequency_anomaly_source','ENABLE ROW LEVEL SECURITY','reject_mutation','current_source_trace','source_document_id','source_document_line_id','source_payload_hash','source_line_hash','wbs_final1_retained_source_row','domain=\'PAYABLES\'','accounting_period_id=p_period','FOR SHARE','source document lineage changed'])assert.match(up,new RegExp(token));});
test('frequency materialization is actor-idempotent and atomically emits audit/outbox with zero actions',()=>{for(const token of ['refs_ai_vendor_invoice_frequency_anomaly_batch_hash','AI_VENDOR_INVOICE_FREQUENCY_ANOMALY:','idem.actor_id IS DISTINCT FROM actor','FOR UPDATE','AI_VENDOR_INVOICE_FREQUENCY_ANOMALY_MATERIALIZED','INSERT INTO audit_event','INSERT INTO outbox_event','can_create_draft\',false','can_review\',false','can_approve\',false','can_post\',false'])assert.match(up,new RegExp(token));});
test('rollback refuses to erase retained frequency evidence',()=>{assert.match(down,/Cannot erase retained vendor frequency anomaly evidence/);assert.match(down,/ERRCODE='55000'/);});

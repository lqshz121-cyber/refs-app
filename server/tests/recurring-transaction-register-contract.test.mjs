import test from 'node:test';import assert from 'node:assert/strict';import {readFile} from 'node:fs/promises';test('recurring transaction SQL is signed-source scoped, paged, audited, redacted, and action-free',async()=>{const sql=await readFile(new URL('../db/migrations/369_recurring_transaction_register_read.sql',import.meta.url),'utf8');for(const token of ["'AP.VIEW'",'wbs_final1_retained_source_row',"r.domain='PAYABLES'",'signed_recurring_obligation_id','signed_service_period_start','signed_service_frequency','refs_recurring_date','raw_row_hash','e.is_current','audit_event','refs_rule_redact_text','p_after','p_limit','can_execute','can_manage_payment'])assert.match(sql,new RegExp(token.replace(/[.*+?^${}()|[\]\\]/g,'\\$&')));assert.doesNotMatch(sql,/INSERT INTO|UPDATE (source_document|journal_entry|ledger_line)|DELETE FROM/);});

import assert from'node:assert/strict';import test from'node:test';import{randomUUID}from'node:crypto';import{createAccountingApi}from'../api/accounting-http.mjs';
const tenantId=randomUUID(),entityId=randomUUID(),periodId=randomUUID(),auditId=randomUUID(),outboxId=randomUUID(),hash=`sha256:${'a'.repeat(64)}`,principal={trusted:true,tenantId,actorId:'reader'};
const item={schema_version:'PERIOD_CLOSE_HISTORY_ITEM_V1',audit_event_id:auditId,period_id:periodId,period_code:'2026-08',version:'4',status:'CLOSED',recorded_at:'2026-08-24T12:00:00.000Z',closed_by:'auth0|controller',readiness_hash:hash,settings_snapshot_id:randomUUID(),settings_hash:hash,close_policy_snapshot_id:randomUUID(),close_policy_hash:hash,financial_statement_snapshot_id:randomUUID(),financial_statement_snapshot_hash:hash,ledger_evidence_hash:hash,reason_hash:hash,command_reference_hash:hash,outbox_event_id:outboxId,outbox_payload_hash:hash,delivery:{status:'PENDING',attempt_count:0,published_at:null},integrity_verified:true};
const page={schema_version:'PERIOD_CLOSE_HISTORY_PAGE_V1',scope:{tenant_id:tenantId,entity_id:entityId,period_id:periodId},total_count:1,read_count:1,items:[item],has_more:false,next_cursor:null,redaction:{reason_hashed:true,command_reference_hashed:true,credential_shaped_actor_redacted:true},action_flags:{can_create_draft:false,can_review:false,can_approve:false,can_post:false}};
test('Period close history GET is exact, scoped, no-store and read only',async()=>{let args;const api=createAccountingApi({authenticate:async()=>principal,kernelFactory:async()=>({readPeriodCloseHistory:async input=>(args=input,page)})});const path=`/api/v1/entities/${entityId}/periods/${periodId}/close-history?limit=25`,response=await api({method:'GET',url:path,headers:{},body:null});assert.equal(response.status,200);assert.equal(response.headers['cache-control'],'no-store');assert.deepEqual(args,{tenantId,entityId,periodId,limit:25,cursorAt:null,cursorId:null});assert.equal(response.body.data.items[0].reason,undefined);for(const request of [{method:'GET',url:`${path}&edit=true`,headers:{},body:null},{method:'GET',url:path,headers:{'if-match':'"4"'},body:null},{method:'GET',url:path,headers:{},body:{}}])assert.equal((await api(request)).status,400);});
test('Period close history rejects action, secret, scope, delivery and cursor drift',async()=>{for(const unsafe of [{...page,action_flags:{...page.action_flags,can_post:true}},{...page,items:[{...item,closed_by:'Bearer unsafe-token'}]},{...page,scope:{...page.scope,entity_id:randomUUID()}},{...page,items:[{...item,delivery:{...item.delivery,status:'UNKNOWN'}}]},{...page,has_more:true,next_cursor:{recorded_at:item.recorded_at,audit_event_id:randomUUID()}}]){const api=createAccountingApi({authenticate:async()=>principal,kernelFactory:async()=>({readPeriodCloseHistory:async()=>unsafe})}),response=await api({method:'GET',url:`/api/v1/entities/${entityId}/periods/${periodId}/close-history?limit=25`,headers:{},body:null});assert.equal(response.status,502);}});

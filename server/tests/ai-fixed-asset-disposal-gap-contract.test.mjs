import assert from 'node:assert/strict';import test from 'node:test';import {readFileSync} from 'node:fs';const up=readFileSync(new URL('../db/migrations/239_ai_fixed_asset_disposal_gap_read.sql',import.meta.url),'utf8');
test('disposal gap reader is exact-period asset-bound Posted and analysis-only',()=>{for(const token of ["'AI.ANALYSIS.EXPLAIN'","j.status='POSTED'","l.dimensions->>'fixed_asset_register_evidence_id'",'l.credit_amount>l.debit_amount','disposal_evidence_status','posted_source_document_ids'])assert.ok(up.includes(token),`missing ${token}`);assert.doesNotMatch(up,/INSERT INTO|UPDATE\s+|DELETE FROM/i);});

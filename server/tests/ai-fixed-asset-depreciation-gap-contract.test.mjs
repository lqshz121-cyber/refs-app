import assert from 'node:assert/strict';import test from 'node:test';import {readFileSync} from 'node:fs';const read=p=>readFileSync(new URL(p,import.meta.url),'utf8'),up=read('../db/migrations/235_ai_fixed_asset_depreciation_gap_read.sql'),server=read('../runtime/accounting-server.mjs');test('fixed asset gap reader is mapping-bound, Posted-only, analysis-only and raw-free',()=>{for(const token of ["'AI.ANALYSIS.EXPLAIN'",'FIXED_ASSET_ACCOUNT_CLASSIFICATION',"j.status='POSTED'",'mapping_snapshot_hash','source_document_ids','NOT_ESTABLISHED'])assert.ok(up.includes(token),`missing ${token}`);for(const forbidden of ['raw_event.payload','request_raw','credential','authorization','INSERT INTO','UPDATE ','DELETE FROM'])assert.doesNotMatch(up,new RegExp(forbidden,'i'));assert.match(server,/FIXED_ASSET_DEPRECIATION:fixedAssetDepreciation/);});

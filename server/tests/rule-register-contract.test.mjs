import test from 'node:test';import assert from 'node:assert/strict';import {readFile} from 'node:fs/promises';test('rule register SQL is scoped, integrity checked, paged, audited, redacted, and action-free',async()=>{const sql=await readFile(new URL('../db/migrations/368_rule_register_read.sql',import.meta.url),'utf8');for(const token of ["'BANK.VIEW'","'WBS.AUTOREC.VIEW'",'refs_jsonb_hash','refs_rule_json_is_safe','refs_rule_text_is_safe','refs_rule_redact_text','rule_evaluation','audit_event','p_after','p_limit','can_auto_categorize',"family IN('BANK','WBS_AUTOREC_MATCH')"])assert.match(sql,new RegExp(token.replace(/[.*+?^${}()|[\]\\]/g,'\\$&')));assert.doesNotMatch(sql,/INSERT INTO|UPDATE mapping_snapshot|DELETE FROM mapping_snapshot/);});

const UUID=/^[0-9a-f]{8}-[0-9a-f]{4}-[1-5][0-9a-f]{3}-[89ab][0-9a-f]{3}-[0-9a-f]{12}$/i,HASH=/^sha256:[0-9a-f]{64}$/,MONEY=/^(?:0|[1-9]\d*)\.\d{4}$/,DATE=/^\d{4}-\d{2}-\d{2}$/,ACTIONS=Object.freeze({can_create_draft:false,can_review:false,can_approve:false,can_post:false}),units=value=>BigInt(value.replace('.',''));
const SOURCE_KEYS='schema_version period_id period_code period_starts_on period_ends_on fixed_asset_register_evidence_id source_document_id source_payload_hash capitalization_proposal_id register_evidence_hash schedule_basis schedule_snapshot_hash post_impairment_policy_evidence_hash post_impairment_policy_identity asset_tag asset_class depreciation_method depreciation_convention placed_in_service_date useful_life_months currency cost_basis salvage_value depreciable_basis actual_posted_accumulated_impairment expected_period_depreciation expected_accumulated_depreciation expected_net_book_value asset_account_code accumulated_depreciation_account_code depreciation_expense_account_code status'.split(' ').sort();
const POLICY_KEYS='policy_id impairment_assessment_evidence_id impairment_journal_entry_id effective_period_id'.split(' ').sort();
const exactKeys=(value,keys)=>value&&typeof value==='object'&&!Array.isArray(value)&&JSON.stringify(Object.keys(value).sort())===JSON.stringify(keys);
const calendarDate=value=>{if(!DATE.test(value||''))return false;const [year,month,day]=value.split('-').map(Number),date=new Date(Date.UTC(year,month-1,day));return date.getUTCFullYear()===year&&date.getUTCMonth()===month-1&&date.getUTCDate()===day;};
const policyEvidence=row=>{const hash=row.post_impairment_policy_evidence_hash,identity=row.post_impairment_policy_identity,impairment=row.actual_posted_accumulated_impairment;if(hash===null||identity===null)return hash===null&&identity===null&&impairment==='0.0000';return HASH.test(hash)&&exactKeys(identity,POLICY_KEYS)&&POLICY_KEYS.every(key=>UUID.test(identity[key]||''))&&MONEY.test(impairment||'')&&units(impairment)>0n;};
const valid=row=>exactKeys(row,SOURCE_KEYS)&&row.schema_version==='AI_FIXED_ASSET_DEPRECIATION_SOURCE_V2'&&UUID.test(row.period_id||'')&&/^\d{4}-(?:0[1-9]|1[0-2])$/.test(row.period_code||'')&&calendarDate(row.period_starts_on)&&calendarDate(row.period_ends_on)&&row.period_starts_on<=row.period_ends_on&&UUID.test(row.fixed_asset_register_evidence_id||'')&&UUID.test(row.source_document_id||'')&&HASH.test(row.source_payload_hash||'')&&UUID.test(row.capitalization_proposal_id||'')&&HASH.test(row.register_evidence_hash||'')&&['ORIGINAL_REGISTER','POST_IMPAIRMENT_REVISED'].includes(row.schedule_basis)&&HASH.test(row.schedule_snapshot_hash||'')&&typeof row.asset_tag==='string'&&row.asset_tag.trim().length>0&&typeof row.asset_class==='string'&&row.asset_class.trim().length>0&&row.depreciation_method==='STRAIGHT_LINE'&&row.depreciation_convention==='FULL_MONTH'&&calendarDate(row.placed_in_service_date)&&Number.isSafeInteger(row.useful_life_months)&&row.useful_life_months>=1&&row.useful_life_months<=600&&/^[A-Z]{3}$/.test(row.currency||'')&&[row.cost_basis,row.salvage_value,row.depreciable_basis,row.actual_posted_accumulated_impairment,row.expected_period_depreciation,row.expected_accumulated_depreciation,row.expected_net_book_value].every(value=>MONEY.test(value||''))&&policyEvidence(row)&&(row.schedule_basis!=='POST_IMPAIRMENT_REVISED'||row.post_impairment_policy_identity!==null)&&units(row.expected_net_book_value)===units(row.cost_basis)-units(row.expected_accumulated_depreciation)-units(row.actual_posted_accumulated_impairment)&&[row.asset_account_code,row.accumulated_depreciation_account_code,row.depreciation_expense_account_code].every(value=>/^[0-9A-Za-z._-]{1,64}$/.test(value||''))&&row.status==='ACTIVE';
export function detectFixedAssetDepreciationReviews(rows,{entityId,accountingPeriodId,limit=500}={}){if(!Array.isArray(rows)||rows.length>5000||!UUID.test(entityId||'')||!UUID.test(accountingPeriodId||'')||!Number.isSafeInteger(limit)||limit<1||limit>500||rows.some(row=>!valid(row)||row.period_id!==accountingPeriodId))throw Object.assign(new Error('Depreciation review requires the exact authoritative schedule and policy evidence.'),{code:'AI_FIXED_ASSET_DEPRECIATION_REVIEW_EVIDENCE_INVALID'});const findings=[];for(const row of rows){if(row.expected_period_depreciation==='0.0000')continue;findings.push(Object.freeze({schema_version:'AI_FIXED_ASSET_DEPRECIATION_REVIEW_V1',finding_type:'FIXED_ASSET_DEPRECIATION_DUE',risk_level:'MEDIUM',rule_id:'AI_FIXED_ASSET_AUTHORITATIVE_SCHEDULE_V2',entity_id:entityId,accounting_period_id:accountingPeriodId,...row,proposed_journal_entry:Object.freeze({journal_type:'DEPRECIATION',debit_account_code:row.depreciation_expense_account_code,credit_account_code:row.accumulated_depreciation_account_code,amount:row.expected_period_depreciation,currency:row.currency,memo:`Monthly depreciation for asset ${row.asset_tag}`,status:'SUGGESTED_ONLY'}),reason:`Authoritative ${row.schedule_basis} schedule ${row.schedule_snapshot_hash} calculates ${row.expected_period_depreciation} of depreciation for ${row.period_code}.`,suggested_action:'Reconcile the authoritative schedule to Posted cost, impairment, and accumulated depreciation, then have a qualified accountant review any Draft depreciation entry.',required_human_fields:Object.freeze(['posted_cost_reconciliation','accumulated_depreciation_reconciliation','impairment_or_disposal_review','period_review','controller_approval']),action_flags:ACTIONS}));}if(findings.length>limit)throw Object.assign(new Error('Fixed asset depreciation finding population exceeds the requested complete response bound.'),{code:'AI_FIXED_ASSET_DEPRECIATION_FINDING_POPULATION_INCOMPLETE'});return Object.freeze({schema_version:'AI_FIXED_ASSET_DEPRECIATION_REVIEW_BATCH_V1',scanned_asset_count:rows.length,finding_count:findings.length,findings:Object.freeze(findings),action_flags:ACTIONS});}

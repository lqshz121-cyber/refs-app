import {detectVendorAccountCodingDrift} from './ai-vendor-account-coding-drift.mjs';
const UUID=/^[0-9a-f]{8}-[0-9a-f]{4}-[1-5][0-9a-f]{3}-[89ab][0-9a-f]{3}-[0-9a-f]{12}$/i;
export function createAiVendorAccountCodingDriftService({postedCodingHistoryReader}={}){if(typeof postedCodingHistoryReader!=='function')throw new TypeError('Vendor account coding drift service requires one read-only Posted coding history reader.');return Object.freeze({async analyze({tenantId,entityId,accountingPeriodId,limit=2000}={}){if(!UUID.test(tenantId||'')||!UUID.test(entityId||'')||!UUID.test(accountingPeriodId||'')||!Number.isSafeInteger(limit)||limit<1||limit>2000)throw Object.assign(new Error('Vendor account coding drift service scope is invalid.'),{code:'AI_VENDOR_ACCOUNT_CODING_DRIFT_SCOPE_INVALID'});const rows=await postedCodingHistoryReader({tenantId,entityId,accountingPeriodId,limit});return detectVendorAccountCodingDrift(rows,{currentAccountingPeriodId:accountingPeriodId});}});}

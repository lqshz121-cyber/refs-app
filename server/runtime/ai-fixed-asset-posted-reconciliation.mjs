const UUID=/^[0-9a-f]{8}-[0-9a-f]{4}-[1-5][0-9a-f]{3}-[89ab][0-9a-f]{3}-[0-9a-f]{12}$/i,HASH=/^sha256:[0-9a-f]{64}$/,MONEY=/^-?(?:0|[1-9]\d*)\.\d{4}$/,ACTIONS=Object.freeze({can_create_draft:false,can_review:false,can_approve:false,can_post:false}),units=v=>BigInt(v.replace('.',''));
const ids=v=>Array.isArray(v)&&v.length<=500&&v.every(x=>UUID.test(x||''))&&new Set(v).size===v.length;
const valid=r=>r&&UUID.test(r.period_id||'')&&UUID.test(r.fixed_asset_register_evidence_id||'')&&HASH.test(r.register_evidence_hash||'')&&typeof r.asset_tag==='string'&&r.asset_tag.trim()&&/^[A-Z]{3}$/.test(r.currency||'')&&[r.expected_period_depreciation,r.expected_accumulated_depreciation,r.posted_period_depreciation_expense,r.posted_accumulated_depreciation,r.period_variance,r.accumulated_variance].every(v=>MONEY.test(v||''))&&ids(r.journal_entry_ids)&&ids(r.journal_line_ids)&&ids(r.ledger_line_ids)&&r.lineage_status==='ASSET_ID_BOUND_POSTED';
export function detectFixedAssetPostedReconciliation(rows,{entityId,accountingPeriodId,limit=500}={}){if(!Array.isArray(rows)||rows.length>5000||!UUID.test(entityId||'')||!UUID.test(accountingPeriodId||'')||!Number.isSafeInteger(limit)||limit<1||limit>500||rows.some(r=>!valid(r)||r.period_id!==accountingPeriodId))throw Object.assign(new Error('Fixed asset Posted reconciliation requires exact asset-bound ledger evidence.'),{code:'AI_FIXED_ASSET_POSTED_RECONCILIATION_EVIDENCE_INVALID'});const findings=[];for(const r of rows){if(units(r.period_variance)===0n&&units(r.accumulated_variance)===0n)continue;findings.push(Object.freeze({schema_version:'AI_FIXED_ASSET_POSTED_RECONCILIATION_V1',finding_type:'FIXED_ASSET_DEPRECIATION_POSTED_MISMATCH',risk_level:'HIGH',rule_id:'AI_FIXED_ASSET_EXPECTED_VS_POSTED_V1',entity_id:entityId,accounting_period_id:accountingPeriodId,...r,reason:`Asset ${r.asset_tag} expected period/accumulated depreciation ${r.expected_period_depreciation}/${r.expected_accumulated_depreciation}, but Posted evidence is ${r.posted_period_depreciation_expense}/${r.posted_accumulated_depreciation}.`,suggested_action:'Inspect asset-bound Posted lines, register assumptions, impairment and disposal evidence; prepare a separately reviewed correction only if supported.',required_human_fields:Object.freeze(['asset_register_recalculation','posted_line_review','impairment_or_disposal_review','correction_decision','controller_approval']),action_flags:ACTIONS}));if(findings.length===limit)break;}return Object.freeze({schema_version:'AI_FIXED_ASSET_POSTED_RECONCILIATION_BATCH_V1',scanned_asset_count:rows.length,finding_count:findings.length,findings:Object.freeze(findings),action_flags:ACTIONS});}

import {detectVendorPaymentTermsDrift} from './ai-vendor-payment-terms-drift.mjs';
const UUID=/^[0-9a-f]{8}-[0-9a-f]{4}-[1-5][0-9a-f]{3}-[89ab][0-9a-f]{3}-[0-9a-f]{12}$/i;
export function createAiVendorPaymentTermsDriftService({paymentTermsHistoryReader}={}){if(typeof paymentTermsHistoryReader!=='function')throw new TypeError('Vendor payment-terms drift service requires one read-only Posted AP history reader.');return Object.freeze({async analyze({tenantId,entityId,accountingPeriodId,limit=2000}={}){if(!UUID.test(tenantId||'')||!UUID.test(entityId||'')||!UUID.test(accountingPeriodId||'')||!Number.isSafeInteger(limit)||limit<1||limit>2000)throw Object.assign(new Error('Vendor payment-terms drift service scope is invalid.'),{code:'AI_VENDOR_PAYMENT_TERMS_SCOPE_INVALID'});const rows=await paymentTermsHistoryReader({tenantId,entityId,accountingPeriodId,limit});return detectVendorPaymentTermsDrift(rows,{currentAccountingPeriodId:accountingPeriodId});}});}

import {detectApInvoiceCutoffRisks} from './ai-ap-invoice-cutoff-review.mjs';
const UUID=/^[0-9a-f]{8}-[0-9a-f]{4}-[1-5][0-9a-f]{3}-[89ab][0-9a-f]{3}-[0-9a-f]{12}$/i;
export function createAiApInvoiceCutoffReviewService({invoiceCutoffReader}={}){if(typeof invoiceCutoffReader!=='function')throw new TypeError('AP invoice cutoff review service requires one read-only evidence reader.');return Object.freeze({async analyze({tenantId,entityId,accountingPeriodId,limit=2000}={}){if(!UUID.test(tenantId||'')||!UUID.test(entityId||'')||!UUID.test(accountingPeriodId||'')||!Number.isSafeInteger(limit)||limit<1||limit>2000)throw Object.assign(new Error('AP invoice cutoff service scope is invalid.'),{code:'AI_AP_INVOICE_CUTOFF_SCOPE_INVALID'});const rows=await invoiceCutoffReader({tenantId,entityId,accountingPeriodId,limit});return detectApInvoiceCutoffRisks(rows,{currentAccountingPeriodId:accountingPeriodId});}});}

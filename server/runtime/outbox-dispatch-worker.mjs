import {setTimeout as delay} from 'node:timers/promises';

export function outboxDispatchHealthResponse(worker){
  const health=worker?.health?.(),readiness=health?.readiness,ready=health?.ok===true&&readiness?.schema_version==='OUTBOX_DISPATCH_READINESS_V1'&&readiness.ready===true;
  return Object.freeze({status:ready?200:503,headers:Object.freeze({'cache-control':'no-store','content-type':'application/json'}),body:Object.freeze({schema_version:'OUTBOX_DISPATCH_PUBLIC_READINESS_V1',ready,scope_count:Number.isSafeInteger(health?.scope_count)?health.scope_count:0,backlog:Object.freeze({pending_count:Number.isSafeInteger(readiness?.pending_count)?readiness.pending_count:0,failed_count:Number.isSafeInteger(readiness?.failed_count)?readiness.failed_count:0,oldest_pending_at:readiness?.oldest_pending_at??null}),checked_at:readiness?.checked_at??null})});
}

export class OutboxDispatchWorker{
  constructor({service,principal,scopes,readinessProbe=null,batchSize=100,intervalMs=5000,maxBackoffMs=300000,concurrency=2,logger=console,sleeper=delay,clock=()=>Date.now()}={}){
    if(!service||!principal?.trusted||!principal.actorId||!Array.isArray(scopes)||scopes.length===0||scopes.some(scope=>!scope.tenantId||!scope.entityId))throw new Error('Outbox worker requires service identity and tenant/entity scopes');
    if(!Number.isInteger(batchSize)||batchSize<1||batchSize>500||!Number.isInteger(concurrency)||concurrency<1||concurrency>16)throw new Error('Outbox worker limits are invalid');
    if(readinessProbe!==null&&typeof readinessProbe!=='function')throw new Error('Outbox readiness probe must be a function');
    this.service=service;this.principal=Object.freeze({...principal});this.scopes=scopes.map(scope=>Object.freeze({tenantId:scope.tenantId,entityId:scope.entityId}));const grouped=new Map();for(const scope of this.scopes){const ids=grouped.get(scope.tenantId)||[];ids.push(scope.entityId);grouped.set(scope.tenantId,ids);}this.dispatchScopes=[...grouped].map(([tenantId,entityIds])=>Object.freeze({tenantId,entityIds:Object.freeze(entityIds)}));this.readinessProbe=readinessProbe;this.readiness=null;this.batchSize=batchSize;this.intervalMs=intervalMs;this.maxBackoffMs=maxBackoffMs;this.concurrency=concurrency;this.logger=logger;this.sleeper=sleeper;this.clock=clock;this.running=false;this.stopping=false;this.loopPromise=null;this.abort=null;this.metrics={cycles:0,claimed:0,published:0,retried:0,deadLettered:0,cycleErrors:0,lastSuccessAt:null,lastErrorAt:null,lastReadinessCheckAt:null};
  }
  health(){return {schema_version:'OUTBOX_DISPATCH_HEALTH_V1',ok:this.running&&!this.stopping&&this.readiness?.ready===true,running:this.running,stopping:this.stopping,scope_count:this.scopes.length,readiness:this.readiness,metrics:{...this.metrics}};}
  async checkReadiness(){if(!this.readinessProbe)throw Object.assign(new Error('Outbox readiness preflight is required'),{code:'OUTBOX_DISPATCH_PREFLIGHT_UNAVAILABLE'});try{const evidence=await this.readinessProbe();if(evidence?.schema_version!=='OUTBOX_DISPATCH_READINESS_V1'||evidence.ready!==true||evidence.scope_count!==this.scopes.length)throw Object.assign(new Error('Outbox readiness evidence is invalid'),{code:'OUTBOX_DISPATCH_PREFLIGHT_INVALID'});this.readiness=evidence;this.metrics.lastReadinessCheckAt=new Date(this.clock()).toISOString();return evidence;}catch(error){this.readiness=null;this.metrics.lastErrorAt=new Date(this.clock()).toISOString();throw error;}}
  async runCycle(){await this.checkReadiness();let cursor=0;const results=[];const consume=async()=>{while(cursor<this.dispatchScopes.length){const scope=this.dispatchScopes[cursor++];try{const batch=await this.service.runOnce(this.principal,{...scope,limit:this.batchSize});this.metrics.claimed+=batch.length;for(const result of batch){if(result.status==='PUBLISHED')this.metrics.published++;else if(result.status==='PENDING')this.metrics.retried++;else this.metrics.deadLettered++;}results.push(...batch);}catch(error){this.metrics.cycleErrors++;this.metrics.lastErrorAt=new Date(this.clock()).toISOString();this.logger.error?.(JSON.stringify({event:'outbox_dispatch_scope_failed',tenantId:scope.tenantId,entityCount:scope.entityIds.length,errorCode:/^[A-Z0-9_]{3,80}$/.test(error?.code||'')?error.code:'OUTBOX_DISPATCH_INTERNAL'}));throw error;}}};await Promise.all(Array.from({length:Math.min(this.concurrency,this.dispatchScopes.length)},consume));this.metrics.cycles++;this.metrics.lastSuccessAt=new Date(this.clock()).toISOString();return results;}
  start(){if(this.loopPromise)return this.loopPromise;this.running=true;this.stopping=false;this.abort=new AbortController();this.loopPromise=this.loop(this.abort.signal).finally(()=>{this.running=false;this.loopPromise=null;});return this.loopPromise;}
  async loop(signal){let backoff=this.intervalMs;while(!signal.aborted){try{await this.runCycle();backoff=this.intervalMs;}catch{backoff=Math.min(this.maxBackoffMs,Math.max(this.intervalMs,backoff*2));}try{await this.sleeper(backoff,undefined,{signal});}catch(error){if(!signal.aborted)throw error;}}}
  async stop(){if(!this.loopPromise)return;this.stopping=true;this.abort.abort();await this.loopPromise;}
}

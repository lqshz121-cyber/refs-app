const UUID=/^[0-9a-f]{8}-[0-9a-f]{4}-[1-5][0-9a-f]{3}-[89ab][0-9a-f]{3}-[0-9a-f]{12}$/i;
const SHA=/^sha256:[0-9a-f]{64}$/;
const DATE=/^\d{4}-\d{2}-\d{2}$/;
const MONEY=/^(0|[1-9]\d{0,15})\.\d{4}$/;
const ACTIONS=Object.freeze({can_create_draft:false,can_review:false,can_approve:false,can_post:false});
const SOURCE_TYPES=Object.freeze(['INVOICE','PAYMENT','BANK_TRANSACTION','LOAN_TRANSACTION','CONSTRUCTION_COST','PROPERTY_MANAGEMENT','TAX_STATEMENT','FIXED_ASSET_EVENT','CLOSING_SETTLEMENT','MANUAL_JOURNAL','REVENUE','DEPOSIT','INTERCOMPANY','REIMBURSEMENT']);
const CLASSIFICATIONS=Object.freeze(['EXPENSE','CAPITALIZATION','PREPAID','ACCRUAL','PAYMENT','LOAN','REVENUE','DEPOSIT','INTERCOMPANY','REIMBURSEMENT','FIXED_ASSET','CONSTRUCTION_COST','PROPERTY_OPERATING_COST','TAX','CLOSING_COST','RECLASS','REVERSAL','BLOCKED']);
const COMMON_SOURCE_KEYS=Object.freeze(['accounting_date','accounting_period_end','accounting_period_id','accounting_period_start','admission_status','amount','business_date','cash_direction','company_code','completeness_status','cost_code_ref','currency','duplicate_status','entity_id','exception_codes','member_ref','project_ref','property_ref','schema_version','source_detail','source_document_id','source_document_line_id','source_line_hash','source_payload_hash','source_type','tenant_id','vendor_ref']);
const DETAIL_KEYS=Object.freeze({
  INVOICE:['execution_evidence','invoice_date','invoice_number','service_period_end','service_period_start'],
  PAYMENT:['bank_transaction_id','payee_ref','payment_date'],
  BANK_TRANSACTION:['bank_account_ref','bank_transaction_id','memo','transaction_date'],
  LOAN_TRANSACTION:['execution_evidence','lender_ref','loan_ref','transaction_kind'],
  CONSTRUCTION_COST:['contract_ref','purchase_order_ref','work_order_ref'],
  PROPERTY_MANAGEMENT:['report_kind','report_period_end','report_period_start'],
  TAX_STATEMENT:['coverage_period_end','coverage_period_start','jurisdiction','tax_kind'],
  FIXED_ASSET_EVENT:['asset_ref','event_kind','placed_in_service_date'],
  CLOSING_SETTLEMENT:['settlement_date','settlement_ref'],
  MANUAL_JOURNAL:['entry_date','journal_number','original_journal_entry_id'],
  REVENUE:['customer_ref','performance_period_end','performance_period_start','revenue_kind'],
  DEPOSIT:['deposit_kind','lease_ref','tenant_ref'],
  INTERCOMPANY:['counterparty_company_code','counterparty_entity_id','intercompany_kind'],
  REIMBURSEMENT:['beneficiary_ref','original_source_document_id','reimbursement_kind']
});
const POLICY_TYPES=Object.freeze(['ACCOUNT','CLASSIFICATION','DIMENSION','MATERIALITY','APPROVAL','PERIOD_CLOSE','REPORT','REVERSAL','TAX','INTERCOMPANY','LOAN_CAPITALIZATION']);
const LINE_KEYS=Object.freeze(['account_class','account_code','account_type','amount','cost_code_ref','currency','dimension_requirements','line_number','member_ref','project_ref','property_ref','side','source_document_id','source_document_line_id','source_line_hash']);
const DELTA_KEYS=Object.freeze(['account_class','account_code','accounting_period_id','amount','cash_flow_classification','cost_code_ref','currency','direction','member_ref','project_ref','property_ref','source_document_line_id','statement']);
const TRACE_KEYS=Object.freeze(['approval_status','company_code','entity_id','policy_type','rule_id','settings_snapshot_hash','settings_snapshot_id','snapshot_hash','snapshot_id','tenant_id','version','accounting_period_id']);
const ACCOUNT_POLICY_KEYS=Object.freeze(['account_class','account_code','account_type','cash_flow_classification','contra','effective_from','effective_to','normal_balance','optional_dimensions','report_statement','required_dimensions','settings_snapshot_hash','settings_snapshot_id']);
const WORKFLOW_ACTIONS=Object.freeze(['DRAFT','SUBMIT','REVIEW','APPROVE','POST']);
const WORKFLOW_PERMISSIONS=Object.freeze({DRAFT:'GL.JE.CREATE',SUBMIT:'GL.JE.SUBMIT',REVIEW:'GL.JE.REVIEW',APPROVE:'GL.JE.APPROVE',POST:'GL.JE.POST'});
const ALLOWED_CLASSIFICATIONS=Object.freeze({
  INVOICE:['EXPENSE','CAPITALIZATION','PREPAID','ACCRUAL','INTERCOMPANY','REIMBURSEMENT','CONSTRUCTION_COST','TAX'],
  PAYMENT:['PAYMENT','PREPAID','LOAN','DEPOSIT','INTERCOMPANY','REIMBURSEMENT'],
  BANK_TRANSACTION:['EXPENSE','PAYMENT','LOAN','REVENUE','DEPOSIT','INTERCOMPANY','REIMBURSEMENT'],
  LOAN_TRANSACTION:['LOAN','CAPITALIZATION','EXPENSE','PREPAID'],
  CONSTRUCTION_COST:['CONSTRUCTION_COST','CAPITALIZATION','EXPENSE','ACCRUAL'],
  PROPERTY_MANAGEMENT:['REVENUE','DEPOSIT','PROPERTY_OPERATING_COST','REIMBURSEMENT','ACCRUAL'],
  TAX_STATEMENT:['TAX','PREPAID','ACCRUAL'],
  FIXED_ASSET_EVENT:['FIXED_ASSET','CAPITALIZATION','EXPENSE','RECLASS','REVERSAL'],
  CLOSING_SETTLEMENT:['CLOSING_COST','CAPITALIZATION','LOAN','TAX'],
  MANUAL_JOURNAL:['RECLASS','REVERSAL','EXPENSE','CAPITALIZATION','ACCRUAL'],
  REVENUE:['REVENUE','ACCRUAL'],DEPOSIT:['DEPOSIT','REVENUE'],INTERCOMPANY:['INTERCOMPANY'],REIMBURSEMENT:['REIMBURSEMENT','EXPENSE']
});

const exact=(value,keys)=>value&&typeof value==='object'&&!Array.isArray(value)&&Object.keys(value).sort().join('|')===[...keys].sort().join('|');
const text=(value,max=256)=>typeof value==='string'&&value.trim().length>0&&value.trim().length<=max;
const nullableText=value=>value===null||text(value);
const validExecutionEvidence=value=>exact(value,['account_master','attachments'])&&Array.isArray(value.attachments)&&value.attachments.every((row,index,rows)=>exact(row,['attachment_id','content_hash','finalization_status','scan_status','storage_version'])&&UUID.test(row.attachment_id||'')&&SHA.test(row.content_hash||'')&&['PENDING','CLEAN','REJECTED','ERROR'].includes(row.scan_status)&&['PENDING','VERIFIED_CLEAN','REJECTED'].includes(row.finalization_status)&&text(row.storage_version,512)&&(index===0||rows[index-1].attachment_id<row.attachment_id))&&Array.isArray(value.account_master)&&value.account_master.length>0&&value.account_master.every((row,index,rows)=>exact(row,['account_code','active','required_member_type','requires_member'])&&text(row.account_code,64)&&typeof row.active==='boolean'&&typeof row.requires_member==='boolean'&&(row.requires_member?text(row.required_member_type,40):row.required_member_type===null)&&(index===0||rows[index-1].account_code<row.account_code));
const validLoanExecutionEvidence=value=>exact(value,['account_master','attachments','import_lineage'])&&Array.isArray(value.attachments)&&value.attachments.every((row,index,rows)=>exact(row,['attachment_id','content_hash','evidence_type','finalization_status','scan_status','storage_version'])&&UUID.test(row.attachment_id||'')&&SHA.test(row.content_hash||'')&&row.evidence_type==='source_attachment'&&['PENDING','CLEAN','REJECTED','ERROR'].includes(row.scan_status)&&['PENDING','VERIFIED_CLEAN','REJECTED'].includes(row.finalization_status)&&text(row.storage_version,512)&&(index===0||rows[index-1].attachment_id<row.attachment_id))&&Array.isArray(value.account_master)&&value.account_master.length>0&&value.account_master.every((row,index,rows)=>exact(row,['account_code','active','required_member_type','requires_member'])&&text(row.account_code,64)&&typeof row.active==='boolean'&&typeof row.requires_member==='boolean'&&(row.requires_member?text(row.required_member_type,40):row.required_member_type===null)&&(index===0||rows[index-1].account_code<row.account_code))&&exact(value.import_lineage,['raw_event_id','source_record_id','source_version'])&&UUID.test(value.import_lineage.raw_event_id||'')&&text(value.import_lineage.source_record_id)&&text(value.import_lineage.source_version);
const validDate=value=>{if(!DATE.test(value||''))return false;const [year,month,day]=value.split('-').map(Number),date=new Date(Date.UTC(year,month-1,day));return date.getUTCFullYear()===year&&date.getUTCMonth()===month-1&&date.getUTCDate()===day;};
const fail=(code,message)=>{throw Object.assign(new Error(message),{code});};
const freeze=value=>{if(value&&typeof value==='object'&&!Object.isFrozen(value)){for(const nested of Object.values(value))freeze(nested);Object.freeze(value);}return value;};
const units=value=>{const [whole,fraction]=value.split('.');return BigInt(whole)*10000n+BigInt(fraction);};
const sum=(rows,side)=>rows.filter(row=>row.side===side).reduce((total,row)=>total+units(row.amount),0n);

function validDetail(source){
  const keys=DETAIL_KEYS[source.source_type];
  if(!keys||!exact(source.source_detail,keys))return false;
  const detail=source.source_detail;
  if(source.source_type==='INVOICE')return text(detail.invoice_number)&&validDate(detail.invoice_date)&&(detail.service_period_start===null||validDate(detail.service_period_start))&&(detail.service_period_end===null||validDate(detail.service_period_end))&&validExecutionEvidence(detail.execution_evidence);
  if(source.source_type==='PAYMENT')return UUID.test(detail.bank_transaction_id||'')&&text(detail.payee_ref)&&validDate(detail.payment_date);
  if(source.source_type==='BANK_TRANSACTION')return text(detail.bank_account_ref)&&UUID.test(detail.bank_transaction_id||'')&&nullableText(detail.memo)&&validDate(detail.transaction_date);
  if(source.source_type==='LOAN_TRANSACTION')return text(detail.lender_ref)&&text(detail.loan_ref)&&['DRAW','INTEREST','FEE','ESCROW','REPAYMENT'].includes(detail.transaction_kind)&&validLoanExecutionEvidence(detail.execution_evidence);
  if(source.source_type==='CONSTRUCTION_COST')return nullableText(detail.contract_ref)&&nullableText(detail.purchase_order_ref)&&nullableText(detail.work_order_ref);
  if(source.source_type==='PROPERTY_MANAGEMENT')return text(detail.report_kind)&&validDate(detail.report_period_start)&&validDate(detail.report_period_end);
  if(source.source_type==='TAX_STATEMENT')return validDate(detail.coverage_period_start)&&validDate(detail.coverage_period_end)&&text(detail.jurisdiction)&&text(detail.tax_kind);
  if(source.source_type==='FIXED_ASSET_EVENT')return text(detail.asset_ref)&&['ACQUISITION','PLACED_IN_SERVICE','DEPRECIATION','DISPOSAL','IMPAIRMENT'].includes(detail.event_kind)&&(detail.placed_in_service_date===null||validDate(detail.placed_in_service_date));
  if(source.source_type==='CLOSING_SETTLEMENT')return validDate(detail.settlement_date)&&text(detail.settlement_ref);
  if(source.source_type==='MANUAL_JOURNAL')return validDate(detail.entry_date)&&text(detail.journal_number)&&UUID.test(detail.original_journal_entry_id||'');
  if(source.source_type==='REVENUE')return text(detail.customer_ref)&&text(detail.revenue_kind)&&(detail.performance_period_start===null||validDate(detail.performance_period_start))&&(detail.performance_period_end===null||validDate(detail.performance_period_end));
  if(source.source_type==='DEPOSIT')return text(detail.deposit_kind)&&nullableText(detail.lease_ref)&&nullableText(detail.tenant_ref);
  if(source.source_type==='INTERCOMPANY')return text(detail.counterparty_company_code,64)&&UUID.test(detail.counterparty_entity_id||'')&&text(detail.intercompany_kind);
  return text(detail.beneficiary_ref)&&UUID.test(detail.original_source_document_id||'')&&text(detail.reimbursement_kind);
}

function completeVariant(source){
  if(source.completeness_status!=='COMPLETE')return !['INVOICE','LOAN_TRANSACTION'].includes(source.source_type)||source.source_detail.execution_evidence.attachments.length===0||source.source_detail.execution_evidence.attachments.some(row=>row.scan_status!=='CLEAN'||row.finalization_status!=='VERIFIED_CLEAN'||row.storage_version.startsWith('pending:'));
  if(source.source_type==='INVOICE')return text(source.vendor_ref)&&source.source_detail.execution_evidence.attachments.length>0&&source.source_detail.execution_evidence.attachments.every(row=>row.scan_status==='CLEAN'&&row.finalization_status==='VERIFIED_CLEAN'&&!row.storage_version.startsWith('pending:'))&&source.source_detail.execution_evidence.account_master.every(row=>row.active===true);
  if(source.source_type==='LOAN_TRANSACTION')return source.source_detail.execution_evidence.attachments.length>0&&source.source_detail.execution_evidence.attachments.every(row=>row.scan_status==='CLEAN'&&row.finalization_status==='VERIFIED_CLEAN'&&!row.storage_version.startsWith('pending:'))&&source.source_detail.execution_evidence.account_master.every(row=>row.active===true);
  if(source.source_type==='CONSTRUCTION_COST')return text(source.vendor_ref)&&text(source.project_ref)&&text(source.property_ref)&&text(source.cost_code_ref);
  if(source.source_type==='PROPERTY_MANAGEMENT')return text(source.property_ref);
  if(source.source_type==='CLOSING_SETTLEMENT')return text(source.property_ref);
  return true;
}

export function assertAiAccountingSourceV1(source,{tenantId,entityId,accountingPeriodId}={}){
  if(!exact(source,COMMON_SOURCE_KEYS)||source.schema_version!=='AI_ACCOUNTING_SOURCE_V1'||source.tenant_id!==tenantId||source.entity_id!==entityId||source.accounting_period_id!==accountingPeriodId||!SOURCE_TYPES.includes(source.source_type)||!UUID.test(source.tenant_id||'')||!UUID.test(source.entity_id||'')||!UUID.test(source.accounting_period_id||'')||!UUID.test(source.source_document_id||'')||!UUID.test(source.source_document_line_id||'')||!SHA.test(source.source_payload_hash||'')||!SHA.test(source.source_line_hash||'')||!text(source.company_code,64)||!validDate(source.business_date)||!validDate(source.accounting_date)||!validDate(source.accounting_period_start)||!validDate(source.accounting_period_end)||source.accounting_period_start>source.accounting_period_end||source.accounting_date<source.accounting_period_start||source.accounting_date>source.accounting_period_end||source.business_date>source.accounting_period_end||!['INFLOW','OUTFLOW','NON_CASH'].includes(source.cash_direction)||!/^[A-Z]{3}$/.test(source.currency||'')||!MONEY.test(source.amount||'')||source.amount==='0.0000'||!['COMPLETE','INCOMPLETE'].includes(source.completeness_status)||!['NONE','POSSIBLE','CONFIRMED'].includes(source.duplicate_status)||!['ADMITTED','NOT_ADMITTED','QUARANTINED','REJECTED'].includes(source.admission_status)||!Array.isArray(source.exception_codes)||source.exception_codes.some(code=>!text(code,80))||![source.project_ref,source.property_ref,source.vendor_ref,source.member_ref,source.cost_code_ref].every(nullableText)||!validDetail(source)||!completeVariant(source))fail('AI_ACCOUNTING_SOURCE_INVALID','A closed, period-bound retained accounting event is required.');
  return source;
}

function validTrace(trace,source){return exact(trace,TRACE_KEYS)&&POLICY_TYPES.includes(trace.policy_type)&&trace.approval_status==='APPROVED'&&trace.tenant_id===source.tenant_id&&trace.entity_id===source.entity_id&&trace.company_code===source.company_code&&trace.accounting_period_id===source.accounting_period_id&&UUID.test(trace.settings_snapshot_id||'')&&SHA.test(trace.settings_snapshot_hash||'')&&UUID.test(trace.snapshot_id||'')&&Number.isSafeInteger(trace.version)&&trace.version>0&&SHA.test(trace.snapshot_hash||'')&&text(trace.rule_id,128);}
function validWorkflowPolicy(policy,source,approvalTrace){const stages=policy?.stages;return exact(policy,['approval_status','company_code','entity_id','period_id','policy_hash','policy_id','settings_snapshot_hash','settings_snapshot_id','stages','tenant_id'])&&policy.approval_status==='APPROVED'&&policy.tenant_id===source.tenant_id&&policy.entity_id===source.entity_id&&policy.company_code===source.company_code&&policy.period_id===source.accounting_period_id&&policy.settings_snapshot_id===approvalTrace?.settings_snapshot_id&&policy.settings_snapshot_hash===approvalTrace?.settings_snapshot_hash&&policy.policy_id===approvalTrace?.snapshot_id&&policy.policy_hash===approvalTrace?.snapshot_hash&&exact(stages,WORKFLOW_ACTIONS)&&WORKFLOW_ACTIONS.every(action=>exact(stages[action],['permission','role'])&&stages[action].permission===WORKFLOW_PERMISSIONS[action]&&text(stages[action].role,80));}
function validRisk(risk){return exact(risk,['approval_threshold','confidence_floor','materiality_threshold','policy_rule_id','risk_level','threshold_comparison'])&&['LOW','MEDIUM','HIGH'].includes(risk.risk_level)&&text(risk.policy_rule_id,128)&&MONEY.test(risk.materiality_threshold||'')&&MONEY.test(risk.approval_threshold||'')&&typeof risk.confidence_floor==='number'&&risk.confidence_floor>=0&&risk.confidence_floor<=1&&['BELOW_MATERIALITY','AT_OR_ABOVE_MATERIALITY','AT_OR_ABOVE_APPROVAL','POLICY_BLOCKED'].includes(risk.threshold_comparison);}
function riskFromPolicy(source,confidence,risk){const amount=units(source.amount),materiality=units(risk.materiality_threshold),approval=units(risk.approval_threshold);if(approval<materiality)fail('AI_ACCOUNTING_DECISION_POLICY_INVALID','Approval threshold cannot be below materiality.');if(confidence<risk.confidence_floor)return {risk_level:'HIGH',threshold_comparison:'POLICY_BLOCKED'};if(amount>=approval)return {risk_level:'HIGH',threshold_comparison:'AT_OR_ABOVE_APPROVAL'};if(amount>=materiality)return {risk_level:'MEDIUM',threshold_comparison:'AT_OR_ABOVE_MATERIALITY'};return {risk_level:'LOW',threshold_comparison:'BELOW_MATERIALITY'};}
function validAccountPolicy(policy,source,settingsId,settingsHash){const domain=['PROJECT','PROPERTY','MEMBER','COST_CODE'];return exact(policy,ACCOUNT_POLICY_KEYS)&&text(policy.account_code,64)&&['ASSET','LIABILITY','EQUITY','REVENUE','EXPENSE'].includes(policy.account_class)&&text(policy.account_type,80)&&['DEBIT','CREDIT'].includes(policy.normal_balance)&&typeof policy.contra==='boolean'&&['BALANCE_SHEET','INCOME_STATEMENT'].includes(policy.report_statement)&&['NONE','OPERATING','INVESTING','FINANCING'].includes(policy.cash_flow_classification)&&Array.isArray(policy.required_dimensions)&&Array.isArray(policy.optional_dimensions)&&[...policy.required_dimensions,...policy.optional_dimensions].every(item=>domain.includes(item))&&new Set([...policy.required_dimensions,...policy.optional_dimensions]).size===policy.required_dimensions.length+policy.optional_dimensions.length&&validDate(policy.effective_from)&&(policy.effective_to===null||validDate(policy.effective_to))&&policy.effective_from<=source.accounting_date&&(policy.effective_to===null||source.accounting_date<=policy.effective_to)&&policy.settings_snapshot_id===settingsId&&policy.settings_snapshot_hash===settingsHash;}
function validLine(line,source,index,accountPolicies){const policy=accountPolicies.find(item=>item.account_code===line.account_code),dimensions={PROJECT:line.project_ref,PROPERTY:line.property_ref,MEMBER:line.member_ref,COST_CODE:line.cost_code_ref},selected=Object.entries(dimensions).filter(([,value])=>value!==null).map(([key])=>key);return exact(line,LINE_KEYS)&&line.line_number===index+1&&['DEBIT','CREDIT'].includes(line.side)&&text(line.account_code,64)&&['ASSET','LIABILITY','EQUITY','REVENUE','EXPENSE'].includes(line.account_class)&&text(line.account_type,80)&&MONEY.test(line.amount||'')&&line.amount!=='0.0000'&&line.currency===source.currency&&Array.isArray(line.dimension_requirements)&&[line.project_ref,line.property_ref,line.member_ref,line.cost_code_ref].every(nullableText)&&line.source_document_id===source.source_document_id&&line.source_document_line_id===source.source_document_line_id&&line.source_line_hash===source.source_line_hash&&policy?.account_class===line.account_class&&policy.account_type===line.account_type&&line.dimension_requirements.length===policy.required_dimensions.length&&line.dimension_requirements.every(item=>policy.required_dimensions.includes(item)&&text(dimensions[item]))&&selected.every(item=>policy.required_dimensions.includes(item)||policy.optional_dimensions.includes(item));}
function validDelta(delta,source){return exact(delta,DELTA_KEYS)&&['BALANCE_SHEET','INCOME_STATEMENT','CASH_FLOW'].includes(delta.statement)&&['INCREASE','DECREASE'].includes(delta.direction)&&['NONE','OPERATING','INVESTING','FINANCING'].includes(delta.cash_flow_classification)&&text(delta.account_code,64)&&['ASSET','LIABILITY','EQUITY','REVENUE','EXPENSE'].includes(delta.account_class)&&delta.accounting_period_id===source.accounting_period_id&&MONEY.test(delta.amount||'')&&delta.amount!=='0.0000'&&delta.currency===source.currency&&[delta.project_ref,delta.property_ref,delta.member_ref,delta.cost_code_ref].every(nullableText)&&delta.source_document_line_id===source.source_document_line_id;}
function deltaBoundToJournal(delta,lines){return lines.some(line=>line.account_code===delta.account_code&&line.account_class===delta.account_class&&line.currency===delta.currency&&line.project_ref===delta.project_ref&&line.property_ref===delta.property_ref&&line.member_ref===delta.member_ref&&line.cost_code_ref===delta.cost_code_ref&&line.amount===delta.amount);}
function derivedDirection(line,policy){return line.side===policy.normal_balance?'INCREASE':'DECREASE';}
const money=amount=>`${amount/10000n}.${String(amount%10000n).padStart(4,'0')}`;
const stable=value=>JSON.stringify(value,Object.keys(value).sort());
export function deriveAiAccountingReportDeltas({source,lines,approvedAccountPolicies}){const groups=new Map(),add=(base,signed)=>{const key=stable(base),current=groups.get(key)??{base,total:0n};current.total+=signed;groups.set(key,current);};for(const line of lines){const policy=approvedAccountPolicies.find(item=>item.account_code===line.account_code),signed=units(line.amount)*(derivedDirection(line,policy)==='INCREASE'?1n:-1n),base={statement:policy.report_statement,cash_flow_classification:'NONE',accounting_period_id:source.accounting_period_id,account_code:line.account_code,account_class:line.account_class,currency:line.currency,project_ref:line.project_ref,property_ref:line.property_ref,member_ref:line.member_ref,cost_code_ref:line.cost_code_ref,source_document_line_id:line.source_document_line_id};add(base,signed);if(source.cash_direction!=='NON_CASH'&&policy.cash_flow_classification!=='NONE'){const cashDirection=derivedDirection(line,policy);if(cashDirection!==(source.cash_direction==='INFLOW'?'INCREASE':'DECREASE'))fail('AI_ACCOUNTING_CASH_DIRECTION_INVALID','Retained cash direction conflicts with the approved cash account effect.');add({...base,statement:'CASH_FLOW',cash_flow_classification:policy.cash_flow_classification},signed);}}return [...groups.values()].filter(group=>group.total!==0n).map(group=>({...group.base,direction:group.total>0n?'INCREASE':'DECREASE',amount:money(group.total<0n?-group.total:group.total)}));}
function completeReportCoverage(lines,deltas,policies,source){const derived=deriveAiAccountingReportDeltas({source,lines,approvedAccountPolicies:policies}),canonical=rows=>rows.map(row=>JSON.stringify(row,Object.keys(row).sort())).sort();return JSON.stringify(canonical(derived))===JSON.stringify(canonical(deltas));}

export function buildAiAccountingDecisionPacketFullV1({tenantId,entityId,accountingPeriodId,source,classification,ruleId,reason,confidence,risk,requiredHumanFields=[],policyTraces,workflowPolicy,approvedAccountPolicies,proposedJournal,expectedReportDeltas,reversalPolicy='NONE',amortizationScheduleTrace=null}){
  assertAiAccountingSourceV1(source,{tenantId,entityId,accountingPeriodId});
  const policyTypes=Array.isArray(policyTraces)?policyTraces.map(trace=>trace?.policy_type):[];
  const requiredPolicyTypes=['ACCOUNT','CLASSIFICATION','MATERIALITY','APPROVAL','PERIOD_CLOSE','REPORT'];
  const classificationPolicyTypes={PREPAID:['REVERSAL'],ACCRUAL:['REVERSAL'],INTERCOMPANY:['INTERCOMPANY'],TAX:['TAX'],CAPITALIZATION:['DIMENSION'],CONSTRUCTION_COST:['DIMENSION'],FIXED_ASSET:['DIMENSION']}[classification]??[];
  const sourcePolicyTypes=classification==='BLOCKED'?[]:{TAX_STATEMENT:['TAX'],LOAN_TRANSACTION:['LOAN_CAPITALIZATION']}[source.source_type]??[];
  const settingsIds=Array.isArray(policyTraces)?new Set(policyTraces.map(trace=>trace?.settings_snapshot_id)):new Set(),settingsHashes=Array.isArray(policyTraces)?new Set(policyTraces.map(trace=>trace?.settings_snapshot_hash)):new Set();
  const computedRisk=validRisk(risk)?riskFromPolicy(source,confidence,risk):null;
  const approvalTrace=Array.isArray(policyTraces)?policyTraces.find(trace=>trace?.policy_type==='APPROVAL'):null;
  if(!CLASSIFICATIONS.includes(classification)||classification!=='BLOCKED'&&!ALLOWED_CLASSIFICATIONS[source.source_type]?.includes(classification)||!text(ruleId,128)||!text(reason,4000)||typeof confidence!=='number'||confidence<0||confidence>1||!computedRisk||risk.risk_level!==computedRisk.risk_level||risk.threshold_comparison!==computedRisk.threshold_comparison||!Array.isArray(requiredHumanFields)||requiredHumanFields.some(field=>!text(field,80))||!Array.isArray(policyTraces)||policyTraces.some(trace=>!validTrace(trace,source))||new Set(policyTypes).size!==policyTypes.length||settingsIds.size!==1||settingsHashes.size!==1||[...requiredPolicyTypes,...classificationPolicyTypes,...sourcePolicyTypes].some(type=>!policyTypes.includes(type))||!validWorkflowPolicy(workflowPolicy,source,approvalTrace))fail('AI_ACCOUNTING_DECISION_POLICY_INVALID','Classification, source semantics, risk, and workflow require exact approved, scoped policy evidence.');
  const settingsId=[...settingsIds][0],settingsHash=[...settingsHashes][0];
  if(!Array.isArray(approvedAccountPolicies)||approvedAccountPolicies.length<2||approvedAccountPolicies.some(policy=>!validAccountPolicy(policy,source,settingsId,settingsHash))||new Set(approvedAccountPolicies.map(policy=>policy.account_code)).size!==approvedAccountPolicies.length)fail('AI_ACCOUNTING_ACCOUNT_POLICY_INVALID','Suggested accounts require exact effective approved account policy membership.');
  const exception=classification==='BLOCKED'||source.completeness_status!=='COMPLETE'||source.duplicate_status!=='NONE'||source.admission_status!=='ADMITTED'||source.exception_codes.length>0;
  const journalKeys=['balanced','lines','reversal_policy','status'];
  if(!exact(proposedJournal,journalKeys)||proposedJournal.status!=='SUGGESTED_ONLY'||!['NONE','NEXT_OPEN_PERIOD','AMORTIZATION_SCHEDULE','HUMAN_DECISION_REQUIRED'].includes(reversalPolicy)||proposedJournal.reversal_policy!==reversalPolicy||!Array.isArray(proposedJournal.lines))fail('AI_ACCOUNTING_SUGGESTED_JOURNAL_INVALID','Suggested Journal must be closed and grant no accounting authority.');
  const lines=proposedJournal.lines;
  if(!Array.isArray(expectedReportDeltas))fail('AI_ACCOUNTING_SUGGESTED_JOURNAL_INVALID','Expected report deltas must be an array.');
  if(exception){if(lines.length!==0||proposedJournal.balanced!==false||expectedReportDeltas.length!==0)fail('AI_ACCOUNTING_EXCEPTION_EFFECT_INVALID','Exceptions must contain zero Journal or report effects.');}
  else if(lines.length<2||lines.length>500||lines.some((line,index)=>!validLine(line,source,index,approvedAccountPolicies))||sum(lines,'DEBIT')!==sum(lines,'CREDIT')||proposedJournal.balanced!==true||expectedReportDeltas.some(delta=>!validDelta(delta,source))||!completeReportCoverage(lines,expectedReportDeltas,approvedAccountPolicies,source))fail('AI_ACCOUNTING_SUGGESTED_JOURNAL_INVALID','A 2..N line balanced, account-policy-bound Journal and exact aggregated BS/IS/CF effects are required.');
  if(reversalPolicy==='AMORTIZATION_SCHEDULE'&&(!exact(amortizationScheduleTrace,['schedule_hash','schedule_id'])||!UUID.test(amortizationScheduleTrace.schedule_id||'')||!SHA.test(amortizationScheduleTrace.schedule_hash||'')))fail('AI_ACCOUNTING_AMORTIZATION_TRACE_INVALID','Amortization requires an exact retained schedule trace.');
  if(reversalPolicy!=='AMORTIZATION_SCHEDULE'&&amortizationScheduleTrace!==null)fail('AI_ACCOUNTING_AMORTIZATION_TRACE_INVALID','Unexpected amortization schedule trace.');
  return freeze({schema_version:'AI_ACCOUNTING_DECISION_PACKET_V1',status:exception?'EXCEPTION':'READY_FOR_HUMAN_REVIEW',tenant_id:tenantId,entity_id:entityId,company_code:source.company_code,accounting_period_id:accountingPeriodId,accounting_date:source.accounting_date,settings_snapshot_id:settingsId,settings_snapshot_hash:settingsHash,source,classification,rule_id:ruleId,reason,confidence,risk,required_human_fields:[...requiredHumanFields],policy_traces:policyTraces.map(trace=>({...trace})),workflow_policy:{...workflowPolicy,stages:Object.fromEntries(WORKFLOW_ACTIONS.map(action=>[action,{...workflowPolicy.stages[action]}]))},approved_account_policies:approvedAccountPolicies.map(policy=>({...policy,required_dimensions:[...policy.required_dimensions],optional_dimensions:[...policy.optional_dimensions]})),proposed_journal:{status:'SUGGESTED_ONLY',balanced:proposedJournal.balanced,reversal_policy:reversalPolicy,lines:lines.map(line=>({...line}))},expected_report_deltas:expectedReportDeltas.map(delta=>({...delta})),amortization_schedule_trace:amortizationScheduleTrace===null?null:{...amortizationScheduleTrace},trace:{source_to_decision:true,settings_to_decision:true,decision_to_draft:false,decision_to_posted_ledger:false,decision_to_report:false},action_flags:ACTIONS});
}

export const AI_ACCOUNTING_SOURCE_TYPES=SOURCE_TYPES;
export const AI_ACCOUNTING_CLASSIFICATIONS=CLASSIFICATIONS;

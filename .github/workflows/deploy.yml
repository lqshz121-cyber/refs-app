name: Deploy to GitHub Pages
on:
  workflow_run:
    workflows: [Accounting Kernel Gate]
    types: [completed]
  workflow_dispatch:
permissions:
  contents: read
  pages: write
  id-token: write
concurrency:
  group: pages
  cancel-in-progress: true
jobs:
  build:
    if: >-
      ${{
        github.event_name == 'workflow_dispatch' ||
        (github.event.workflow_run.conclusion == 'success' && github.event.workflow_run.head_branch == 'main')
      }}
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ github.event.workflow_run.head_sha || github.sha }}
      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm
          cache-dependency-path: |
            package-lock.json
            server/package-lock.json
      - name: Install frontend dependencies exactly
        run: npm ci
      - name: Install kernel dependencies exactly
        working-directory: server
        run: npm ci
      - name: Run frontend gate
        run: npm test
      - name: Run kernel static and unit gate
        working-directory: server
        run: npm test
      - name: Build
        env:
          # GitHub Pages is the public, non-provider accounting demonstration.
          # Authoritative deployments use Render and must supply every API/OIDC value.
          REFS_PUBLIC_RUNTIME_MODE: LOCAL_MOCK
        run: npm run build
      - name: Verify runtime deployment assets
        env:
          # Verification must evaluate the same explicit public mode used by the
          # immediately preceding build step. Step-level environments do not
          # otherwise carry across GitHub Actions steps.
          REFS_PUBLIC_RUNTIME_MODE: LOCAL_MOCK
        run: npm run verify:runtime-deployment-assets
      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: dist
  deploy:
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4

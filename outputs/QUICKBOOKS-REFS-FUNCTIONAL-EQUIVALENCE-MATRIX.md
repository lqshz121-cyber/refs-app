# QuickBooks Online → REFS Functional Equivalence Matrix

This branch matrix records only directly observed QBO evidence and verified authoritative REFS behavior. It does not replace historical matrices and does not mark unobserved behavior as equivalent.

## Evidence log — 2026-08-19

| Surface | Newly observed QBO evidence | REFS change | Verification | Status |
|---|---|---|---|---|
| Expenses navigation | Opening **Expenses** in the QBO main navigation expands `Expense transactions`, `Vendors`, `Bills`, `Bill payments`, `Contractors`, and `1099s`. Selecting `Expense transactions` navigates to the Expenses page. | Existing REFS primary navigation continues to open its first applicable child per the user's product rule. No QBO-only contractor, 1099, payment-provider, or external workflow was added. | Read-only QBO DOM inspection; no create, edit, payment, export, settings, or data action. | PARTIAL — structure observed; REFS intentionally follows the user's first-child rule. |
| Expenses page hierarchy | At a 525×599 viewport, QBO shows a 24px/500 `Expenses` heading, compact 36px transaction control, Filter, date scope, and one empty state. Body typography is 14px Avenir/Helvetica with `#f4f5f8` background. | AP heading is now compact rather than a decorative hero. The duplicate four-card KPI summary and repeated empty adjustment panel are removed from Expenses only. Filters, Bills, Vendor credits, AP Aging, drill/Back, pagination, READ ONLY state, and folded WBS evidence remain available. | `npm run test:authoritative-documents` exit 0; `npm run test:authoritative-full-shell` exit 0; `npm run test:authoritative-visual-parity` exit 0; `npm run build` exit 0; `npm test` exit 0 (visual verifier 64/64). | CANDIDATE — observed shell and local contract only. |
| Expenses empty state | QBO text: `No expenses found` and `Try to change some filters to see more results.` Local copy audit found the REFS follow-up still exposed the implementation phrase `scoped API result`. | REFS renders one `No expenses found` state with concise guidance: `Try changing the filters. Empty results do not confirm a zero balance.` The accounting caveat remains, while transport language is removed. Filters, API reads, list state and full-page Back are unchanged. | `npm run test:authoritative-documents` exit 0; `npm run test:payables-responsive` exit 0; `npm run test:authoritative-full-shell` exit 0; `npm run test:authoritative-visual-parity` exit 0; `npm run build` exit 0; `npm test` exit 0 (visual verifier 64/64); `git diff --check` exit 0. | CANDIDATE — only the observed empty-state hierarchy is matched; no populated QBO expense rows or authenticated runtime were verified. |
| Expenses first-screen copy | The QBO session remains expired at the Intuit logout route, so no new product wording is claimed. Local authoritative review found the Expenses subtitle and default scope exposed implementation terms (`accounting API`, `retained API rows`) even though the page already carries an explicit READ ONLY badge. | The subtitle is now `Review bills, vendor credits, and AP aging.` and the default scope is `All records`. API-backed authority, READ ONLY state, tabs, filters, pagination, full-page evidence and exact Back context are unchanged. | `npm run test:authoritative-documents` exit 0; `npm run test:payables-responsive` exit 0; `npm run test:authoritative-full-shell` exit 0; `npm run test:authoritative-visual-parity` exit 0; `npm run build` exit 0; `npm test` exit 0 (visual verifier 64/64); `git diff --check` exit 0. | ADDITIVE CANDIDATE — local UX-copy correction only; authenticated Expenses runtime and current QBO equivalence remain unverified. |
| Expenses filter terminology | The QBO session remains expired, so no new external filter wording is claimed. Previously observed QBO Expenses hierarchy and the user's product direction favor short business labels; local review found REFS exposed storage-oriented terms such as `Category (offset account)`, `All retained vendors`, and `All retained offset accounts`. | Expenses now shows `Vendor`, `All vendors`, `Category`, and `All categories`; the active scope reports `Category: <code>`. The exact vendor and account-code values, filters, API requests, result count, pagination, drills and Back context are unchanged. AR wording and accounting evidence fields are not altered. | `npm run test:authoritative-documents` exit 0; `npm run test:payables-responsive` exit 0; `npm run test:authoritative-full-shell` exit 0; `npm run test:authoritative-visual-parity` exit 0; `npm run build` exit 0; `npm test` exit 0 (visual verifier 64/64); `git diff --check` exit 0. | ADDITIVE CANDIDATE — local filter-copy correction only; authenticated Expenses runtime and current QBO equivalence remain unverified. |
| Expenses filter density | Read-only browser discovery confirmed that the available QBO tab had expired to the `TIMER_EXPIRED` logout route, so no new QBO behavior is claimed. Previously observed QBO Expenses used a compact transaction/filter hierarchy; local responsive audit found REFS displayed Search, Status, two dates, Vendor, Category, Reset and result count simultaneously, producing three to four toolbar rows at sidebar-reduced widths. | Expenses now keeps Search, Status, Reset, result count and a native `More filters` disclosure in the primary toolbar. From, Through, Vendor and Category remain fully available inside the disclosure; its label shows the active secondary-filter count. The wide view uses one compact row, tablet uses two columns and phone uses one column. Filter values, API reads, pagination, full-page drill and exact Back context are unchanged; Receivables retains its existing layout. | `npm run test:authoritative-documents` exit 0; `npm run test:payables-responsive` exit 0; `npm run test:authoritative-browser-acceptance-preflight` exit 0; `npm run test:authoritative-full-shell` exit 0; `npm run test:authoritative-visual-parity` exit 0; `npm run build` exit 0; `npm test` exit 0 (visual verifier 64/64); `git diff --check` exit 0. | ADDITIVE CANDIDATE — local Expenses density/responsive correction only; authenticated populated Expenses runtime and current QBO equivalence remain unverified. |
| Bill and adjustment blocked-lineage copy | QBO remained at the expired logout route, so no new detail-state wording is claimed. Local rendered audit found the default missing-lineage warning used two long implementation-oriented sentences before the user reached the retained Bill or adjustment facts. | The unchanged `BLOCKED — authoritative lineage unavailable` state now says that source, receipt, mapping, audit, journal and ledger links were not returned for this revision, followed by `List facts remain read only.` Complete lineage still renders the same immutable identifiers; incomplete or mismatched lineage remains blocked and cannot become source or journal authority. | `npm run test:authoritative-documents` exit 0; `npm run test:authoritative-browser-acceptance-preflight` exit 0; `npm run test:authoritative-full-shell` exit 0; `npm run test:authoritative-visual-parity` exit 0; `npm run build` exit 0; `npm test` exit 0 (visual verifier 64/64); `git diff --check` exit 0. | ADDITIVE CANDIDATE — local AP/AR readability correction only; authenticated detail runtime and current QBO equivalence remain unverified. |
| Expenses mixed empty-state hierarchy | The QBO session remains expired, so no new external behavior is claimed. Local rendered audit found a valid Bill table followed by an unrelated `No adjustments match…` card when adjustment rows were filtered out; a fully filtered mixed view could render separate Bill and adjustment empty cards. | The mixed Expenses view now omits an empty adjustment section when Bills are visible and renders one `No expenses match these filters` state when the combined result is empty. Vendor credits keeps its dedicated empty state, while API facts, filtering, counts, pagination, evidence drill and exact Back context are unchanged. | `npm run test:authoritative-documents` exit 0; `npm run test:payables-responsive` exit 0; `npm run test:authoritative-full-shell` exit 0; `npm run test:authoritative-visual-parity` exit 0; `npm run build` exit 0; `npm test` exit 0 (visual verifier 64/64); `git diff --check` exit 0. | ADDITIVE CANDIDATE — local hierarchy/page-length correction only; populated authenticated Expenses runtime and current QBO equivalence remain unverified. |
| Expenses table action language | Browser control was unavailable and the QBO session remained unauthenticated, so no new QBO table wording is claimed. Local rendered audit found the Expenses Bill and Vendor-credit tables exposed `Authoritative API rows only`, an `Evidence` column, and `Open evidence` before the user reached the actual audit detail page. | Expenses now labels the list `Bills`, shows a concise Bill/adjustment count, and uses `Details` / `View details` for AP Bill and adjustment rows. Row IDs, values, status, filtering, pagination, immutable context, full-page detail, lineage block and Back behavior are unchanged; shared AR tables retain their prior wording. | `npm run test:authoritative-documents` exit 0; `npm run test:payables-responsive` exit 0; `npm run test:authoritative-full-shell` exit 0; `npm run test:authoritative-visual-parity` exit 0; `npm run build` exit 0; `npm test` exit 0 (visual verifier 64/64); `git diff --check` exit 0. | ADDITIVE CANDIDATE — local Expenses terminology alignment only; authenticated populated lists and current QBO equivalence remain unverified. |
| Expenses visible result count | No fresh QBO interaction was recorded because browser control was unavailable; the current tab location alone is not treated as product evidence. Local authoritative review found the Expenses count exposed internal object families such as `0 bills | 1 adjustments` even when the active Vendor credits view rendered only one credit list. | The filter bar now reports one concise count derived from the rows actually rendered: `1 result` or `N results`. Bills, Vendor credits, mixed retained results and AR use the same presentation rule without changing API reads, filters, pagination, full-page evidence or Back context. | `npm run test:authoritative-documents` exit 0; `npm run test:authoritative-full-shell` exit 0; `npm run test:authoritative-visual-parity` exit 0; `npm run build` exit 0; `npm test` exit 0 (visual verifier 64/64); `git diff --check` exit 0. | ADDITIVE CANDIDATE — local result-summary correction; populated QBO rows and authenticated Render runtime remain unverified. |
| Expenses tab/state alignment | No new QBO interaction evidence was available. Local navigation audit found transaction type `ALL` rendered both authoritative Bills and adjustment results while visually selecting Bills, so the selected tab did not describe the page beneath it. | The existing mixed result capability is retained as an explicit `All transactions` tab. Bills and Vendor credits remain separate sibling views, while the transaction selector, Reset, filters, AP Aging, pagination, full-page evidence and exact Back context are unchanged. | `npm run test:authoritative-documents` exit 0; `npm run test:authoritative-full-shell` exit 0; `npm run test:payables-responsive` exit 0; `npm run test:authoritative-list-context` exit 0; `npm run test:authoritative-visual-parity` exit 0; `npm run build` exit 0; `npm test` exit 0 (visual verifier 64/64); `git diff --check` exit 0. | ADDITIVE CANDIDATE — local navigation-state correction; populated QBO behavior and authenticated Render runtime remain unverified. |
| Vendor credits empty state | No new QBO page evidence was available. Local branch audit found that an empty authoritative AP scope rendered no list and no state message after selecting Vendor credits, because the shared source-empty guard suppressed both the Bill and adjustment branches. | Vendor credits now renders one dedicated `No vendor credits found` state with a concise fail-closed AP-balance note. All transactions still renders its single existing Expenses empty state, Bills remains unchanged, and no credit, balance, journal or source fact is inferred. | `npm run test:authoritative-documents` exit 0; `npm run test:authoritative-full-shell` exit 0; `npm run test:payables-responsive` exit 0; `npm run test:authoritative-visual-parity` exit 0; `npm run build` exit 0; `npm test` exit 0 (visual verifier 64/64); `git diff --check` exit 0. | ADDITIVE CANDIDATE — local empty-state correction; populated QBO behavior and authenticated Render runtime remain unverified. |
| Vendor credits pagination | The signed-in QBO Reports tab was visible to browser discovery, but DOM inspection was blocked by browser URL policy and was not bypassed; no new QBO Expenses behavior is claimed. Local authoritative Expenses audit found the standalone Vendor credits view rendered every retained credit adjustment in one unbounded page despite the parent immutable list contract already defining a 25-row page. | Vendor credits now reuses the exact normalized list page and page size, renders Previous / Page / Next controls only when needed, and carries that same page through its existing full-page evidence and Back context. `All transactions`, Bills, filters, API facts, lineage and all no-write boundaries are unchanged. | `npm run test:authoritative-documents` exit 0; `npm run test:authoritative-full-shell` exit 0; `npm run test:authoritative-visual-parity` exit 0; `npm run build` exit 0; `npm test` exit 0 (visual verifier 64/64); `git diff --check` exit 0. | ADDITIVE CANDIDATE — local long-page correction; populated QBO Vendor credits behavior and authenticated Render runtime remain unverified. |
| Reports Favorites | QBO Standard reports shows compact Favorites rows, including AR aging, Balance Sheet, and Profit and Loss; rows are about 41px high. | Prior branch commits compacted Favorites, moved AR Aging into the same list, folded Statement Snapshot, and capped directory preview at 12 rows while retaining full-report access. | `npm test` exit 0; visual verifier 64/64; `npm run build` exit 0 at `de617d7`. | PARTIAL — authenticated Render runtime not verified. |
| Reports common shortcuts | Browser control remained unavailable and QBO authentication was not confirmed, so no new QBO shortcut wording is claimed. Local rendered audit found a fixed, non-customizable shortcut list labelled `FAVORITES`, internal descriptions such as `retained account balances` and `inferred classifications`, and two `GET ONLY` badges inconsistent with the shared read-only vocabulary. | The fixed section is now `COMMON REPORTS`; Trial Balance, Income Statement and cash descriptions use concise business language; visible `GET ONLY` badges are standardized to `READ ONLY`. The same five shortcuts, search isolation, direct full-page launch, statement snapshot, API readers and exact Back context remain unchanged. | `npm run test:authoritative-reports` exit 0; `npm run test:authoritative-full-shell` exit 0; `npm run test:authoritative-visual-parity` exit 0; `npm run build` exit 0; `npm test` exit 0 (visual verifier 64/64); `git diff --check` exit 0. | ADDITIVE CANDIDATE — local Reports naming/readability correction only; authenticated Reports runtime and current QBO equivalence remain unverified. |
| Reports catalog density | The same direct Standard reports audit showed a compact, list-led hierarchy at 525×599 with a 20px Favorites heading and roughly 41px report rows. QBO body copy used 14px Avenir/Helvetica on `#f4f5f8`. The current browser navigation block was not bypassed, so no new QBO interaction is claimed. | Converts the large report finder card to a compact search row, converts four explanatory category cards into 42px tabs, removes repeated `API / READ` chrome and the duplicate workbench implementation paragraph, and shortens `Refresh statement evidence` to `Refresh`. Search, every report category, Favorites, the folded property/project directory, Snapshot, full-page drill and exact Back context remain. | `npm run test:authoritative-reports` exit 0; `npm run test:authoritative-full-shell` exit 0; `npm run test:authoritative-visual-parity` exit 0; `npm run build` exit 0; `npm test` exit 0 (visual verifier 64/64). | CANDIDATE — previously observed QBO layout evidence reused; authenticated Render runtime not verified. |
| Reports Center first-screen copy | QBO authentication remains expired, so no new external wording is claimed. Local rendered audit found the Reports Center title area and search-result accessibility label exposed `from the accounting API`, `Authoritative reports catalog`, and `Matching API-backed statements`. | Reports Center now uses `Review posted financial reports`, `Reports catalog`, and `Matching statements`. Search, result counts, Favorites, categories, direct statement launch, property/project directory, report readers and exact Back context are unchanged. | `npm run test:authoritative-reports` exit 0; `npm run test:authoritative-full-shell` exit 0; `npm run test:authoritative-visual-parity` exit 0; `npm run build` exit 0; `npm test` exit 0 (visual verifier 64/64); `git diff --check` exit 0. | ADDITIVE CANDIDATE — local Reports UX-copy correction only; authenticated Reports runtime and current QBO equivalence remain unverified. |
| Full-report first-screen copy | Browser control was unavailable and the QBO session remained expired, so no new external wording is claimed. Local rendered audit found the full-report title area exposed `POSTED EVIDENCE | API GET`, `from the accounting API`, `Refresh statement evidence`, and `GET ONLY`. | Full reports now use `FINANCIAL REPORT`, `Review posted balances`, `Report scope`, `Refresh`, and `READ ONLY`. Entity/period, report rows, contained table, loading state, GET reader, evidence drill and exact Back-to-Reports context are unchanged. | `npm run test:authoritative-reports` exit 0; `npm run test:authoritative-full-shell` exit 0; `npm run test:authoritative-visual-parity` exit 0; `npm run build` exit 0; `npm test` exit 0 (visual verifier 64/64); `git diff --check` exit 0. | ADDITIVE CANDIDATE — local Reports UX-copy correction only; authenticated full-report runtime and current QBO equivalence remain unverified. |
| Full-report tablet/high-zoom containment | QBO remains on the expired logout route, so no new QBO responsive behavior is claimed. Local stylesheet audit found the shared 768px rule deliberately removed generic table height limits, while the Reports-specific containment resumed only at 720px; a complete statement could therefore become an unbounded page between 721px and 768px. | The existing full-statement table now joins the 60vh keyboard-scrollable containment rule throughout the 768px breakpoint. Sticky headers, every report row, exact nested Back context, refresh GET, evidence drill and read-only authority remain unchanged; no rows are hidden or truncated. | `npm run test:authoritative-reports` exit 0; `npm run test:authoritative-full-shell` exit 0; `npm run test:authoritative-visual-parity` exit 0; `npm run build` exit 0; `npm test` exit 0 (visual verifier 64/64); `git diff --check` exit 0. | ADDITIVE CANDIDATE — local responsive correction only; authenticated 721–768px browser rendering and current QBO equivalence remain unverified. |
| Statement row action language | Browser control remained unavailable and the QBO session was not authenticated, so no new QBO row-action wording is claimed. Local rendered audit found core statement previews and full reports used `accounts in retained evidence`, `Open full report`, an `Evidence` action column, and `Open evidence`, unlike the surrounding concise report navigation. | Core statement previews and full reports now use `N accounts`, `View report`, `Details`, and `View details`. The same row IDs, immutable return context, lineage admission checks, full-page drill, contained table and read-only API data remain unchanged; secondary control reports keep their explicit evidence terminology. | `npm run test:authoritative-reports` exit 0; `npm run test:authoritative-full-shell` exit 0; `npm run test:authoritative-visual-parity` exit 0; `npm run build` exit 0; `npm test` exit 0 (visual verifier 64/64); `git diff --check` exit 0. | ADDITIVE CANDIDATE — local Reports terminology alignment only; authenticated report rows and current QBO equivalence remain unverified. |
| Reports duplicate navigation | The observed QBO Standard reports hierarchy did not present two adjacent copies of the same report-category navigation before Favorites. | Removes the presentation-only static category shelf above the real interactive category tabs. The real tabs retain category switching and exact return context; no report or API reader is removed. | `npm run test:authoritative-reports` exit 0; `npm run test:authoritative-full-shell` exit 0; `npm run test:authoritative-visual-parity` exit 0; `npm run build` exit 0; `npm test` exit 0 (visual verifier 64/64). | CANDIDATE — hierarchy cleanup only; authenticated runtime not verified. |
| Reports one-click full-page drill | User requirement and prior QBO observation establish a report-list → report-page → Back interaction; the catalog should not append or require a second same-report selector. | Core Favorites and matching statement search results now open the existing full-page report directly. Removes the duplicate Financial statements tab row from the preview. Full-page rows are frozen to the exact selected report so title/data cannot drift. Row evidence, refresh, Source/JE drill and exact Back-to-catalog context remain unchanged. | `npm run test:authoritative-reports` exit 0; `npm run test:authoritative-full-shell` exit 0; `npm run test:authoritative-visual-parity` exit 0; `npm run build` exit 0; `npm test` exit 0 (visual verifier 64/64). | CANDIDATE — interaction contract verified locally; authenticated runtime not verified. |
| Full-statement header density | Authenticated QBO Balance Sheet and General Ledger full pages exposed one `Back to standard reports` link; adjacent duplicate section text was not present. Report controls and mutation/export actions were not used. | Full statement pages show one `Back to standard reports`, keep entity and period once in the Evidence scope chip, and use one concise read-only subtitle. Report rows, GET Refresh, evidence drill, exact return context and focus restoration are unchanged. | `npm run test:authoritative-reports` exit 0; `npm run test:authoritative-full-shell` exit 0; `npm run test:authoritative-visual-parity` exit 0; `npm run build` exit 0; `npm test` exit 0 (visual verifier 64/64); `git diff --check` exit 0. | PARTIAL — header hierarchy is observed and locally verified; authenticated populated REFS rendering and unsupported QBO controls remain unverified. |
| Reports Center scope density | QBO authentication remained expired; no new product evidence was claimed. Local review found the same entity/period scope shown in the page-header scope chip and again as a standalone line above statement results. | Removed the duplicate standalone entity/period/read-only line. The top Reporting scope chip remains the single visible scope authority; report search, categories, statement snapshot, preview, Refresh, drills and return context are unchanged. | `npm run test:authoritative-reports` exit 0; `npm run test:authoritative-full-shell` exit 0; `npm run test:authoritative-visual-parity` exit 0; `npm run build` exit 0; `npm test` exit 0 (visual verifier 64/64); `git diff --check` exit 0. | ADDITIVE CANDIDATE — local presentation contract only; authenticated Reports runtime and QBO equivalence remain unverified. |
| Reports finder result consistency | QBO remains at the `TIMER_EXPIRED` logout URL, so no fresh external search behavior is claimed. Local interaction inspection found a statement alias such as `Profit and Loss` correctly returned the existing Income Statement reader while the page simultaneously displayed `No API-backed report category matches`, because the empty state considered categories but not statement or property/project matches. | The finder now reports one concise total across category, core statement and property/project matches. The empty state renders only when all three API-backed result families are empty. Search aliases, direct full-report launch, category state, property/project directory, report GET readers and Back context are unchanged. | `npm run test:authoritative-reports` exit 0; `npm run test:authoritative-full-shell` exit 0; `npm run test:authoritative-visual-parity` exit 0; `npm run build` exit 0; `npm test` exit 0 (visual verifier 64/64); `git diff --check` exit 0. | ADDITIVE CANDIDATE — search/empty-state correction; authenticated runtime and current QBO equivalence remain unverified. |
| Reports finder result isolation | The QBO browser again resolved only to the `TIMER_EXPIRED` logout route, so no fresh external search behavior is claimed. Local Reports rendering showed a `Profit and Loss` search result followed by the complete unfiltered Favorites list, duplicating Income Statement and lengthening a filtered page. | While a finder query is active, Reports now renders its matching statement/category/directory results without appending the unfiltered Favorites list. Clearing the query restores all Favorites, including Accounts receivable aging. Report availability, direct full-page launch, API readers, category state and exact Back context are unchanged. | `npm run test:authoritative-reports` exit 0; `npm run test:authoritative-full-shell` exit 0; `npm run test:authoritative-visual-parity` exit 0; `npm run build` exit 0; `npm test` exit 0 (visual verifier 64/64); `git diff --check` exit 0. | ADDITIVE CANDIDATE — local filtered-page correction only; authenticated runtime and QBO equivalence remain unverified. |
| Reports finder result-only state | QBO remains signed out at `TIMER_EXPIRED`, so no fresh external search behavior is asserted. Continuing the local rendered audit found a statement-only search still followed by an empty property directory, an empty category tablist, and the complete previously selected report workbench. | An active finder query is now a concise result-only state. Empty result families are omitted, the prior report workbench is not appended, and selecting a property or category result clears the query before opening that existing workbench. Direct statement matches still open their full-page report. Clearing search restores the unchanged catalog and all API-backed workbenches. | `npm run test:authoritative-reports` exit 0; `npm run test:authoritative-full-shell` exit 0; `npm run test:authoritative-visual-parity` exit 0; `npm run build` exit 0; `npm test` exit 0 (visual verifier 64/64); `git diff --check` exit 0. | ADDITIVE CANDIDATE — local search information-architecture correction only; authenticated runtime and QBO equivalence remain unverified. |
| Reports finder empty-state copy | The QBO session remains expired, so no new external empty-state wording is claimed. Local UI review found the no-result state exposed internal implementation language (`API-backed report category`) and disagreed with the finder summary. | Both the finder summary and empty state now use `No reports found`. The empty state offers one concise recovery action—try another search or clear it—while retaining the explicit statement that no report data was inferred. Search matching and all report functions are unchanged. | `npm run test:authoritative-reports` exit 0; `npm run test:authoritative-full-shell` exit 0; `npm run test:authoritative-visual-parity` exit 0; `npm run build` exit 0; `npm test` exit 0 (visual verifier 64/64); `git diff --check` exit 0. | ADDITIVE CANDIDATE — UX copy correction only; authenticated runtime and QBO equivalence remain unverified. |
| AP/AR Aging scope density | QBO authentication remained expired; no new product evidence was claimed. Local review found entity, period and as-of scope repeated in a second evidence block; a later route audit also found AP Aging and both AR Aging entry paths passed raw config instead of the shared readable presentation scope. | Removed the duplicate scope block, shortened the header description, and wired all AP/AR Aging routes to `displayConfig`. Company, period and as-of remain explicit in the controls; read-only badge, Refresh, control totals, aging buckets, blocked/empty states and exact Back context are unchanged. | `npm run test:authoritative-aging` exit 0; `npm run test:authoritative-full-shell` exit 0; `npm run test:payables-responsive` exit 0; `npm run test:authoritative-visual-parity` exit 0; `npm run build` exit 0; `npm test` exit 0 (visual verifier 64/64); `git diff --check` exit 0. | ADDITIVE CANDIDATE — local presentation contract only; authenticated aging runtime and QBO equivalence remain unverified. |
| AP/AR historical aging summary and detail | Authenticated QBO evidence showed separate AP/AR Aging Summary and Detail reports scoped by an as-of date, counterparty rows, bucket totals, document facts and explicit Back. Currency-only totals cannot prove vendor/customer or document membership. | Migration 253 adds an exact entity/period/as-of historical snapshot with deterministic ID/hash, server-read vendor/customer summary, paged document detail, source and posted-JE trace, and no-store closed API contracts. Payment/receipt allocations, reversals, credits and AP voids are evaluated by their POSTED journal date; later reversals do not rewrite an earlier snapshot. The UI opens counterparty detail and document evidence as independent read-only pages with Back, while preserving the former currency aggregate only as a control view that makes no membership inference. | API client gate, accounting HTTP/OpenAPI 109/109, migration safety 58/58, fresh PostgreSQL 16 reversible migration 1/1, and fresh PostgreSQL historical payment/reversal snapshot 1/1 all exit 0; authoritative Aging and build gates exit 0. | PARTIAL — authoritative runtime is implemented locally; authenticated Render/OIDC populated AP and AR acceptance, signed persisted snapshot evidence, and QBO visual parity remain unverified. |
| Journal full-page Back density | QBO authentication remained expired; no new product evidence was claimed. Local review found the complete Journal list return context permanently expanded beside Back, making the header long even though the same exact context is needed only when inspected. | Ready, loading and blocked Journal detail states now place the unchanged query/status/date/page/revision return context inside the shared native `List filters retained` disclosure. Back, focus/scroll restoration, immutable scope validation, lines, lineage and controlled workflow behavior are unchanged. | `npm run test:authoritative-journals` exit 0; `npm run test:authoritative-list-context` exit 0; `npm run test:authoritative-full-shell` exit 0; `npm run test:authoritative-visual-parity` exit 0; `npm run build` exit 0; `npm test` exit 0 (visual verifier 64/64); `git diff --check` exit 0. | ADDITIVE CANDIDATE — local presentation contract only; authenticated Journal runtime and QBO equivalence remain unverified. |
| Journal detail field density | QBO authentication remained expired; no new product evidence was claimed. Local review found Journal date, status and revision presented in the scope/header and repeated again in the evidence field grid. | Removed the second copies from the field grid. Journal date and revision remain in the immutable scope strip; status remains in the header badge. Journal number/ID/type, line count, created/posted timestamps, description, lines and source lineage remain visible. | `npm run test:authoritative-journals` exit 0; `npm run test:authoritative-full-shell` exit 0; `npm run test:authoritative-visual-parity` exit 0; `npm run build` exit 0; `npm test` exit 0 (visual verifier 64/64); `git diff --check` exit 0. | ADDITIVE CANDIDATE — local presentation contract only; authenticated Journal runtime and QBO equivalence remain unverified. |
| Journal detail first-screen copy | Browser control remained unavailable and QBO authentication was not confirmed, so no new QBO detail wording is claimed. Local rendered audit found the Journal detail title and line section exposed `EXACT READ EVIDENCE`, `GET-only facts`, `EXACT API LINE FACTS`, and `retained lines`. | Journal detail now uses `JOURNAL ENTRY`, `Review journal lines and posting details`, `JOURNAL LINES`, concise debit/credit guidance, and `N line(s)`. Immutable scope, status, revision, IDs, debit/credit values, dimensions, audit/ledger/source fields, blocked mismatch behavior and exact Back context are unchanged. | `npm run test:authoritative-journals` exit 0; `npm run test:authoritative-full-shell` exit 0; `npm run test:authoritative-visual-parity` exit 0; `npm run build` exit 0; `npm test` exit 0 (visual verifier 64/64); `git diff --check` exit 0. | ADDITIVE CANDIDATE — local Accounting UX-copy correction only; authenticated Journal detail and current QBO equivalence remain unverified. |
| Journal lineage drill presentation | QBO remained signed out, so no new drill wording is claimed. Local rendered audit found the Journal lineage page exposed `Exact GET evidence for Journal <UUID>...` and represented its read-only policy with a permanent `GET only` empty-state card and document-empty icon. | The Journal lineage page now uses `Review posted journal lines`, a shared `READ ONLY` badge, and a collapsed `Journal identifiers` disclosure for Journal ID, revision and currency. The policy is a concise text note rather than an empty-state illustration. Lines, debit/credit, GL/source drills, exact re-reads, fail-closed matching and Back remain unchanged. | `npm run test:authoritative-lineage-drill` exit 0; `npm run test:authoritative-provider-evidence-trace` exit 0; `npm run test:authoritative-browser-acceptance-preflight` exit 0; `npm run test:authoritative-full-shell` exit 0; `npm run test:authoritative-visual-parity` exit 0; `npm run build` exit 0; `npm test` exit 0 (visual verifier 64/64); `git diff --check` exit 0. | ADDITIVE CANDIDATE — local Journal drill readability/icon correction only; authenticated runtime and current QBO equivalence remain unverified. |
| Journal list hierarchy | The signed-in QBO tab remains inaccessible to DOM inspection under browser URL policy, which was not bypassed; no new QBO Journal structure is claimed. Local Accounting audit found a decorative `Register → Scoped evidence → Exact Back` rail immediately above queue cards, filters and the Journal table that already implement those three concepts. | Removed only the duplicate reading rail. Entity/Draft/Needs review/Posted queues, filters, count, pagination, permissions, controlled workflow actions, full-page evidence, immutable lineage, table position and exact Back restoration are unchanged. | `npm run test:authoritative-journals` exit 0; `npm run test:authoritative-full-shell` exit 0; `npm run test:authoritative-visual-parity` exit 0; `npm run build` exit 0; `npm test` exit 0 (visual verifier 64/64); `git diff --check` exit 0. | ADDITIVE CANDIDATE — local hierarchy/density correction; authenticated Journal runtime and QBO equivalence remain unverified. |
| Journal list action language | Browser control remained unavailable and QBO authentication was not confirmed, so no new QBO Journal wording is claimed. Local rendered audit found queue cards and the list exposed `All retained journals`, `Retained status only`, `API list status`, `matching journal entries`, and `Evidence / Open evidence`. | Journal queues now use `All entries`, `Awaiting action`, and `Posted entries`; the count is `N result(s)` and the row action is `Details / View details`. Status filtering, pagination, entity scope, capability read, explicit confirmation, SoD-controlled workflow commands, full-page drill and exact Back context are unchanged. | `npm run test:authoritative-journals` exit 0; `npm run test:authoritative-full-shell` exit 0; `npm run test:authoritative-visual-parity` exit 0; `npm run build` exit 0; `npm test` exit 0 (visual verifier 64/64); `git diff --check` exit 0. | ADDITIVE CANDIDATE — local Accounting terminology alignment only; authenticated Journal workflow/runtime and current QBO equivalence remain unverified. |
| Journal mobile queue density | QBO remained signed out, so no new mobile behavior is claimed. Local responsive audit found the four Journal queue cards changed from a 4-column desktop row to four full-width cards below 600px, pushing the filters and register table far below the phone viewport. | At phone widths the same Entity register, Draft, Needs review and Posted controls now form one keyboard-focusable, horizontally scrollable status strip with compact 150px cards and scroll snapping. Desktop and tablet grids, counts, `aria-pressed`, queue filtering, the Status selector, Journal table, paging, workflow capabilities and full-page Back are unchanged. | `npm run test:authoritative-journals` exit 0; `npm run test:authoritative-full-shell` exit 0; `npm run test:authoritative-visual-parity` exit 0; `npm run build` exit 0; `npm test` exit 0 (visual verifier 64/64); `git diff --check` exit 0. | ADDITIVE CANDIDATE — local mobile density correction only; authenticated mobile runtime and current QBO equivalence remain unverified. |
| General Ledger list density | QBO authentication remained expired; no new product evidence was claimed. Local review found an internal three-step reading rail above a long scope paragraph, duplicating hierarchy already conveyed by the page, table and full-page drill. | Removed the list reading rail, shortened the page subtitle and placed immutable-period/four-decimal/currency rules in a native `Scope rules` disclosure. Entity and period remain directly visible; filters, result count, POSTED table, paging, empty/blocked states and full-page lineage drill are unchanged. | `npm run test:authoritative-general-ledger` exit 0; `npm run test:authoritative-full-shell` exit 0; `npm run test:authoritative-visual-parity` exit 0; `npm run build` exit 0; `npm test` exit 0 (visual verifier 64/64); `git diff --check` exit 0. | ADDITIVE CANDIDATE — local presentation contract only; authenticated General Ledger runtime and QBO equivalence remain unverified. |
| General Ledger first-screen copy | QBO authentication remains expired, so no new external wording is claimed. Local rendered audit found the GL first screen exposed implementation labels (`from the accounting API`, `Search posted evidence`, `Refresh evidence`, and `Showing server page`). | GL now uses `Review posted ledger activity`, `Search`, `Refresh`, `Loading posted ledger lines…`, and `Page N of M`. POSTED-only admission, company/period scope, filters, server paging, GET reads, evidence columns, full-page lineage and exact Back restoration are unchanged. | `npm run test:authoritative-general-ledger` exit 0 (4/4); `npm run test:authoritative-full-shell` exit 0; `npm run test:authoritative-visual-parity` exit 0; `npm run build` exit 0; `npm test` exit 0 (visual verifier 64/64); `git diff --check` exit 0. | ADDITIVE CANDIDATE — local Accounting UX-copy correction only; authenticated GL runtime and current QBO equivalence remain unverified. |
| General Ledger table action language | Browser control remained unavailable and QBO authentication was not confirmed, so no new QBO GL wording is claimed. Local rendered audit found the list exposed `POSTED lines`, `retained evidence columns`, and `Evidence / Open evidence` despite its existing POSTED-only scope and detail drill. | General Ledger now shows `N result(s)`, a neutral contained-table label, and `Details / View details`. POSTED-only reads, entity/period, account/search filters, paging, line values and IDs, full-page lineage drill, table position and exact Back restoration are unchanged. | `npm run test:authoritative-general-ledger` exit 0 (4/4); `npm run test:authoritative-full-shell` exit 0; `npm run test:authoritative-visual-parity` exit 0; `npm run build` exit 0; `npm test` exit 0 (visual verifier 64/64); `git diff --check` exit 0. | ADDITIVE CANDIDATE — local Accounting terminology alignment only; authenticated GL rows and current QBO equivalence remain unverified. |
| General Ledger detail first-screen copy | Browser control remained unavailable and QBO authentication was not confirmed, so no new QBO detail wording is claimed. Local rendered audit found the GL line detail exposed `LINE EVIDENCE`, `scoped API`, `Exact API snapshot`, `current API snapshot`, `Immutable evidence identifiers`, and `API READ`. | The detail now uses `GENERAL LEDGER`, `Review this posted ledger line`, `Posted line details`, a concise Back-restoration note, `Identifiers`, and `READ ONLY`. Account/date/currency/member, debit/credit, Journal and immutable IDs, missing-lineage block and exact Back context are unchanged. | `npm run test:authoritative-general-ledger` exit 0 (4/4); `npm run test:authoritative-full-shell` exit 0; `npm run test:authoritative-visual-parity` exit 0; `npm run build` exit 0; `npm test` exit 0 (visual verifier 64/64); `git diff --check` exit 0. | ADDITIVE CANDIDATE — local Accounting UX-copy correction only; authenticated GL detail and current QBO equivalence remain unverified. |
| Account Register readable scope | QBO authentication remained expired; no new product evidence was claimed. Local source audit found Account Register promoted raw entity and period UUIDs into its visible Evidence scope label. | Register scope now uses the runtime's entity and period presentation labels, falling back to `Configured entity` and `Configured period`; raw UUIDs remain available only to API scope, not visible headings. Register filters, account, currencies, POSTED count, table, paging and exact Back context are unchanged. | `npm run test:authoritative-coa-register` exit 0; `npm run test:authoritative-full-shell` exit 0; `npm run test:authoritative-visual-parity` exit 0; `npm run build` exit 0; `npm test` exit 0 (visual verifier 64/64); `git diff --check` exit 0. | ADDITIVE CANDIDATE — local presentation contract only; authenticated Register runtime and QBO equivalence remain unverified. |
| Account Register first-screen copy | QBO authentication remains expired, so no new external wording is claimed. Local rendered audit found the Register exposed implementation labels including `Evidence scope`, `Currencies returned`, `Search register evidence`, `Refresh evidence`, and retained-snapshot language in empty states. | Register now uses `Posted account activity`, `Company / period`, `Currency`, `Posted entries`, `Search`, `Refresh`, and concise loading/empty guidance. Account, company/period, currency values, POSTED rows, source IDs, filters, paging, contained scroll and exact COA Back restoration are unchanged. | `npm run test:authoritative-coa-register` exit 0; `npm run test:authoritative-full-shell` exit 0; `npm run test:authoritative-visual-parity` exit 0; `npm run build` exit 0; `npm test` exit 0 (visual verifier 64/64); `git diff --check` exit 0. | ADDITIVE CANDIDATE — local Accounting UX-copy correction only; authenticated Register runtime and current QBO equivalence remain unverified. |
| Account Register tablet/high-zoom scope density | QBO remains on the expired logout route, so no new QBO responsive behavior is claimed. Local stylesheet audit found the four Register scope facts used a two-by-two grid of 94px cards from 601px through 1024px even though the phone view already proved the same facts fit a compact horizontal strip. | The existing Account, Company / period, Currency and Posted entries facts now use one keyboard-scrollable, snap-aligned strip through 1024px. Chart of Accounts keeps its separate two-column tablet layout; Register filters, account/period scope, values, table, paging and exact Back context are unchanged. | `npm run test:authoritative-coa-register` exit 0; `npm run test:authoritative-full-shell` exit 0; `npm run test:authoritative-visual-parity` exit 0; `npm run build` exit 0; `npm test` exit 0 (visual verifier 64/64); `git diff --check` exit 0. | ADDITIVE CANDIDATE — local responsive-density correction only; authenticated 601–1024px runtime and current QBO equivalence remain unverified. |
| Chart of Accounts readable scope | QBO authentication remained expired; no new product evidence was claimed. Local source audit found the Chart of Accounts heading always said `Configured entity · configured period`, and the shared COA/Account Inquiry route passed raw config instead of readable runtime scope labels. | Chart of Accounts and Account Inquiry now receive `displayConfig` and reuse the same company/period labels as Account Register, with fail-closed configured-scope fallbacks; internal IDs remain tooltip/API evidence only. Account filters, POSTED balance columns, paging, empty states and Register drill are unchanged. | `npm run test:authoritative-coa-register` exit 0; `npm run test:authoritative-full-shell` exit 0; `npm run test:authoritative-visual-parity` exit 0; `npm run build` exit 0; `npm test` exit 0 (visual verifier 64/64); `git diff --check` exit 0. | ADDITIVE CANDIDATE — local presentation contract only; authenticated Chart of Accounts runtime and QBO equivalence remain unverified. |
| Chart of Accounts first-screen copy | Authenticated QBO showed the concise `Chart of accounts` heading and `Filter by name or number` search prompt. No control, row action, pagination, batch, export or print action was used. | COA now uses the shared `ACCOUNTING` eyebrow, `Review accounts and posted balances`, the observed search prompt, `Status`, `Currency`, `All currencies`, `Refresh`, and concise loading/empty guidance. Account values, active/inactive state, currency separation, POSTED balances, filters, paging, Register drill and exact Back context are unchanged. | `npm run test:authoritative-coa-register` exit 0; `npm run test:authoritative-full-shell` exit 0; `npm run test:authoritative-visual-parity` exit 0; `npm run build` exit 0; `npm test` exit 0 (visual verifier 64/64); `git diff --check` exit 0. | PARTIAL — observed wording is locally aligned; authenticated populated REFS runtime and QBO row destinations remain unverified. |
| COA/Register refresh state | No fresh QBO interaction was available. Local Accounting audit found both Chart of Accounts and Account Register left `Refresh evidence` enabled while their authoritative GET was already loading, allowing duplicate reads with no visible in-progress button state; result-count changes were also not announced. | Both surfaces now disable the read button during `LOADING`, label it `Loading…`, restore `Refresh evidence` when settled, and expose their result count through a polite live region. Filters, paging, table columns, scope, empty/error states, POSTED-only facts and exact Back context are unchanged. | `npm run test:authoritative-coa-register` exit 0; `npm run test:authoritative-full-shell` exit 0; `npm run test:authoritative-visual-parity` exit 0; `npm run build` exit 0; `npm test` exit 0 (visual verifier 64/64); `git diff --check` exit 0. | ADDITIVE CANDIDATE — local loading/read-state correction; authenticated Accounting runtime and QBO equivalence remain unverified. |
| General Ledger read state | No fresh QBO interaction was available. Local Accounting audit found Apply, Reset, Refresh, Previous and Next could issue another authoritative GET while the current General Ledger read was loading, with no in-progress button label. | General Ledger now disables all read-triggering controls during `LOADING`, labels Refresh as `Loading…`, restores the controls when the read settles, and announces result-count changes. Editable filters, server paging, POSTED rows, full-page lineage drill and exact Back behavior are unchanged. | `npm run test:authoritative-general-ledger` exit 0 (4/4); `npm run test:authoritative-full-shell` exit 0; `npm run test:authoritative-visual-parity` exit 0; `npm run build` exit 0; `npm test` exit 0 (visual verifier 64/64); `git diff --check` exit 0. | ADDITIVE CANDIDATE — local loading/read-state correction; authenticated General Ledger runtime and QBO equivalence remain unverified. |
| Reports refresh state | No fresh QBO interaction was available. Local Reports audit found both the Reports Center statement control and a full-page statement kept idle-looking `Refresh` labels while their authoritative GET was already loading. | Both report surfaces now replace the refresh label with disabled `Loading…` feedback for the duration of the read, then restore the original concise action. Report categories, finder, preview cap, full-page report, evidence drill, exact Back context and GET-only boundary are unchanged. | `npm run test:authoritative-reports` exit 0; `npm run test:authoritative-full-shell` exit 0; `npm run test:authoritative-visual-parity` exit 0; `npm run build` exit 0; `npm test` exit 0 (visual verifier 64/64); `git diff --check` exit 0. | ADDITIVE CANDIDATE — local presentation/read-state correction; authenticated Reports runtime and QBO equivalence remain unverified. |
| Secondary Reports read state | QBO DOM inspection remains blocked by browser URL policy and was not bypassed; no external control behavior is claimed. Local Reports audit found eleven existing GET-only readers with inconsistent loading behavior: several load buttons remained enabled during a request, while others were disabled but retained an idle-looking label. | Statement Snapshot, mapped Cash Flow, period comparison, CWIP, construction loan, prepaid, amortization schedules, intercompany, budget, consolidation and exact-dimension profitability now all disable their existing load action during `LOADING` and display `Loading…`. Inputs, API routes, report families, empty/blocked states, full-page evidence and immutable Back context are unchanged. | `npm run test:authoritative-reports` exit 0; `npm run test:authoritative-full-shell` exit 0; `npm run test:authoritative-visual-parity` exit 0; `npm run build` exit 0; `npm test` exit 0 (visual verifier 64/64); `git diff --check` exit 0. | ADDITIVE CANDIDATE — local duplicate-read/read-state correction; authenticated secondary-report runtime and QBO equivalence remain unverified. |
| Reports narrow-screen page length | The current QBO session expired to the Intuit logout route before a new DOM audit, so no QBO mobile behavior is claimed. Local CSS audit first found that the global narrow-screen rule removed every table height limit; a follow-up audit found the Reports override stopped at 720px, leaving secondary Aging, CWIP, Prepaid and dimensional report tables unbounded from 721px through the shared 768px tablet/high-zoom breakpoint. | At widths up to 768px, all Reports-library tables now retain a bounded `min(64vh, 560px)` height and contained overscroll. Report data, columns, filters, keyboard-focusable table regions, full-page evidence drill and Back behavior are unchanged; other workspaces retain their prior narrow-screen behavior. | `npm run test:authoritative-reports` exit 0; `npm run test:authoritative-full-shell` exit 0; `npm run test:authoritative-visual-parity` exit 0; `npm run build` exit 0; `npm test` exit 0 (visual verifier 64/64); `git diff --check` exit 0. | ADDITIVE CANDIDATE — local responsive usability correction; authenticated tablet/mobile runtime and QBO equivalence remain unverified. |
| Expenses duplicate transaction selector | The current QBO session remains expired, so no new QBO behavior is claimed. Local Expenses audit found that the primary tab row already selects All transactions, Bills, or Vendor credits while the filter toolbar repeated the same choice in a second Transaction type select. | Removed the duplicate Transaction type select from the Expenses filter toolbar. The existing accessible tabs remain the single presentation control and continue to drive the same `transactionType` state, list filtering, paging, applied-scope copy, evidence drill and exact Back context. Status, date, vendor, category, search and Reset filters remain unchanged. | `npm run test:authoritative-documents` exit 0; `npm run test:payables-responsive` exit 0; `npm run test:authoritative-full-shell` exit 0; `npm run test:authoritative-visual-parity` exit 0; `npm run build` exit 0; `npm test` exit 0 (visual verifier 64/64); `git diff --check` exit 0. | ADDITIVE CANDIDATE — local information-density correction; authenticated Expenses runtime and QBO equivalence remain unverified. |
| AP/AR shared tab geometry | The current QBO session remains on the `TIMER_EXPIRED` logout route, so no new QBO behavior is claimed. Local component audit found that available Expenses and Receivables tabs rendered without the shared `.tab` class; inactive items therefore fell back to browser-native button chrome while selected and unavailable items used the product system. | Every available AP/AR tab now receives the shared `tab` geometry and adds `tab-on` only when selected. Tab labels, availability, tab semantics, filtering, AP Aging launch, unavailable Vendors/Receipts/Counterparties states and exact Back behavior are unchanged. | `npm run test:authoritative-documents` exit 0; `npm run test:payables-responsive` exit 0; `npm run test:authoritative-full-shell` exit 0; `npm run test:authoritative-visual-parity` exit 0; `npm run build` exit 0; `npm test` exit 0 (visual verifier 64/64); `git diff --check` exit 0. | ADDITIVE CANDIDATE — local cross-page visual-consistency correction; authenticated AP/AR runtime and QBO equivalence remain unverified. |
| Accounting list pagination styling | The current QBO session remains on the `TIMER_EXPIRED` logout route, so no new QBO behavior is claimed. Local Accounting audit found AP, AR, Vendor credits and Journal pagination used unclassed browser-native buttons. The only pagination layout selector targeted a direct child that these nested page structures never produce, leaving the controls visually uncontained. | AP/AR and Journal Previous/Next controls now use the shared small ghost-button geometry. A shared wrapping pagination row now applies at the actual nested locations. Page counts, disabled boundaries, list filters, page state, API reads, evidence drills and exact Back restoration are unchanged. | `npm run test:authoritative-documents` exit 0; `npm run test:authoritative-journals` exit 0; `npm run test:payables-responsive` exit 0; `npm run test:authoritative-visual-parity` exit 0; `npm run build` exit 0; `npm test` exit 0 (visual verifier 64/64); `git diff --check` exit 0. | ADDITIVE CANDIDATE — local cross-page pagination consistency correction; authenticated paged runtime and QBO equivalence remain unverified. |
| COA/Register concise scope | The current QBO session remains expired, so no new QBO behavior is claimed. Local Accounting audit found the visible COA scope rendered a full technical sentence about entity, period, currency separation and fixed four-decimal strings, duplicating information already expressed by the heading, filters and currency columns. | The shared COA/Register scope now renders `Company · Period`, while COA adds the concise suffix `balances by currency`. Exact entity/period IDs and the fixed-four-decimal API contract remain in the scope tooltip. Filters, columns, amount values, paging, Register drill, empty/error states and exact Back restoration are unchanged. | `npm run test:authoritative-coa-register` exit 0; `npm run test:authoritative-full-shell` exit 0; `npm run test:authoritative-visual-parity` exit 0; `npm run build` exit 0; `npm test` exit 0 (visual verifier 64/64); `git diff --check` exit 0. | ADDITIVE CANDIDATE — local information-density correction; authenticated Accounting runtime and QBO equivalence remain unverified. |
| Reports duplicate category heading | The current QBO session remains expired, so no new QBO behavior is claimed. Local Reports audit found the selected category was already explicit in the accessible tab row, then repeated immediately below as a second visible workbench heading. | Removed the duplicate category title inside the report workbench. The category remains named by the selected tab and the workbench `aria-label`; Statements keeps a compact right-aligned Refresh action row. Search, categories, shortcuts, report readers, empty/blocked states, full-page evidence and Back remain unchanged. | `npm run test:authoritative-reports` exit 0; `npm run test:authoritative-full-shell` exit 0; `npm run test:authoritative-visual-parity` exit 0; `npm run build` exit 0; `npm test` exit 0 (visual verifier 64/64); `git diff --check` exit 0. | ADDITIVE CANDIDATE — local hierarchy/density correction; authenticated Reports runtime and QBO equivalence remain unverified. |
| Source Documents readable scope | QBO opened only the `TIMER_EXPIRED` logout page, so no new product evidence was claimed. Local source audit found Source Documents promoted raw entity/period UUIDs into the primary scope strip, repeated a long technical scope paragraph, and its authoritative route did not pass the shared readable presentation scope. | The visible scope strip now uses authoritative company and period labels with configured-scope fallbacks; the production route supplies the shared `displayConfig`, while IDs remain tooltip/API scope only. The header description is shortened and the existing evidence disclosure retains the detailed exclusions. Filters, metrics, table columns, full-page lineage detail and exact Back restoration are unchanged. | `npm run test:authoritative-source-documents` exit 0; `npm run test:authoritative-full-shell` exit 0; `npm run test:authoritative-visual-parity` exit 0; `npm run build` exit 0; `npm test` exit 0 (visual verifier 64/64); `git diff --check` exit 0. | ADDITIVE CANDIDATE — local presentation contract only; authenticated Source Documents runtime and QBO equivalence remain unverified. |
| Source Documents narrow-screen page length | QBO remains on the expired logout route, so no new QBO responsive behavior is claimed. Local source audit found both the nine-column Source Documents list and seven-column retained-line detail lost their table height limit at the shared 768px breakpoint, allowing long source evidence to stretch the full page. | Both existing keyboard-focusable table regions now retain a 60vh local scroller through the tablet/phone breakpoint. Every source/document field, immutable detail, filters, API reads and exact list scroll/focus Back context remain unchanged; no row is hidden or truncated. | `npm run test:authoritative-source-documents` exit 0; `npm run test:authoritative-full-shell` exit 0; `npm run test:authoritative-visual-parity` exit 0; `npm run build` exit 0; `npm test` exit 0 (visual verifier 64/64); `git diff --check` exit 0. | ADDITIVE CANDIDATE — local responsive page-length correction only; authenticated narrow-screen runtime and current QBO equivalence remain unverified. |
| Accounting Operations readable scope | QBO remains on the `TIMER_EXPIRED` logout page, so no new product evidence was claimed. Local route audit found Project Cost & CWIP, Unit / Lot profitability, Property Ops, Construction Loan, Amortization, Intercompany and Consolidation passed raw config while their workspaces expect the shared readable company/period presentation. | All seven read-only Accounting Operations routes now receive `displayConfig`, keeping their visible scope consistent with the authoritative top bar and Reports Center. API entity/period IDs, filters, report readers, evidence drills, Back behavior and every mutation boundary are unchanged. | `npm run test:authoritative-full-shell` exit 0; `npm run test:authoritative-reports` exit 0; `npm run test:authoritative-amortization` exit 0; `npm run test:authoritative-property-rent` exit 0; `npm run test:authoritative-visual-parity` exit 0; `npm run build` exit 0; `npm test` exit 0 (visual verifier 64/64); `git diff --check` exit 0. | ADDITIVE CANDIDATE — local scope consistency only; authenticated runtime and QBO equivalence remain unverified. |
| AP/AR full-page detail readable scope | QBO remains on the `TIMER_EXPIRED` logout page, so no new product evidence was claimed. Local Expenses/Receivables source audit found Bill, Invoice and adjustment details hard-coded `Configured entity` and `Configured period` even after their parent routes loaded authoritative presentation labels. | AP and AR document/adjustment detail routes now pass `displayConfig`; details display the shared company and period labels only when exact entity/period IDs match, otherwise retaining fail-closed configured-scope fallbacks. Raw IDs remain audit tooltips. List filters, immutable lineage, evidence fields and exact Back restoration are unchanged. | `npm run test:authoritative-documents` exit 0; `npm run test:payables-responsive` exit 0; `npm run test:authoritative-full-shell` exit 0; `npm run test:authoritative-visual-parity` exit 0; `npm run build` exit 0; `npm test` exit 0 (visual verifier 64/64); `git diff --check` exit 0. | ADDITIVE CANDIDATE — local detail readability only; authenticated detail runtime and QBO equivalence remain unverified. |
| Journal / GL / Source / Report drill readable Back scope | QBO remains on the `TIMER_EXPIRED` logout page, so no new product evidence was claimed. Local Accounting source audit found the shared immutable lineage drill retained exact IDs internally but did not carry the runtime presentation labels into each frame, causing Back rows to fall back to generic entity/period text. | The shared drill now decorates every rendered Journal, GL, Source, report chooser, report row and evidence frame with the same company/period labels as the authoritative shell. Missing presentation metadata keeps the honest configured-scope fallback. GET-only re-reads, immutable-match guards, frame stack, IDs and Back behavior are unchanged. | `npm run test:authoritative-lineage-drill` exit 0; `npm run test:authoritative-journals` exit 0; `npm run test:authoritative-general-ledger` exit 0; `npm run test:authoritative-reports` exit 0; `npm run test:authoritative-visual-parity` exit 0; `npm run build` exit 0; `npm test` exit 0 (visual verifier 64/64); `git diff --check` exit 0. | ADDITIVE CANDIDATE — local drill readability only; authenticated lineage runtime and QBO equivalence remain unverified. |
| Journal loading / blocked detail scope | QBO remains on the `TIMER_EXPIRED` logout page, so no new product evidence was claimed. Local state-path audit found Journal ready evidence now used readable labels, while the full-page loading/error return context still froze only a period label and hard-coded the company fallback. | Opening a Journal now freezes both authoritative company and period presentation labels with the immutable return token before the GET begins. Ready, loading and blocked detail states therefore use the same scope wording; missing metadata still fails back to configured scope. Detail reads, status guards, IDs, workflow permissions and exact Back restoration are unchanged. | `npm run test:authoritative-journals` exit 0; `npm run test:authoritative-lineage-drill` exit 0; `npm run test:authoritative-full-shell` exit 0; `npm run test:authoritative-visual-parity` exit 0; `npm run build` exit 0; `npm test` exit 0 (visual verifier 64/64); `git diff --check` exit 0. | ADDITIVE CANDIDATE — state consistency only; authenticated Journal runtime and QBO equivalence remain unverified. |
| Prior-period report drill scope | QBO remains on the `TIMER_EXPIRED` logout page, so no new product evidence was claimed. Local Reports audit found a prior-period evidence row changed the immutable period ID but could retain the current period's readable label in the shared lineage page. | Current/prior comparison rows now carry their matching period code into the evidence drill. The shared lineage config displays that selected code; a non-current row without a readable code uses `Selected report period` rather than incorrectly reusing the current label. Source IDs, GET readers, immutable scope guards, nested Back and report filters are unchanged. | `npm run test:authoritative-reports` exit 0; `npm run test:authoritative-lineage-drill` exit 0; `npm run test:authoritative-full-shell` exit 0; `npm run test:authoritative-visual-parity` exit 0; `npm run build` exit 0; `npm test` exit 0 (visual verifier 64/64); `git diff --check` exit 0. | ADDITIVE CANDIDATE — local scope correctness only; authenticated report drill runtime and QBO equivalence remain unverified. |
| Bank Batch Pipeline readable scope | QBO remains on the `TIMER_EXPIRED` logout page, so no new product evidence was claimed. Local route audit found direct Bank Transactions and Reconcile entries received the runtime's readable company/period presentation, while their combined Bank Batch Pipeline route passed only raw API scope and displayed a generic company fallback. | The combined route now passes the same `displayConfig` as both direct entries. Its Bank and Reconciliation readers therefore retain one consistent company/period presentation while exact IDs stay in API scope. Queue reads, statement worksheet, filters, detail/Back and all command/permission boundaries are unchanged. | `npm run test:authoritative-bank-batch-pipeline` exit 0; `npm run test:authoritative-bank` exit 0; `npm run test:authoritative-full-shell` exit 0; `npm run test:authoritative-visual-parity` exit 0; `npm run build` exit 0; `npm test` exit 0 (visual verifier 64/64); `git diff --check` exit 0. | ADDITIVE CANDIDATE — local scope consistency only; authenticated Bank runtime and QBO equivalence remain unverified. |
| AP/AR adjustment detail Back context | QBO remains on the `TIMER_EXPIRED` logout page, so no new product evidence was claimed. Local Expenses/Receivables audit found adjustment details retained exact list state internally but did not receive or expose the immutable return token; the shell instead showed only query/page as permanently expanded global text. | Adjustment details now receive the exact return token and present the full query/status/date/vendor/category/type/page scope in the same default-closed `List filters retained` disclosure as Bills and Invoices. The duplicate global return text is removed and the action boundary is shortened. Back still restores filters, page, focus and scroll; evidence fields, lineage and all mutations remain unchanged. | `npm run test:authoritative-documents` exit 0; `npm run test:payables-responsive` exit 0; `npm run test:authoritative-full-shell` exit 0; `npm run test:authoritative-visual-parity` exit 0; `npm run build` exit 0; `npm test` exit 0 (visual verifier 64/64); `git diff --check` exit 0. | ADDITIVE CANDIDATE — local Back/presentation contract only; authenticated detail runtime and QBO equivalence remain unverified. |
| COA → Register exact Back position | QBO remains on the `TIMER_EXPIRED` logout page, so no new product evidence was claimed. Local Accounting audit found COA → Register retained account filters, status, currency, page and focus, but Back did not restore the list's vertical page position or its contained wide-table horizontal position. | Opening a Register now freezes the configured entity/period plus page and contained-table scroll coordinates. Back restores vertical scroll, horizontal table position and the exact opener only when the frozen entity/period still match. Account/register GET readers, filters, balances, columns and the no-write boundary are unchanged. | `npm run test:authoritative-coa-register` exit 0; `npm run test:coa-readonly-shell` exit 0; `npm run test:authoritative-full-shell` exit 0; `npm run test:authoritative-visual-parity` exit 0; `npm run build` exit 0; `npm test` exit 0 (visual verifier 64/64); `git diff --check` exit 0. | ADDITIVE CANDIDATE — local Back restoration only; authenticated Register runtime and QBO equivalence remain unverified. |
| Full report → account evidence nested Back | QBO remains on the `TIMER_EXPIRED` logout page, so no new product evidence was claimed. Local Reports audit found account evidence opened from a long full statement reused the Reports Center's older scroll context; nested Back restored the full report component but not the row's current vertical position, contained-table horizontal position or opener focus. | The nested drill now freezes a fresh same-entity/same-period return token at the full-report row, including page scroll, contained-table scroll and row focus. Back restores all three before the outer Back returns to the exact Reports category/search/preview. Report GET readers, rows, lineage gates and no-export/no-customize boundary are unchanged. | `npm run test:authoritative-reports` exit 0; `npm run test:authoritative-full-shell` exit 0; `npm run test:authoritative-visual-parity` exit 0; `npm run build` exit 0; `npm test` exit 0 (visual verifier 64/64); `git diff --check` exit 0. | ADDITIVE CANDIDATE — local nested Back contract only; authenticated report runtime and QBO equivalence remain unverified. |
| WBS/AutoRec secondary evidence density | QBO remains on the `TIMER_EXPIRED` logout page, so no new QBO product evidence was claimed. Local authoritative WBS audit found the primary Payables review and list were followed by AutoRec review, Match Review/G11 and control reconciliation readers all expanded at once, producing a very long default page despite these being secondary exact-ID evidence tools. | The three readers remain fully available but now share one default-closed native disclosure pattern with concise titles and GET-only status. Opening a disclosure retains every existing field, state, table, blocked/empty message and read endpoint; Payables, WBS observation, signed-contract verification and all no-write boundaries are unchanged. | `npm run test:authoritative-wbs-transition` exit 0; `npm run test:authoritative-wbs-payable` exit 0; `npm run test:authoritative-wbs-live-pilot-bridge` exit 0; `npm run test:authoritative-full-shell` exit 0; `npm run test:authoritative-visual-parity` exit 0; `npm run build` exit 0; `npm test` exit 0 (visual verifier 64/64); `git diff --check` exit 0. | ADDITIVE CANDIDATE — REFS-specific density improvement, not a QBO-equivalence claim; authenticated WBS/AutoRec runtime remains unverified. |
| AutoRec review and Match Review state language | QBO redirects to Intuit sign-in, and these WBS/AutoRec controls are REFS-specific rather than observed QBO equivalents. Local design-system audit found the default-closed readers still exposed `persisted`, `PostgreSQL`, `UUID`, and transport-oriented empty-result wording after users opened them. | Review loading, empty and blocked copy now describes the selected company/source records and retained candidates directly. Match Review asks for a retained review ID and clearly says posted evidence remains unavailable until completion. Receipt-backed selection, exact ID validation, G11 guards, read endpoints, rows and all no-action boundaries are unchanged. | `npm run test:authoritative-wbs-transition` exit 0; `npm run test:authoritative-wbs-payable` exit 0; `npm run test:authoritative-wbs-live-pilot-bridge` exit 0; `npm run test:authoritative-full-shell` exit 0; `npm run test:authoritative-visual-parity` exit 0; `npm run build` exit 0; `git diff --check` exit 0. | ADDITIVE CANDIDATE — REFS-specific presentation correction only; QBO equivalence and authenticated WBS/AutoRec runtime remain unverified. |
| WBS observation first-screen language | QBO redirects to Intuit sign-in, and the WBS observation surface is a REFS-specific integration control rather than an observed QBO equivalent. Local design-system audit found its default header, scope guidance, loading and empty state repeatedly exposed API/GET transport mechanics and a long explanation before any rows were shown. | The connection now says it displays read-only WBS evidence. Scope, loading and empty states are shorter while retaining the ten-row bound, signed-in company context, mixed-company exception treatment and explicit prohibition on Review, Draft and Post. Provider scope inputs, observation hashes, retained evidence, TEST ONLY workflow gates and every API command boundary are unchanged. | `npm run test:authoritative-wbs-live-pilot-bridge` exit 0; `npm run test:authoritative-wbs-transition` exit 0; `npm run test:authoritative-wbs-payable` exit 0; `npm run test:authoritative-full-shell` exit 0; `npm run test:authoritative-visual-parity` exit 0; `npm run build` exit 0; `git diff --check` exit 0. | ADDITIVE CANDIDATE — REFS-specific presentation correction only; authenticated WBS/Render behavior and QBO equivalence remain unverified. |
| WBS / REFS control reconciliation state language | A fresh read-only attempt reached the Intuit sign-in page, so no QBO behavior was claimed; this control reconciliation is REFS-specific. Local design-system audit found its loading, blocked and empty states repeated `immutable`, `exact`, `metric snapshot` and all-caps internal status language instead of a concise accounting outcome. | The reader now uses `Loading control reconciliation`, names the WBS receipt, approved mapping and REFS snapshot once, and asks users to choose Cost GL or Property. Missing receipt or mapping evidence remains blocked and is explicitly never treated as zero. Scope inputs, source/target/difference rows, comparison result, receipt/mapping validation and no-action authority are unchanged. | `npm run test:authoritative-wbs-transition` exit 0; `npm run test:authoritative-wbs-payable` exit 0; `npm run test:authoritative-wbs-live-pilot-bridge` exit 0; `npm run test:authoritative-full-shell` exit 0; `npm run test:authoritative-visual-parity` exit 0; `npm run build` exit 0; `git diff --check` exit 0. | ADDITIVE CANDIDATE — REFS-specific presentation correction only; authenticated WBS/Render behavior and QBO equivalence remain unverified. |
| Verified WBS transition table geometry | The QBO session remains expired, and this signed WBS contract is a REFS-specific control rather than a QBO-equivalence surface. Local source audit found the transition table had a dedicated six-column 1050px layout in the shared stylesheet, but the rendered table omitted that class, so the intended column geometry never applied and phone views became unbounded. | The verified transition table now activates its existing dedicated class and keeps a 60vh contained scroller at phone widths. Transition IDs, operations, observed states, reason requirements, roles, accounting guards, signed contract facts and every false authority flag are unchanged; no WBS or accounting command is added. | `npm run test:authoritative-wbs-transition` exit 0; `npm run test:authoritative-full-shell` exit 0; `npm run test:authoritative-visual-parity` exit 0; `npm run build` exit 0; `npm test` exit 0 (visual verifier 64/64); `git diff --check` exit 0. | ADDITIVE CANDIDATE — REFS-specific responsive/geometry correction only; authenticated WBS runtime remains unverified. |
| WBS review/control narrow-screen containment | The QBO session remains expired, and AutoRec review/control evidence is a REFS-specific workflow rather than an observed QBO equivalent. Follow-up source audit found the ten-column Review candidates and five-column Control reconciliation tables already had dedicated geometry but, unlike the transition table beside them, lost their height limit at the shared 768px reset. | Transition, Review candidates and Control reconciliation now share one 60vh keyboard-scrollable containment rule. Candidate IDs, source/version/date/currency/amount/bank/mapping facts, control source/target/difference/result rows and all signed/no-authority boundaries remain unchanged. | `npm run test:authoritative-wbs-transition` exit 0; `npm run test:authoritative-wbs-payable` exit 0; `npm run test:authoritative-full-shell` exit 0; `npm run test:authoritative-visual-parity` exit 0; `npm run build` exit 0; `npm test` exit 0 (visual verifier 64/64); `git diff --check` exit 0. | ADDITIVE CANDIDATE — REFS-specific responsive correction only; authenticated WBS/AutoRec runtime remains unverified. |
| Reviewed WBS Payables first-screen density | The QBO session remains expired; reviewed WBS Payables is a REFS-specific, server-authoritative workflow rather than an observed QBO equivalent. Local audit found its first-screen subtitle exposed migration/read-model wording (`exact 094 review evidence`) and its Back-restorable list scroller had no stable class, so the generic phone override could make the page unbounded. | The subtitle now says `Review approved source evidence and prepare a Draft.`, Refresh uses the shared concise label, and the list receives a stable class with 60vh phone containment. Exact review evidence, maker reason, server revalidation, AP Bill/AUTO Journal Draft-only command, row values, full-page evidence and Back scroll/focus restoration are unchanged; submit/review/approve/post remain unavailable here. | `npm run test:authoritative-wbs-payable` exit 0; `npm run test:authoritative-wbs-transition` exit 0; `npm run test:authoritative-full-shell` exit 0; `npm run test:authoritative-visual-parity` exit 0; `npm run build` exit 0; `npm test` exit 0 (visual verifier 64/64); `git diff --check` exit 0. | ADDITIVE CANDIDATE — REFS-specific readability/responsive correction only; authenticated WBS runtime remains unverified. |
| WBS Payables loading and empty-state language | The QBO session redirects to Intuit sign-in, so no fresh QBO behavior was claimed. Local design-system audit found the reviewed and admitted Payables states exposed `PostgreSQL`, the internal `READY_FOR_DRAFT` enum, `authenticated API`, and a long implementation-level validation inventory on the primary workflow surface. | Loading and empty states now use concise accounting language: reviewed Payables load directly, Draft readiness is described without an enum, and admitted review names source/scope/mapping/SoD/attachments while explicitly retaining the signed-only boundary. API readers, receipt/mapping/attachment checks, reviewer/maker separation, Draft command and all submit/approve/post exclusions are unchanged. | `npm run test:authoritative-wbs-payable` exit 0; `npm run test:authoritative-wbs-transition` exit 0; `npm run test:authoritative-full-shell` exit 0; `npm run test:authoritative-visual-parity` exit 0; `npm run build` exit 0; `git diff --check` exit 0. | ADDITIVE CANDIDATE — REFS-specific presentation correction only; QBO runtime equivalence and authenticated WBS data remain unverified. |
| Reviewed WBS Payable detail Back | QBO remains on the `TIMER_EXPIRED` logout page, so no new QBO product evidence was claimed. Local WBS Payable audit found list → evidence detail replaced the list correctly, but Back only cleared the detail and did not restore the long page's vertical position, the wide list table's horizontal position or the exact row action focus. | Opening evidence now freezes entity/period, page scroll, table scroll and a stable row-action focus ID before the detail GET. Back restores all three only for the unchanged scope. Reviewed-evidence reads, Draft command, idempotency, SoD and every retained evidence field remain unchanged. | `npm run test:authoritative-wbs-payable` exit 0; `npm run test:authoritative-wbs-transition` exit 0; `npm run test:authoritative-full-shell` exit 0; `npm run test:authoritative-visual-parity` exit 0; `npm run build` exit 0; `npm test` exit 0 (visual verifier 64/64); `git diff --check` exit 0. | ADDITIVE CANDIDATE — REFS-specific Back improvement, not a QBO-equivalence claim; authenticated WBS Payable runtime remains unverified. |
| AP/AR evidence wide-table Back | QBO remains on the `TIMER_EXPIRED` logout page, so no new QBO product evidence was claimed. Local authoritative Expenses audit found Bill/Invoice and adjustment evidence already replaced the list and retained filters, pagination, page scroll and opener focus, but Back reset the contained wide table to its left edge. | Document and adjustment row actions now freeze their own table scroll coordinate in the same entity/period return token. Back remounts the correct Bill/Invoice or adjustment list, restores page and table position, then focuses the exact opener. OIDC/API reads, filters, pagination, immutable lineage blocking and all no-action boundaries are unchanged. | `npm run test:authoritative-list-context` exit 0; `npm run test:authoritative-documents` exit 0; `npm run test:authoritative-full-shell` exit 0; `npm run test:authoritative-visual-parity` exit 0; `npm run build` exit 0; `npm test` exit 0 (visual verifier 64/64); `git diff --check` exit 0. | ADDITIVE CANDIDATE — local authoritative Back contract only; authenticated AP/AR runtime and QBO equivalence remain unverified. |
| Journal register wide-table Back | QBO remains on the `TIMER_EXPIRED` logout page, so no new QBO product evidence was claimed. Local authoritative Accounting audit found Journal list → exact detail already retained filters, page, entity, period, revision, currency, page scroll and opener focus, but Back reset the nine-column register's contained horizontal scroll. | The Journal evidence action now freezes its own table coordinate in the existing immutable scope token. Back remounts the register, restores page and table position, then focuses the exact Journal opener. Exact detail GET, lineage scope guards, capability reads and existing workflow commands are unchanged. | `npm run test:authoritative-journals` exit 0; `npm run test:authoritative-list-context` exit 0; `npm run test:authoritative-full-shell` exit 0; `npm run test:authoritative-visual-parity` exit 0; `npm run build` exit 0; `npm test` exit 0 (visual verifier 64/64); `git diff --check` exit 0. | ADDITIVE CANDIDATE — local authoritative Back contract only; authenticated Journal runtime and QBO equivalence remain unverified. |
| Reports evidence wide-table Back | QBO remains on the `TIMER_EXPIRED` logout page, so no new QBO product evidence was claimed. Local authoritative Reports audit found nested full-statement account evidence already restored its contained table position, but the shared report preview, snapshot, cash-flow, comparison, rollforward and dimensional evidence opener restored only page and focus. | Every shared report evidence action now freezes the horizontal position of its own originating table in the existing immutable entity/period return token. Back remounts the exact catalog or workbench view, restores page and table position, then focuses the original row action. GET-only report readers, calculations, lineage guards, filters and no-export/no-customize boundaries are unchanged. | `npm run test:authoritative-reports` exit 0; `npm run test:authoritative-list-context` exit 0; `npm run test:authoritative-full-shell` exit 0; `npm run test:authoritative-visual-parity` exit 0; `npm run build` exit 0; `npm test` exit 0 (visual verifier 64/64); `git diff --check` exit 0. | ADDITIVE CANDIDATE — local authoritative Back contract only; authenticated Reports runtime and QBO equivalence remain unverified. |
| Bank Transactions wide-table Back | QBO remains on the `TIMER_EXPIRED` logout page, so no new QBO product evidence was claimed. Local authoritative Banking audit found list → independent detail already retained account, date range, API offset, page scroll and opener focus, but Back reset the seven-column contained table to its left edge. | The Bank detail action now freezes its originating table coordinate in the same immutable entity/period/account/date/page token. Back remounts the scoped queue, restores page and table position, then focuses the exact source row. OIDC/API reads, server Match validation, Reconcile boundaries, pagination and every posting/payment/export exclusion are unchanged. | `npm run test:authoritative-bank` exit 0; `npm run test:authoritative-list-context` exit 0; `npm run test:authoritative-full-shell` exit 0; `npm run test:authoritative-visual-parity` exit 0; `npm run build` exit 0; `npm test` exit 0 (visual verifier 64/64); `git diff --check` exit 0. | ADDITIVE CANDIDATE — local authoritative Back contract only; authenticated Banking runtime and QBO equivalence remain unverified. |
| General Ledger wide-table Back | QBO remains on the `TIMER_EXPIRED` logout page, so no new QBO product evidence was claimed. Local authoritative Accounting audit found the nine-column POSTED ledger list → lineage detail retained the current in-memory API page, filters, opener focus and page scroll, but did not retain its contained horizontal table position. | Opening a ledger line now records the originating table coordinate beside its stable opener and page position. Exiting the lineage drill remounts the same list, restores the table, returns focus to the exact evidence action and restores page position through an injectable environment rather than a global browser singleton. API data, pagination, immutable lineage IDs and all no-adjust/no-post/no-export boundaries are unchanged. | `npm run test:authoritative-general-ledger` exit 0; `npm run test:authoritative-full-shell` exit 0; `npm run test:authoritative-visual-parity` exit 0; `npm run build` exit 0; `npm test` exit 0 (visual verifier 64/64); `git diff --check` exit 0. | ADDITIVE CANDIDATE — local authoritative Back contract only; authenticated General Ledger runtime and QBO equivalence remain unverified. |
| General Ledger detail density | QBO remains on the `TIMER_EXPIRED` logout page, so no new QBO product evidence was claimed. The user's established product requirement is a concise, consistent full-page detail. Local audit found a three-step reading rail and permanently expanded return-context paragraph repeating hierarchy already conveyed by the Back row, scope strip, amount card and identifier section. | Removed the duplicate reading rail, shortened explanatory copy and moved the unchanged account/search/page context into a native `List filters retained` disclosure. Debit, credit, Journal, description, immutable Journal/line/ledger/source IDs, missing-lineage state and exact Back behavior remain present. | `npm run test:authoritative-general-ledger` exit 0; `npm run test:authoritative-full-shell` exit 0; `npm run test:authoritative-visual-parity` exit 0; `npm run build` exit 0; `npm test` exit 0 (visual verifier 64/64); `git diff --check` exit 0. | ADDITIVE CANDIDATE — presentation-only; authenticated General Ledger runtime and QBO equivalence remain unverified. |
| Reconciliation scope and copy density | QBO remains on the `TIMER_EXPIRED` logout page, so no new QBO product evidence was claimed. The user's established requirement is a concise, consistent accounting page. Local audit found entity/account/cutoff/status repeated across the header, scope-picker description, post-filter sentence, summary status sentence, detail scope strip and detail footer; the detail scope strip also promoted a raw entity UUID. | Shortened the workspace, picker, summary and detail descriptions; removed the duplicate post-filter, summary-status and footer sentences. Status remains a direct field in the summary scope strip, and detail scope now uses the authoritative entity presentation label with a `Configured entity` fallback instead of the raw UUID. Existing statement discovery, signed-source selection, worksheet, lifecycle commands, revision/idempotency/SoD guards, empty/blocked states and Back are unchanged. | `npm run test:authoritative-bank` exit 0; `npm run test:authoritative-bank-responsive` exit 0 (320/900/1280 and 200% zoom); `npm run test:authoritative-full-shell` exit 0; `npm run test:authoritative-visual-parity` exit 0; `npm run build` exit 0; `npm test` exit 0 (visual verifier 64/64); `git diff --check` exit 0. | ADDITIVE CANDIDATE — presentation/readability only; authenticated Reconciliation runtime and QBO equivalence remain unverified. |
| Bank Transaction detail density | QBO remains on the `TIMER_EXPIRED` logout page, so no new QBO product evidence was claimed. The user's established requirement is a concise full-page detail. Local audit found account, Match state and source version duplicated between the immutable scope strip and transaction field grid, while the retained parent date range appeared only in a separate footer sentence. | Consolidated entity, account, date range, source version and Match state into the scope strip; removed their duplicate field-grid copies and the footer sentence. Transaction date, direction, amount, type, source/Journal/Match IDs, lifecycle, scope mismatch block, controller Match correction and exact Back remain unchanged. | `npm run test:authoritative-bank` exit 0; `npm run test:authoritative-bank-responsive` exit 0 (320/900/1280 and 200% zoom); `npm run test:authoritative-full-shell` exit 0; `npm run test:authoritative-visual-parity` exit 0; `npm run build` exit 0; `npm test` exit 0 (visual verifier 64/64); `git diff --check` exit 0. | ADDITIVE CANDIDATE — presentation-only; authenticated Bank detail runtime and QBO equivalence remain unverified. |
| Source Documents boundary density | QBO remains on the `TIMER_EXPIRED` logout page, so no new QBO product evidence was claimed. Source Documents is a REFS evidence register rather than a claimed QBO-equivalent surface. Local audit found its Included, Excluded and Read-only boundary explanations permanently expanded between the scope strip and filters, lengthening every default visit. | The complete evidence-boundary block now uses the shared default-closed `Evidence boundaries` disclosure. Persisted metadata/source identity/line references, attachment/raw-payload exclusions and no-import/no-map/no-post/no-external-call statements remain available unchanged. KPI counts, readable scope, filters, table, full-page lineage and exact Back are unchanged. | `npm run test:authoritative-source-documents` exit 0; `npm run test:authoritative-full-shell` exit 0; `npm run test:authoritative-visual-parity` exit 0; `npm run build` exit 0; `npm test` exit 0 (visual verifier 64/64); `git diff --check` exit 0. | ADDITIVE CANDIDATE — REFS-specific presentation improvement, not a QBO-equivalence claim; authenticated Source Documents runtime remains unverified. |

## Explicit unverified gaps

- QBO populated Expenses table columns, row selection, detail drill, permissions, audit history, vendor/bill/payment linkage, and non-empty responsive behavior were not observed in this round.
- QBO Print Checks, New transaction, QuickBooks Payments promotion, Export to Excel, Print, settings, external connections, contractor/1099 workflows, payment initiation, and mutation behavior are excluded and were not exercised.
- Authoritative Render/OIDC/PostgreSQL runtime behavior was not tested in this local UI branch. Passing SSR/build tests does not establish production or QBO equivalence.
- AR remains unchanged because its comparable QBO page was not audited in this round.
- QBO report search result behavior, category switching, populated report rows, permissions, audit trail, and narrow full-report drill were not exercised in this round. Smart Tip, Performance center, customize/save/share/export/print and promotions remain intentionally excluded.

## Accounting evidence reuse and candidate — Chart of Accounts (2026-08-19)

| Surface | Evidence source | REFS change | Verification | Status |
|---|---|---|---|---|
| COA list hierarchy | Authenticated read-only QBO DOM observed `Chart of accounts`, name-or-number filtering, limit `All`, `Showing accounts 1–200`, the Name / Account type / QuickBooks balance / Bank balance / Action table, and populated-row action `View register`. No account or row action was invoked. | Removes the COA-only reading rail and duplicate four-card summary. Keeps API filters, retained currency evidence, wide table, `View register`, Refresh GET, paging, empty/error states and full-page Register/Back. Pagination reads `Showing accounts X to Y of Z`; Register uses the same range wording for entries. | `npm run test:authoritative-coa-register` exit 0; `npm run test:authoritative-full-shell` exit 0; `npm run test:authoritative-visual-parity` exit 0; `npm run build` exit 0; `npm test` exit 0 (visual verifier 64/64). | PARTIAL — QBO-specific balances and row destinations plus authenticated populated REFS runtime remain unverified. |
| Scope readability | QBO list evidence did not expose REFS internal entity/period UUIDs as primary page copy. | COA keeps exact entity/period IDs in a title tooltip and displays concise `Configured entity · configured period` text. No API scope or audit value changes. | Focused and full regression contracts exit 0. | CANDIDATE — visual/data-minimization change only. |
| Account Register hierarchy | The current QBO session is expired, so no fresh Register behavior is claimed. The user's established requirement is a concise Accounting page with exact full-page Back. Local source inspection found Account Register repeated `Selected account → POSTED entries → Retained source IDs` immediately above cards and a table that already conveyed those facts, while its parent filters occupied a permanent text bar. | Removes the duplicate reading rail and shortens the subtitle. The exact COA search/status/currency/page return context remains available under `Chart of Accounts filters retained`; Back, focus restoration, account/period/currency scope, filters, pagination, POSTED rows and retained source IDs remain unchanged. | `npm run test:authoritative-coa-register` exit 0; `npm run test:authoritative-full-shell` exit 0; `npm run test:authoritative-visual-parity` exit 0; `npm run build` exit 0; `npm test` exit 0 (visual verifier 64/64); `git diff --check` exit 0. | ADDITIVE CANDIDATE — presentation-only; QBO Register and authenticated runtime remain unverified. |
| Journal detail scope row | QBO remains at the `TIMER_EXPIRED` logout URL, so no fresh Journal-detail equivalence is claimed. The user's established layout requirement is that one snapshot's peer metrics stay on one desktop row. Local source inspection found exactly five Journal scope facts rendered in a fixed four-column grid, orphaning `Journal date` on a second row at desktop widths. | Changes only the desktop Journal scope grid to five equal columns so entity, period, currency, revision and Journal date remain together. The existing two-column tablet and one-column phone fallbacks, full-page Back, scope mismatch block, line evidence, identifiers, permissions and audit behavior are unchanged. | `npm run test:authoritative-journals` exit 0; `npm run test:authoritative-full-shell` exit 0; `npm run test:authoritative-visual-parity` exit 0; `npm run test:navigation-a11y` exit 0; `npm run build` exit 0; `npm test` exit 0 (visual verifier 64/64); `git diff --check` exit 0. | ADDITIVE CANDIDATE — responsive presentation fix only; authenticated Journal detail and current QBO equivalence remain unverified. |
| Register / accounting-table mobile containment | QBO remains at the `TIMER_EXPIRED` logout URL, so no fresh responsive equivalence is claimed. Local source inspection found the four Register scope facts forced into four vertical cards at phone widths, while a later generic mobile rule removed the height limit from Register, COA and Journal tables. This made evidence pages unnecessarily long despite retained 25/50/100-row pagination. | At phone widths the four Register scope facts now form a compact horizontal, scrollable strip with consistent low-profile cards. Register, COA and Journal tables retain a 60vh internal scroller after the generic mobile rule, while sticky headers, row pagination, horizontal table access, full-page detail and exact Back context remain unchanged. | `npm run test:authoritative-coa-register` exit 0; `npm run test:authoritative-journals` exit 0; `npm run test:authoritative-full-shell` exit 0; `npm run test:authoritative-visual-parity` exit 0; `npm run build` exit 0; `npm test` exit 0 (visual verifier 64/64); `git diff --check` exit 0. | ADDITIVE CANDIDATE — responsive presentation only; authenticated runtime and current QBO responsive behavior remain unverified. |
| Financial-statement section density | QBO remains at the `TIMER_EXPIRED` logout URL, so no fresh report-table equivalence is claimed. Local source inspection found both the Reports Center preview and full-page financial statement rendered each statement section twice: once as an accessible section group row and again as a repeated column on every account row. | Removes the repeated Section column from those two statement tables and keeps the section group heading with `scope=rowgroup`. Account, debit, credit, balance, detail drill, 12-row Center preview, full-page report, contained scroll and exact Back context remain unchanged. | `npm run test:authoritative-reports` exit 0; `npm run test:authoritative-full-shell` exit 0; `npm run test:authoritative-visual-parity` exit 0; `npm run build` exit 0; `npm test` exit 0 (visual verifier 64/64); `git diff --check` exit 0. | ADDITIVE CANDIDATE — concise presentation only; authenticated runtime and current QBO report equivalence remain unverified. |
| Reports icon-system consistency | QBO remains at the `TIMER_EXPIRED` logout URL, so no fresh external icon mapping is claimed. The user's established requirement is one coherent icon vocabulary across the site. Local design-system audit found Reports category tabs and shortcut cards were text-only while primary/secondary navigation already used one self-authored 24px stroke system. | Report categories, core statements, search matches, Property/Project directory entries and A/R Aging now reuse the existing shared `Icon` component in a single 20px decorative slot. A stable semantic map assigns lines, book, bars, exchange, bank, layers, cycle and calendar glyphs; no letter tile or new icon asset is introduced. Labels, search, categories, report launch, drill, Back and API behavior are unchanged. | `npm run test:authoritative-reports` exit 0; `npm run test:authoritative-full-shell` exit 0; `npm run test:authoritative-visual-parity` exit 0; `npm run test:navigation-a11y` exit 0; `npm run build` exit 0; `npm test` exit 0 (visual verifier 64/64); `git diff --check` exit 0. | ADDITIVE CANDIDATE — visual-system consistency only; authenticated runtime and current QBO icon equivalence remain unverified. |
| Authoritative state-icon semantics | QBO remains at the `TIMER_EXPIRED` logout URL, so no fresh external empty-state mapping is claimed. Local full-shell design audit found every authoritative loading, error, blocked, permission, cleared and empty state inherited the same CSS data-URI document icon even when the state was not a document empty state. This also bypassed the shared icon component. | `StateBlock` now maps each state to an existing shared line icon: cycle for loading, shield for error/blocked/permission, check for cleared and document for a true empty result. All use one 48px token-driven container and the legacy pseudo icon is suppressed only for `StateBlock`; non-authoritative legacy empty states remain untouched. Copy, roles, live-region behavior, retry actions, API reads and fail-closed behavior are unchanged. | `npm run test:authoritative-full-shell` exit 0; `npm run test:authoritative-overview` exit 0; `npm run test:authoritative-read-state` exit 0; `npm run test:authoritative-visual-parity` exit 0; `npm run test:navigation-a11y` exit 0; `npm run build` exit 0; `npm test` exit 0 (visual verifier 64/64); `git diff --check` exit 0. | ADDITIVE CANDIDATE — visual semantics only; authenticated runtime and current QBO state presentation remain unverified. |
| Expenses mobile table containment | QBO remains at the `TIMER_EXPIRED` logout URL, so no fresh mobile equivalence is claimed. Local responsive audit found a generic phone rule removed every table height limit, allowing a 25-row Bills or Vendor credits page to stretch the full document even though both lists already provide paging and horizontal table access. | At phone widths the existing Bills and Vendor credits table regions now retain a 60vh internal scroller with contained overscroll. Sticky headers, columns, 25-row pagination, filters, API reads, full-page evidence and exact Back context are unchanged; no accounting or document action is added. | `npm run test:authoritative-documents` exit 0; `npm run test:payables-responsive` exit 0; `npm run test:authoritative-full-shell` exit 0; `npm run test:authoritative-visual-parity` exit 0; `npm run build` exit 0; `npm test` exit 0 (visual verifier 64/64); `git diff --check` exit 0. | ADDITIVE CANDIDATE — local responsive page-length correction only; authenticated mobile runtime and current QBO equivalence remain unverified. |
| Property-report shortcut copy | QBO remains at the `TIMER_EXPIRED` logout URL, so no fresh external report wording is claimed. Local Reports audit found every Property, Project, Unit, Lot, CWIP, construction-loan, prepaid and budget shortcut repeated implementation terms such as retained dimensions, POSTED ledger movement and immutable snapshots before a report was opened. | The eight shortcuts now use one short business sentence each: review income and expenses, review activity and balances, or compare budget to actual. Report names, search aliases, category placement, authenticated readers, evidence fields, drill-through and Back context are unchanged; the opened reports retain their full authority boundaries. | `npm run test:authoritative-reports` exit 0; `npm run test:authoritative-full-shell` exit 0; `npm run test:authoritative-visual-parity` exit 0; `npm run build` exit 0; `npm test` exit 0 (visual verifier 64/64); `git diff --check` exit 0. | ADDITIVE CANDIDATE — first-screen copy only; authenticated runtime and current QBO equivalence remain unverified. |
| GL / Journal-line mobile containment | Browser discovery confirmed the QBO tab still resolves to `TIMER_EXPIRED`, so no new mobile equivalence is claimed. Local Accounting audit found General Ledger and Journal-line tables kept contained scrolling on desktop but lost it under the generic phone override, allowing long posted-ledger and multi-line Journal evidence to extend the entire page. | At phone widths General Ledger and Journal-line tables now share the same 60vh contained scroller as Register, COA and Journal lists. GL paging, Journal line order and fields, sticky headers, horizontal access, full-page evidence, immutable IDs and exact Back restoration are unchanged. | `npm run test:authoritative-general-ledger` exit 0; `npm run test:authoritative-journals` exit 0; `npm run test:authoritative-coa-register` exit 0; `npm run test:authoritative-visual-parity` exit 0; `npm run build` exit 0; `npm test` exit 0 (visual verifier 64/64); `git diff --check` exit 0. | ADDITIVE CANDIDATE — local responsive page-length correction only; authenticated mobile runtime and current QBO equivalence remain unverified. |

### Accounting gaps still unverified

- Current QBO COA could not be re-opened because the browser URL policy blocked navigation; the block was not bypassed.
- QBO account-type eligibility, actual View register/Run report destinations, populated balances, permissions, audit, selection/batch actions, settings, account CRUD, export/print, and current responsive behavior remain unverified.
- REFS does not infer QBO account type from account name/code. Its existing authoritative Register action remains a read-only retained-ledger drill, not a QBO account-action equivalence claim.

## REFS additive controls — Accrual and Amortization (2026-08-19)

| Surface | Evidence source | REFS change | Verification | Status |
|---|---|---|---|---|
| Accrual / Amortization centers | User explicitly requires retaining REFS advantages while adopting a concise QBO-like shell. Current source audit confirms both centers remain in authoritative navigation and retain authenticated API readers, signed-source evidence, independent review, Draft-only boundaries, standard posting separation and posted rollforward. These are REFS-specific controls, not observed QBO equivalents. | Shortens first-screen descriptions and Refresh/Back labels. Moves repeated control badges and detailed prerequisites into a default-closed `Control requirements` disclosure. Review, Draft, posted rollforward, source hashes, SoD and server capability checks are unchanged. | `npm run test:authoritative-accrual` exit 0; `npm run test:authoritative-amortization` exit 0; `npm run test:authoritative-full-shell` exit 0; `npm run test:authoritative-visual-parity` exit 0; `npm run build` exit 0; `npm test` exit 0 (visual verifier 64/64). | ADDITIVE CANDIDATE — explicitly not QBO equivalence. |
| WBS / AutoRec evidence workspace | User requires REFS-specific WBS/AutoRec strengths to remain while the page becomes shorter and visually closer to the compact QBO hierarchy. Source audit found a six-stage accounting path rendered once at page level and again inside the live observation state. This is REFS-specific evidence, not QBO evidence. | Removes only the always-visible duplicate path, retains the contextual path in Live Pilot, shortens three evidence descriptions, and reduces the source-ID field from four rows to two. AutoRec review, Match Review, G11/GL trace, control reconciliation, signed-contract verification, fail-closed states and every API call remain unchanged. | `npm run test:authoritative-wbs-transition` exit 0; `npm run test:authoritative-full-shell` exit 0; `npm run test:authoritative-visual-parity` exit 0; `npm run build` exit 0; `npm test` exit 0 (visual verifier 64/64); `git diff --check` exit 0. | ADDITIVE CANDIDATE — no QBO-equivalence claim; authenticated runtime not verified. |
| WBS Live Pilot connection summary | The user requires the same API-authoritative controls in a shorter, more consistent shell. Source audit found connection status and source repeated across the header, four boundary chips and six fact cards. No fresh QBO interaction is claimed. | Keeps live status and `READ ONLY` in the header, preserves `UNSIGNED PILOT · NOT ADMITTED · NOT POSTABLE` as one concise boundary line, and reduces six fact cards to four non-duplicated facts. Refresh becomes a concise label. Observation, attestation, exception, controlled-test and all API behavior remain unchanged. | `npm run test:authoritative-wbs-live-pilot-bridge` exit 0; `npm run test:authoritative-wbs-transition` exit 0; `npm run test:authoritative-full-shell` exit 0; `npm run test:authoritative-visual-parity` exit 0; `npm run build` exit 0; `npm test` exit 0 (visual verifier 64/64); `git diff --check` exit 0. | ADDITIVE CANDIDATE — presentation-only; authenticated runtime not verified. |
| AI Audit / AI JE hierarchy | The QBO tab was signed out (`TIMER_EXPIRED`), so no fresh QBO evidence was collected or bypassed. The user's established product requirement is a concise, consistent shell that preserves REFS controls. Source audit found two six-chip authority shelves and long repeated header/workbench descriptions. | Converts both authority shelves into the same compact ASCII text-boundary pattern used by WBS, shortens headings and Refresh labels, and retains every human-maker, assignment, review, Draft, submit, approve, post and auto-post restriction. All API reads and commands remain unchanged. | `npm run test:authoritative-ai-audit` exit 0; `npm run test:authoritative-full-shell` exit 0; `npm run test:authoritative-visual-parity` exit 0; `npm run build` exit 0; `npm test` exit 0 (visual verifier 64/64); `git diff --check` exit 0. | ADDITIVE CANDIDATE — no QBO-equivalence claim; authenticated runtime not verified. |
| AI Audit category navigation | The QBO session remains expired, so this change follows the user's explicit requirement for shorter pages with concise, consistent navigation rather than claiming a newly observed QBO behavior. The local source audit found overview, findings, controller actions and amortization evidence appended into one very long page. | Adds four shared-style page tabs: `Overview`, `Findings`, `Actions`, and `Amortization`. The default view no longer appends all other categories below it. Every existing evidence table, human action, proposal control, API read and authority boundary remains reachable; the tab state is presentation-only and creates no accounting authority. | `npm run test:authoritative-ai-audit` exit 0; `npm run test:authoritative-full-shell` exit 0; `npm run test:authoritative-visual-parity` exit 0; `npm run build` exit 0; `npm test` exit 0 (visual verifier 64/64); `git diff --check` exit 0. | ADDITIVE CANDIDATE — presentation-only; no QBO-equivalence claim and authenticated runtime not verified. |
| Stable navigation icon identity | The QBO session remains expired and no new external icon observation is claimed. The user's established requirement is one coherent icon vocabulary across every page. Source inspection found primary workspace icons assigned by array position, so catalog reordering could silently give a workspace another module's icon. | Replaces position-based primary icon selection with a stable workspace-name map while retaining the existing self-authored line-icon vocabulary, 24px rail geometry, group accents and 18px secondary icons. Reordering or filtering the catalog can no longer change a workspace's semantic icon. | `npm run test:authoritative-navigation-model` exit 0; `npm run test:navigation-a11y` exit 0; `npm run test:authoritative-full-shell` exit 0; `npm run test:authoritative-visual-parity` exit 0; `npm run build` exit 0; `npm test` exit 0 (visual verifier 64/64); `git diff --check` exit 0. | ADDITIVE CANDIDATE — presentation invariant only; no QBO-equivalence claim. |
| Mapping Exceptions icon semantics | QBO redirects to Intuit sign-in, so no fresh external icon mapping is claimed. Local full-catalog audit confirmed all 43 navigation routes have explicit shared icons, but `Mapping Exceptions` reused the completion check used by approvals and rules, visually suggesting a completed state rather than a controlled exception. | `Mapping Exceptions` now uses the existing shared shield glyph. Payables/Receivables remain distinct wallet/inbox glyphs; every navigation icon stays in the same self-authored stroke system and no letter tile or new asset is introduced. Routes, availability, labels, permissions and accounting behavior are unchanged. | `npm run test:authoritative-navigation-model` exit 0; `npm run test:navigation-a11y` exit 0; `npm run test:authoritative-full-shell` exit 0; `npm run test:authoritative-visual-parity` exit 0; `npm run build` exit 0; `git diff --check` exit 0. | ADDITIVE CANDIDATE — local icon semantics only; current QBO icon equivalence and authenticated runtime remain unverified. |
| Payables / Receivables icon distinction | QBO remained signed out, so no new external icon mapping is claimed. The user's established requirement is a consistent icon language without awkward duplicates; local navigation audit found Payables and Receivables used the same wallet glyph despite being separate pages. | Payables retains the wallet glyph and Receivables now uses the existing inbox glyph. Both remain in the same self-authored 24px stroke vocabulary and shared 18px secondary slot; no color tile, letter badge, route, label or function changes. | `npm run test:navigation-a11y` exit 0; `npm run test:authoritative-full-shell` exit 0; `npm run test:authoritative-visual-parity` exit 0; `npm run build` exit 0; `npm test` exit 0 (visual verifier 64/64); `git diff --check` exit 0. | ADDITIVE CANDIDATE — icon identity/readability only; no QBO-equivalence claim and authenticated runtime remains unverified. |
| AP/AR unavailable tab consistency | The QBO session remains expired, so no new external interaction is claimed. The user's screenshot and local source audit showed unavailable AP/AR categories rendered as an unstyled text node inside an otherwise styled tab track. | Keeps unavailable categories discoverable but renders them as compact, muted, non-focusable tabs with correct `role=tab`, `aria-disabled=true`, and shared 32px geometry. It does not enable Vendors, Receipts, or Counterparties and does not change Bills, credits, aging, filters, detail, or Back behavior. | `npm run test:authoritative-documents` exit 0; `npm run test:payables-responsive` exit 0; `npm run test:authoritative-full-shell` exit 0; `npm run test:authoritative-visual-parity` exit 0; `npm run build` exit 0; `npm test` exit 0 (visual verifier 64/64); `git diff --check` exit 0. | ADDITIVE CANDIDATE — presentation and accessibility fix only; unavailable functions remain unavailable. |
| Single-entry workspace navigation | The QBO session remains expired, so no fresh external navigation evidence is claimed. The user explicitly requires a primary workspace with only one child to open that page directly without repeating the child in a secondary menu. Local source inspection confirmed the primary click already selected the first route, but `Journal Entry` and `Reports` still rendered duplicate child rows. | One-page workspaces now open directly from the primary rail and release the unused secondary-panel width on desktop. Multi-page workspaces retain their complete secondary menus. At drawer widths, the brand and reachable Close control remain while the duplicate child landmark stays suppressed. No route, reader, permission or accounting behavior changes. | `npm run test:authoritative-navigation-model` exit 0; `npm run test:navigation-a11y` exit 0; `npm run test:authoritative-full-shell` exit 0; `npm run test:authoritative-visual-parity` exit 0; `npm run build` exit 0; `npm test` exit 0 (visual verifier 64/64); `git diff --check` exit 0. | ADDITIVE CANDIDATE — user-directed interaction fix; no QBO-equivalence claim and authenticated runtime not verified. |
| Compact primary workspace labels | The QBO session remains expired, so no new external label evidence is claimed. The user's established requirement is a concise QBO-like left rail. Local source inspection found first-word shortening made both Accounting Settings and Accounting Operations display as `Accounting`, while `Administration` exceeded the compact rail. | Uses a stable semantic short-label map: `Settings`, `Operations`, and `Admin` are now unique and fit the rail. Full accessible names, icons, routes, secondary labels and accounting functions remain unchanged. | `npm run test:authoritative-full-shell` exit 0; `npm run test:navigation-a11y` exit 0; `npm run test:authoritative-visual-parity` exit 0; `npm run build` exit 0; `npm test` exit 0 (visual verifier 64/64); `git diff --check` exit 0. | ADDITIVE CANDIDATE — presentation clarity only; no QBO-equivalence claim. |
| AP/AR detail return context | QBO is signed out with `TIMER_EXPIRED`, so no new external detail evidence is claimed. The user's explicit requirement is a concise full-page detail with an obvious Back path. Local source inspection found the Bill/Invoice Back row permanently displayed a long pipe-delimited list of search, status, dates, counterparty, account, transaction type and page. | Keeps the immutable return context intact but folds it behind the native `List filters retained` disclosure. Back still restores the exact list state. The adjacent action prohibition is shortened to one clear read-only sentence; no document action is added or enabled. | `npm run test:authoritative-documents` exit 0; `npm run test:payables-responsive` exit 0; `npm run test:authoritative-full-shell` exit 0; `npm run test:authoritative-visual-parity` exit 0; `npm run build` exit 0; `npm test` exit 0 (visual verifier 64/64); `git diff --check` exit 0. | ADDITIVE CANDIDATE — presentation-only; authenticated detail/Back behavior remains unverified. |
| Bill / Invoice detail field density | The QBO tab remains signed out with `TIMER_EXPIRED`, so no fresh external equivalence evidence is claimed. The user's established requirement is a concise accounting page without repeated facts. Local inspection found counterparty, document date, due date, status, original amount and open balance repeated across the detail header, summary and retained-facts table. | Keeps those key facts once in the header and summary, and limits the retained-facts table to unique evidence: entity, currency, offset account, posted journal, period and description. Immutable lineage, blocked state, API data, Back context and all read-only restrictions are unchanged. | `npm run test:authoritative-documents` exit 0; `npm run test:payables-responsive` exit 0; `npm run test:authoritative-full-shell` exit 0; `npm run test:authoritative-visual-parity` exit 0; `npm run build` exit 0; `npm test` exit 0 (visual verifier 64/64); `git diff --check` exit 0. | ADDITIVE CANDIDATE — presentation-only; authenticated detail and current QBO equivalence remain unverified. |
| AP/AR immutable-lineage disclosure | The current QBO page again resolves to the `TIMER_EXPIRED` logout state, so no new QBO lineage presentation is claimed. The user's established requirement is a shorter page with secondary evidence available on demand. Local inspection found complete source, receipt, mapping, posted-journal, audit and ledger lineage always expanded ahead of retained document facts. | Complete same-revision lineage now uses the shared default-closed `Immutable authoritative lineage` disclosure with a `POSTED EVIDENCE` status. Keyboard-scrollable evidence remains intact. Missing or mismatched lineage still renders the always-visible `BLOCKED — authoritative lineage unavailable` warning and is never folded away. | `npm run test:authoritative-documents` exit 0; `npm run test:payables-responsive` exit 0; `npm run test:authoritative-full-shell` exit 0; `npm run test:authoritative-visual-parity` exit 0; `npm run build` exit 0; `npm test` exit 0 (visual verifier 64/64); `git diff --check` exit 0. | ADDITIVE CANDIDATE — presentation-only; authenticated AP/AR detail and QBO equivalence remain unverified. |
| AP/AR Aging narrow-screen page length | QBO remains on the expired logout route, so no new QBO responsive behavior is claimed. Local source audit found the standalone Aging control-total and bucket tables sit outside Reports Center and therefore lost their height limit at the shared 768px reset despite already using a dedicated table class. | Both control and aging-bucket tables now retain a 60vh keyboard-scrollable region through tablet and phone widths. Entity, accounting period, as-of date, currency rows, bucket values, full-page evidence, refresh GET and exact Back context are unchanged; an empty scoped response is still never treated as zero. | `npm run test:authoritative-aging` exit 0; `npm run test:authoritative-documents` exit 0; `npm run test:authoritative-full-shell` exit 0; `npm run test:authoritative-visual-parity` exit 0; `npm run build` exit 0; `npm test` exit 0 (visual verifier 64/64); `git diff --check` exit 0. | ADDITIVE CANDIDATE — local responsive page-length correction only; authenticated Aging runtime and current QBO equivalence remain unverified. |
| AP/AR adjustment detail hierarchy | QBO remains at the `TIMER_EXPIRED` logout URL, so this round produced no fresh external adjustment evidence. The user's established concise-page requirement and local inspection showed adjustment status, scope, amount and audit fields presented as one undifferentiated six-row table. | Uses the same four-fact summary hierarchy as Bill/Invoice for amount, period, currency and revision. The remaining unique audit fields—entity, document, source adjustment, Journal, created timestamp and reason—remain complete inside a default-closed `Adjustment evidence fields` disclosure. Header status, full-page Back, immutable lineage and read-only boundaries are unchanged. | `npm run test:authoritative-documents` exit 0; `npm run test:payables-responsive` exit 0; `npm run test:authoritative-full-shell` exit 0; `npm run test:authoritative-visual-parity` exit 0; `npm run build` exit 0; `npm test` exit 0 (visual verifier 64/64); `git diff --check` exit 0. | ADDITIVE CANDIDATE — presentation-only; populated adjustments, authenticated runtime and QBO equivalence remain unverified. |
| Expenses type-tab content isolation | QBO still resolves to `TIMER_EXPIRED`, so no new external tab behavior is claimed. Local interaction inspection found an explicit `Vendor credits` scope first rendered an unrelated Bill empty-state card, while an explicit `Bills` scope appended an unrelated adjustment empty-state card below the requested list. This made the selected view appear unresponsive and lengthened the page. | Explicit `Bills` and `Vendor credits` views now render only their corresponding retained list. `All retained transactions` continues to expose both API result classes, so no function or evidence is removed. Filters, result counts, AP Aging, unavailable Vendors, full-page evidence and Back state are unchanged. | `npm run test:authoritative-documents` exit 0; `npm run test:payables-responsive` exit 0; `npm run test:authoritative-full-shell` exit 0; `npm run test:authoritative-visual-parity` exit 0; `npm run build` exit 0; `npm test` exit 0 (visual verifier 64/64); `git diff --check` exit 0. | ADDITIVE CANDIDATE — user-flow correction; authenticated runtime and current QBO equivalence remain unverified. |
| Primary navigation state semantics | The current QBO session remains on the `TIMER_EXPIRED` logout route, so no new QBO behavior is claimed. Local production-shell audit found every active primary rail control announced as the current page, including multi-page workspaces that instead expose a secondary page panel. | Multi-page rail groups now announce expanded/collapsed state and bind the active group to the single rendered secondary panel. One-page Reports and Journal remain direct current-page controls with no child menu. Visible labels, icons, routes, default first-page selection, drawer close behavior and page availability are unchanged. | `npm run test:authoritative-full-shell` exit 0; `npm run test:navigation-a11y` exit 0; `npm run test:authoritative-visual-parity` exit 0; `npm run build` exit 0; `npm test` exit 0 (visual verifier 64/64); `git diff --check` exit 0. | ADDITIVE CANDIDATE — navigation semantics/accessibility correction only; authenticated runtime and QBO equivalence remain unverified. |
| WBS Live Pilot narrow-screen containment | QBO remains on the expired logout route; Live Pilot observation is a REFS-specific control rather than an observed QBO equivalent. Local source audit found its bounded observation table already had dedicated wide-column geometry but lost its height limit at the shared 768px reset, so a populated observation could stretch the whole page. | The existing read-only observation table now retains a 60vh keyboard-scrollable region through tablet and phone widths. Tool scope, immutable rows, `NOT_ADMITTED` boundary, empty state, Refresh reads and every TEST ONLY or retained-evidence action remain unchanged; no Provider call or accounting command is added. | `npm run test:authoritative-wbs-live-pilot-bridge` exit 0; `npm run test:authoritative-full-shell` exit 0; `npm run test:authoritative-visual-parity` exit 0; `npm run build` exit 0; `npm test` exit 0 (visual verifier 64/64); `git diff --check` exit 0. | ADDITIVE CANDIDATE — REFS-specific responsive correction only; authenticated Live Pilot runtime and QBO equivalence remain unverified. |
| Signed Bank statements narrow-screen containment | QBO remains on the expired logout route, so no new QBO responsive behavior is claimed. Local Banking audit found the eight-column signed/admitted statement list already used dedicated 900px geometry and horizontal access, but the shared 768px reset removed its height limit and could make a populated evidence list stretch the whole page. | The existing signed-statement list now retains a 60vh keyboard-scrollable region through tablet and phone widths. Admission state, balances, transaction counts, signature evidence, full-page review, controlled lifecycle and every server permission remain unchanged; no reconciliation or accounting command is added. | `npm run test:authoritative-bank` exit 0; `npm run test:authoritative-bank-responsive` exit 0; `npm run test:authoritative-full-shell` exit 0; `npm run test:authoritative-visual-parity` exit 0; `npm run build` exit 0; `npm test` exit 0 (visual verifier 64/64); `git diff --check` exit 0. | ADDITIVE CANDIDATE — local responsive page-length correction only; authenticated signed-statement runtime and current QBO equivalence remain unverified. |
| Retained WBS exception lists narrow-screen containment | QBO remains on the expired logout route; retained WBS exception evidence is a REFS-specific control rather than an observed QBO equivalent. Local source audit found the retained exception-batch and exception-row lists used generic table regions, so both lost their height limit at the shared 768px reset and could make a populated review page excessively long. | Both existing read-only retained-evidence lists now use one stable, 60vh keyboard-scrollable region through tablet and phone widths. Batch scope, immutable row facts, signed-link readiness, next owner, detail drill and all no-authority boundaries remain unchanged; no WBS retrieval, retention or accounting action is added. | `npm run test:authoritative-wbs-live-pilot-bridge` exit 0; `npm run test:authoritative-full-shell` exit 0; `npm run test:authoritative-visual-parity` exit 0; `npm run build` exit 0; `npm test` exit 0 (visual verifier 64/64); `git diff --check` exit 0. | ADDITIVE CANDIDATE — REFS-specific responsive correction only; authenticated retained-evidence runtime and QBO equivalence remain unverified. |
| Bank queue / reconciliation evidence narrow-screen containment | QBO remains on the expired logout route, so no fresh responsive equivalence is claimed. Local Banking audit found the transaction queue, authorized worksheet and posted-adjustment clearance lists all used generic table regions; the shared 768px reset therefore removed their height limits and could make a populated controller page excessively long. | The three existing API-backed table regions now share a stable 60vh keyboard-scrollable container through tablet and phone widths. Transactions, Match evidence, clearance state, controller reason, Draft preparation, posted-adjustment evidence, exact Back and all server permissions are unchanged; no action is added or executed. | `npm run test:authoritative-bank` exit 0; `npm run test:authoritative-bank-responsive` exit 0; `npm run test:authoritative-full-shell` exit 0; `npm run test:authoritative-visual-parity` exit 0; `npm run build` exit 0; `npm test` exit 0 (visual verifier 64/64); `git diff --check` exit 0. | ADDITIVE CANDIDATE — local responsive page-length correction only; authenticated Banking/Reconcile runtime and current QBO equivalence remain unverified. |
| Journal lineage narrow-screen containment | QBO remains on the expired logout route, so no fresh responsive equivalence is claimed. Local Accounting audit found the independent Journal lineage page used a generic six-column table region; the shared 768px reset removed its height limit and could make a multi-line Journal drill stretch the whole page. | The existing read-only Journal-line evidence now uses a stable 60vh keyboard-scrollable region through tablet and phone widths. Line order, account, debit, credit, GL/Source drill, immutable identifiers, exact Back and all no-edit/no-workflow/no-post/no-export boundaries remain unchanged. | `npm run test:authoritative-lineage-drill` exit 0; `npm run test:authoritative-journals` exit 0; `npm run test:authoritative-full-shell` exit 0; `npm run test:authoritative-visual-parity` exit 0; `npm run build` exit 0; `npm test` exit 0 (visual verifier 64/64); `git diff --check` exit 0. | ADDITIVE CANDIDATE — local responsive page-length correction only; authenticated Journal drill and current QBO equivalence remain unverified. |
| Admitted WBS Payables review queue containment | QBO remains on the expired logout route; signed WBS Payables review is a REFS-specific control rather than an observed QBO equivalent. Local source audit found the seven-column admitted review queue used a generic table region even though the adjacent reviewed-evidence queue already had a dedicated 60vh Payables container. | The admitted review queue now reuses the same stable keyboard-scrollable Payables region through tablet and phone widths. Signed admission, document/date/vendor/amount facts, attachment evidence, reviewer role, review command and strict no-Draft/no-submit/no-approve/no-post boundary remain unchanged. | `npm run test:authoritative-wbs-payable` exit 0; `npm run test:authoritative-wbs-transition` exit 0; `npm run test:authoritative-full-shell` exit 0; `npm run test:authoritative-visual-parity` exit 0; `npm run build` exit 0; `npm test` exit 0 (visual verifier 64/64); `git diff --check` exit 0. | ADDITIVE CANDIDATE — REFS-specific responsive consistency only; authenticated WBS Payables review and QBO equivalence remain unverified. |
| AutoRec G11 evidence narrow-screen containment | QBO remains on the expired logout route; G11 journal/ledger proof is a REFS-specific control rather than an observed QBO equivalent. Local source audit found the eight-column posted G11 line list used a generic table region, so the shared 768px reset removed its height limit and could make a populated dual-leg evidence drill excessively long. | The existing G11 line evidence now uses a stable 60vh keyboard-scrollable region through tablet and phone widths. Event/role, both posted Journals, account/member, debit/credit, ledger IDs, match review and 291001 control facts remain unchanged; no AutoRec, Journal or ledger action is added. | `npm run test:authoritative-wbs-transition` exit 0; `npm run test:authoritative-wbs-payable` exit 0; `npm run test:authoritative-full-shell` exit 0; `npm run test:authoritative-visual-parity` exit 0; `npm run build` exit 0; `npm test` exit 0 (visual verifier 64/64); `git diff --check` exit 0. | ADDITIVE CANDIDATE — REFS-specific responsive correction only; authenticated G11 runtime and QBO equivalence remain unverified. |
| WBS / AutoRec read-only status vocabulary | QBO remains on the expired logout route; AutoRec review, G11 and WBS/REFS control reconciliation are REFS-specific controls, so no QBO label equivalence is claimed. Local rendered audit found these three secondary readers still exposed the HTTP-oriented label `GET ONLY`, unlike the shared `READ ONLY` vocabulary already used by Expenses, Accounting, Reports, Bank and Live Pilot. | All three default-closed evidence readers now use the same concise `READ ONLY` status. Their GET requests, exact-ID inputs, loaded fields, blocked/empty states, G11 evidence, control totals and every no-write boundary remain unchanged. | `npm run test:authoritative-wbs-transition` exit 0; `npm run test:authoritative-wbs-payable` exit 0; `npm run test:authoritative-full-shell` exit 0; `npm run test:authoritative-visual-parity` exit 0; `npm run build` exit 0; `npm test` exit 0 (visual verifier 64/64); `git diff --check` exit 0. | ADDITIVE CANDIDATE — visible vocabulary consistency only; authenticated WBS/AutoRec runtime and QBO equivalence remain unverified. |
| Dimension-report evidence scope vocabulary | QBO remains on the expired logout route, so no fresh report-detail wording is claimed. Local full-site copy scan found the Property/Project/Unit/Lot profitability detail was the final customer-visible surface still showing an implementation badge (`API GET`) and a transport-oriented scope sentence while the rest of Reports used `READ ONLY`. | The same evidence card now says `Retained entity, period, dimension, and classification.` and uses the shared `READ ONLY` badge. Dimension type/reference, classification basis, period, GL/Journal/source drill, exact Back and the underlying GET reader remain unchanged. | `npm run test:authoritative-reports` exit 0; `npm run test:authoritative-full-shell` exit 0; `npm run test:authoritative-visual-parity` exit 0; `npm run build` exit 0; `npm test` exit 0 (visual verifier 64/64); `git diff --check` exit 0. | ADDITIVE CANDIDATE — Reports readability consistency only; authenticated dimension detail and current QBO equivalence remain unverified. |
| Journal detail read-only policy presentation | QBO remains on the expired logout route, so no fresh Journal-detail policy wording is claimed. Local Accounting review found the normal read-only boundary rendered as a permanent `blocked` state card with an empty-state illustration, even when the Journal scope was valid. | Valid Journal detail now uses the shared compact `READ ONLY` note: `No editing, workflow, posting, reversing, export, or inferred source links.` The true entity/period/Journal/revision/currency mismatch still renders the existing BLOCKED state. Lines, status, scope, workflow capabilities, lineage and Back are unchanged. | `npm run test:authoritative-journals` exit 0; `npm run test:authoritative-lineage-drill` exit 0; `npm run test:authoritative-full-shell` exit 0; `npm run test:authoritative-visual-parity` exit 0; `npm run build` exit 0; `npm test` exit 0 (visual verifier 64/64); `git diff --check` exit 0. | ADDITIVE CANDIDATE — local icon/semantic hierarchy correction only; authenticated Journal runtime and QBO equivalence remain unverified. |
| General Ledger optional-lineage presentation | QBO remains on the expired logout route, so no fresh GL drill presentation is claimed. Local Accounting review found the absence of an optional Journal/Source continuation rendered as a full empty-state illustration below an otherwise complete posted-line detail. | The completed GL detail now uses a compact `READ ONLY` note: `Journal and source drill-through are available only when an exact link is returned.` Amounts, Journal and ledger identifiers, exact Back context and the no-inference boundary remain unchanged; no link is created or guessed. | `npm run test:authoritative-general-ledger` exit 0 (4/4); `npm run test:authoritative-coa-register` exit 0; `npm run test:authoritative-full-shell` exit 0; `npm run test:authoritative-visual-parity` exit 0; `npm run build` exit 0; `npm test` exit 0 (visual verifier 64/64); `git diff --check` exit 0. | ADDITIVE CANDIDATE — local icon/semantic hierarchy correction only; authenticated GL runtime and QBO equivalence remain unverified. |
| Statement of cash flows first-screen density | QBO remains on the expired logout route, so no fresh cash-flow page wording is claimed. Local Reports review found two permanent technical mapping/no-inference paragraphs and a transport-oriented `Load mapped cash-flow evidence` control above the statement. | The first screen now says `Review mapped operating, investing, and financing cash activity.` and uses `Load cash flow`. The complete mapping and no-inference rules remain available in a default-closed `Classification rules` disclosure. Mapping validation, BLOCKED states, totals, rows, lineage, permissions and API reads are unchanged. | `npm run test:authoritative-reports` exit 0; `npm run test:authoritative-full-shell` exit 0; `npm run test:authoritative-visual-parity` exit 0; `npm run test:navigation-a11y` exit 0; `npm run build` exit 0; `npm test` exit 0 (visual verifier 64/64); `git diff --check` exit 0. | ADDITIVE CANDIDATE — local Reports density/copy correction only; authenticated cash-flow runtime and QBO equivalence remain unverified. |
| Full financial-report page length and nested Back | Earlier QBO report evidence established full-page report drill and explicit Back behavior. QBO is currently logged out, so no fresh row-count or pagination control claim is made. Local responsive audit found REFS still rendered every returned statement row on one page despite the product requirement to avoid excessively long pages. | Trial Balance, Balance Sheet, Income Statement and direct cash evidence now render 25 authoritative rows per presentation page with an explicit range, Previous and Next. A report row drill freezes the exact report page in its return context; Back restores the same page before restoring table position and focus. The API response, totals, row identities, permissions and lineage rules are unchanged. | `npm run test:authoritative-reports` exit 0; `npm run test:authoritative-full-shell` exit 0; `npm run test:authoritative-visual-parity` exit 0; `npm run test:navigation-a11y` exit 0; `npm run build` exit 0; `npm test` exit 0 (visual verifier 64/64); `git diff --check` exit 0. | ADDITIVE CANDIDATE — local bounded-page and Back-context correction only; authenticated large-report runtime and current QBO pagination equivalence remain unverified. |
| General Ledger list heading hierarchy | QBO is currently logged out, so no fresh General Ledger heading text is claimed. Local Accounting design-system review found the list page eyebrow repeated both the page title and the `POSTED EVIDENCE` implementation boundary immediately above `General Ledger` and `Review posted ledger activity.` | The list page now uses the shared section eyebrow `ACCOUNTING`, followed by the unchanged `General Ledger` title and concise business subtitle. The existing `READ ONLY` badge, scoped GET, filters, pagination, POSTED-only data contract, detail drill and exact Back behavior are unchanged. | `npm run test:authoritative-general-ledger` exit 0 (4/4); `npm run test:authoritative-full-shell` exit 0; `npm run test:authoritative-visual-parity` exit 0; `npm run build` exit 0; `git diff --check` exit 0. | ADDITIVE CANDIDATE — local heading hierarchy correction only; authenticated General Ledger runtime and current QBO heading equivalence remain unverified. |
| Direct cash report naming | QBO is currently logged out, so no fresh report label is claimed. Local Reports UX-copy review found the authoritative direct-cash reader exposed the implementation-oriented label `Cash movement evidence` in the core report list even though the page already explains that it is cash-account activity and not a classified statement of cash flows. | The core report is now named `Cash activity`, with `Review cash-account movement.` as its shortcut description and `Cash activity summary` as its accessible summary name. The classification boundary remains visible and still forbids inference of operating, investing, or financing activity. API rows, totals, lineage, report drill and Back are unchanged. | `npm run test:authoritative-reports` exit 0; `npm run test:authoritative-full-shell` exit 0; `npm run test:authoritative-visual-parity` exit 0; `npm run build` exit 0; `git diff --check` exit 0. | ADDITIVE CANDIDATE — local naming/copy correction only; authenticated direct-cash runtime and current QBO wording equivalence remain unverified. |
| Accounts Receivable first-screen wording | QBO is currently logged out, so no fresh Accounts Receivable wording is claimed. Local UX-copy review found the page subtitle promised receipts even though the Receipts tab is explicitly unavailable, and exposed the implementation phrase `from the accounting API`. | The subtitle now says `Review invoices and AR aging.` The unavailable Receipts tab remains visible and disabled, while invoices, AR aging, filters, API-backed facts, full-page detail and exact Back behavior remain unchanged. | `npm run test:authoritative-documents` exit 0; `npm run test:authoritative-full-shell` exit 0; `npm run test:authoritative-visual-parity` exit 0; `npm run build` exit 0; `git diff --check` exit 0. | ADDITIVE CANDIDATE — local wording accuracy only; authenticated Accounts Receivable runtime and current QBO wording equivalence remain unverified. |
| Reports scope status wording | QBO is currently logged out, so no fresh Reports scope-chip wording is claimed. Local Reports copy audit found the compact entity/period chip still exposed the implementation-oriented phrase `POSTED ledger evidence` while the surrounding page already explains the report boundary. | The scope chip now uses the concise status `Posted only`. Entity, period, posted-data restriction, report categories, search, statement readers, lineage and exact Back behavior remain unchanged. | `npm run test:authoritative-reports` exit 0; `npm run test:authoritative-full-shell` exit 0; `npm run test:authoritative-visual-parity` exit 0; `npm run build` exit 0; `git diff --check` exit 0. | ADDITIVE CANDIDATE — local wording consistency only; authenticated Reports runtime and current QBO wording equivalence remain unverified. |
| Accounts Receivable summary vocabulary | A read-only attempt to open QBO Reports redirected to the Intuit sign-in page, so no fresh QBO AR wording is claimed. Local authoritative-shell audit found the retained AR summary still exposed `API total`, `Filtered`, and `All retained API rows` on its first screen. | The existing four counts now use `All records` and `After filters`, and the default filter scope says `All records`. Counts, invoices, adjustments, filters, unavailable Receipts, AR Aging, detail and exact Back behavior remain unchanged. | `npm run test:authoritative-documents` exit 0; `npm run test:authoritative-full-shell` exit 0; `npm run test:authoritative-visual-parity` exit 0; `npm run build` exit 0; `git diff --check` exit 0. | ADDITIVE CANDIDATE — local wording/readability correction only; authenticated Accounts Receivable runtime and current QBO equivalence remain unverified. |
| Accounts Receivable filter and empty-state wording | QBO remains on the Intuit sign-in route, so no fresh QBO AR empty-state wording is claimed. Local authoritative-shell audit found the customer option and filtered-empty states still exposed `retained`, `presentation filters`, `retained list facts`, and `authoritative adjustments`. | The customer option now says `All customers`; invoice and adjustment empty states use concise `match these filters` / `No adjustments found` language with an actionable reset suggestion. The zero-balance caveat, filters, counts, invoice/adjustment readers, AR Aging and Back behavior remain unchanged. | `npm run test:authoritative-documents` exit 0; `npm run test:authoritative-full-shell` exit 0; `npm run test:authoritative-visual-parity` exit 0; `npm run build` exit 0; `git diff --check` exit 0. | ADDITIVE CANDIDATE — local empty-state readability correction only; authenticated Accounts Receivable runtime and current QBO equivalence remain unverified. |
| Journal filtered-empty wording | QBO remains on the Intuit sign-in route, so no fresh QBO Journal empty-state wording is claimed. Local Accounting copy audit found the no-match state still exposed `presentation filters`, `retained list facts`, and `local no-match`. | The state now says `No journal entries match these filters` and suggests changing or resetting filters, while retaining the warning that the result does not confirm zero ledger activity. Queue counts, status/date/search filters, pagination, Journal detail, workflow permissions and exact Back remain unchanged. | `npm run test:authoritative-journals` exit 0; `npm run test:authoritative-full-shell` exit 0; `npm run test:authoritative-visual-parity` exit 0; `npm run build` exit 0; `git diff --check` exit 0. | ADDITIVE CANDIDATE — local empty-state readability correction only; authenticated Journal runtime and current QBO equivalence remain unverified. |
| Chart of Accounts / Register table vocabulary | QBO remains on the Intuit sign-in route, so no fresh QBO table-label claim is made. Local Accounting table audit found the COA action column and Register trace column still used implementation-oriented `Evidence`, `Retained source IDs`, `No retained source ID`, and `No posted currency evidence` labels. | COA now labels the action column `Details`; Register labels retained IDs as `Source documents`; missing source/currency values use `None` / `Not available`. All source IDs, currencies, balances, filters, pagination, Register drill and exact Back behavior remain unchanged. | `npm run test:authoritative-coa-register` exit 0; `npm run test:authoritative-full-shell` exit 0; `npm run test:authoritative-visual-parity` exit 0; `npm run build` exit 0; `git diff --check` exit 0. | ADDITIVE CANDIDATE — local table readability correction only; authenticated COA/Register runtime and current QBO equivalence remain unverified. |
| Dimension profitability detail action | QBO remains on the Intuit sign-in route, so no fresh QBO profitability-table wording is claimed. Local Reports design-system audit found Property/Project/Unit/Lot profitability still used `Evidence` / `Open evidence`, unlike the `Details` / `View details` action used by core financial reports, GL and COA. | The profitability table now uses `Details` and `View details`. Section, account, debit, credit, balance, exact dimension scope, lineage drill and Back behavior remain unchanged. | `npm run test:authoritative-reports` exit 0; `npm run test:authoritative-full-shell` exit 0; `npm run test:authoritative-visual-parity` exit 0; `npm run build` exit 0; `git diff --check` exit 0. | ADDITIVE CANDIDATE — local action-label consistency only; authenticated profitability runtime and current QBO equivalence remain unverified. |
| Reports workbench detail-action consistency | QBO remains on the Intuit sign-in route, so no fresh QBO workbench-label claim is made. Local design-system audit found cash-flow classification, period comparison, CWIP, construction-loan, prepaid, intercompany, budget and consolidation tables still used `Evidence` / `Open evidence`; period comparison also used the longer `Evidence status`. | These eight existing read-only tables now use `Details` / `View details`, and period comparison uses `Status`. All mappings, amounts, status values, snapshots, exact lineage drills, permissions and Back behavior remain unchanged. | Source scan for the retired table/action labels returned no matches; `npm run test:authoritative-reports` exit 0; `npm run test:authoritative-full-shell` exit 0; `npm run test:authoritative-visual-parity` exit 0; `npm run build` exit 0; `git diff --check` exit 0. | ADDITIVE CANDIDATE — local cross-report action-label consistency only; authenticated workbench runtime and current QBO equivalence remain unverified. |
| WBS Payables table-action consistency | QBO remains on the Intuit sign-in route; signed WBS Payables are a REFS-specific control rather than an observed QBO equivalent. Local design-system audit found the reviewed queue used `Action / Open evidence`, while the admitted queue used `Evidence / Inspect review evidence`, creating two table vocabularies for adjacent workflows. | The reviewed queue now uses `Action / View details`; the admitted queue uses the same `Action` header while retaining the distinct `Inspect review evidence` and `Add support evidence` controls. Attachment, reviewer, Draft, permission and no-submit/no-approve/no-post boundaries remain unchanged. | `npm run test:authoritative-wbs-payable` exit 0; `npm run test:authoritative-full-shell` exit 0; `npm run test:authoritative-visual-parity` exit 0; `npm run build` exit 0; `git diff --check` exit 0. | ADDITIVE CANDIDATE — local WBS Payables vocabulary consistency only; authenticated runtime and QBO equivalence remain unverified. |
| Accounts Receivable list/action consistency | A read-only attempt to open QBO Reports redirected to the Intuit sign-in page, so no fresh QBO AR wording is claimed. Local design-system scan found shared AP/AR tables used concise counts and `Details / View details` for AP, but exposed API/storage copy and `Evidence / Open evidence` for AR invoices and adjustments. | AR invoices and adjustments now use concise item counts plus the shared `Details / View details` action. Document facts, adjustment facts, filters, AR Aging, full-page detail, lineage, exact Back and all read-only boundaries remain unchanged. | `npm run test:authoritative-documents` exit 0; `npm run test:authoritative-full-shell` exit 0; `npm run test:authoritative-visual-parity` exit 0; `npm run build` exit 0; `git diff --check` exit 0. | ADDITIVE CANDIDATE — local AP/AR design-language consistency only; authenticated AR runtime and current QBO equivalence remain unverified. |
| Accounting / Reports lineage navigation copy | QBO remains on the Intuit sign-in route, so no fresh QBO drill wording is claimed. Local cross-page copy audit found normal lineage navigation exposed `exact GET`, `API-returned`, and `retained ledger line` implementation terms. | Loading, report selection, ledger selection and report-row guidance now use concise linked-record language while preserving the explicit no-inference rule. Exact entity/period/Journal/GL/Source validation, blocked state, buttons and Back behavior remain unchanged. | `npm run test:authoritative-lineage-drill` exit 0; `npm run test:authoritative-full-shell` exit 0; `npm run test:authoritative-visual-parity` exit 0; `npm run build` exit 0; `git diff --check` exit 0. | ADDITIVE CANDIDATE — local lineage readability correction only; authenticated lineage runtime and current QBO equivalence remain unverified. |
| Shared AP/AR compact filters | A read-only QBO Expenses attempt redirected to the Intuit sign-in page, so no fresh QBO filter-layout claim is made. Local responsive audit found Expenses already collapsed date/vendor/category filters, while Receivables permanently displayed date/customer fields and the reusable pattern still carried Expenses-only class names. Verification also found a restored AP-only Category value could incorrectly mark AR secondary filters active even though it never filtered AR rows. | AP and AR now share one compact Search/Status/More filters/Reset/result row. Date and counterparty fields stay collapsed by default, active secondary filters automatically remain visible, and AP alone displays and counts Category. All valid filter values, counts, pagination, API rows, empty states and details remain unchanged. | `npm run test:authoritative-documents` exit 0; `npm run test:payables-responsive` exit 0; `npm run test:authoritative-full-shell` exit 0; `npm run test:authoritative-visual-parity` exit 0; `npm run test:authoritative-browser-acceptance-preflight` exit 0; `npm run pretest` exit 0; `npm run build` exit 0; `git diff --check` exit 0. | ADDITIVE CANDIDATE — local AP/AR responsive consistency and stale-scope presentation correction only; authenticated runtime and current QBO equivalence remain unverified. |
| Account Register scope-strip density | A read-only QBO Chart of Accounts attempt redirected to the Intuit sign-in page, so no fresh QBO Register layout claim is made. Local Accounting audit found Register still rendered Account, Company/period, Currency and Posted entries as four 94px cards with unrelated colored top borders on desktop, even though narrow layouts already treated them as one compact strip. | Register scope is now one bordered, horizontally scrollable fact strip at every width, with lightweight dividers and no per-card color coding or shadow. All four facts, account/period/currency scope, search, pagination, table, source IDs and Back behavior remain unchanged; the separate COA surface is untouched. | `npm run test:authoritative-coa-register` exit 0; `npm run test:authoritative-full-shell` exit 0; `npm run test:authoritative-visual-parity` exit 0; `npm run build` exit 0; `git diff --check` exit 0. | ADDITIVE CANDIDATE — local Accounting density and design-language correction only; authenticated Register runtime and current QBO equivalence remain unverified. |
| Reports workbench mobile table containment | QBO remains on the Intuit sign-in route, so no fresh QBO responsive-table claim is made. Local source/CSS audit found the phone breakpoint removed the generic table height limit; six populated report workbenches—statement snapshot, cash activity, period comparison, intercompany, budget and consolidation—still used that generic class and could create very long pages. | All six tables now share a dedicated keyboard-scrollable workbench region capped at 60vh on tablet/high-zoom and phone widths. Columns, rows, status, snapshot identifiers, detail actions, lineage and exact Back behavior remain unchanged. | `npm run test:authoritative-reports` exit 0; `npm run test:authoritative-full-shell` exit 0; `npm run test:authoritative-visual-parity` exit 0; `npm run build` exit 0; `git diff --check` exit 0. | ADDITIVE CANDIDATE — local responsive page-length correction only; authenticated report workbenches and current QBO equivalence remain unverified. |
| Property and profitability report containment | A read-only QBO Reports attempt redirected to the Intuit sign-in page, so no fresh QBO row-count or responsive behavior is claimed. Local source/CSS audit found CWIP, construction-loan, prepaid/amortization and dimension-profitability tables had dedicated horizontal geometry but lost the shared height boundary at the tablet/high-zoom reset, allowing populated results to stretch the whole page. | These existing report tables now retain a 60vh keyboard-scrollable region through tablet, high-zoom and phone widths. Every returned row, amount, mapping state, dimension, detail action, lineage rule and exact Back context remains unchanged; no client-side row truncation or accounting action is introduced. | `npm run test:authoritative-reports` exit 0; `npm run test:authoritative-full-shell` exit 0; `npm run test:authoritative-visual-parity` exit 0; `npm run test:navigation-a11y` exit 0; `npm run build` exit 0; `npm test` exit 0 (visual verifier 64/64); `git diff --check` exit 0. | ADDITIVE CANDIDATE — local responsive page-length correction only; authenticated populated reports and current QBO equivalence remain unverified. |
| Cash-and-capital report first-screen copy | QBO remains on the Intuit sign-in route, so no fresh QBO wording claim is made. Local UX-copy audit found CWIP, construction-loan and prepaid cards permanently displayed two long paragraphs of mapping, inference and zero-balance rules before any result, while their load controls exposed `evidence` transport terminology. | Each card now leads with one business-purpose sentence and a concise load action. The complete mapping, debit/credit interpretation and no-zero-inference rules remain available in a default-closed `Report rules` disclosure. Loading and empty states use plain language while still stating that no result does not confirm a zero balance. API reads, BLOCKED states, rows, amounts, mappings, detail drills and Back behavior are unchanged. | `npm run test:authoritative-reports` exit 0; `npm run test:authoritative-full-shell` exit 0; `npm run test:authoritative-visual-parity` exit 0; `npm run build` exit 0; `git diff --check` exit 0. | ADDITIVE CANDIDATE — local Reports copy/progressive-disclosure correction only; authenticated populated reports and current QBO equivalence remain unverified. |
| Prior-period comparison first-screen copy | QBO remains on the Intuit sign-in route, so no fresh QBO wording claim is made. Local UX-copy audit found the comparison card exposed UUID, API, retained-evidence and POSTED implementation terms across two permanent paragraphs before a result. | The card now asks for an `Earlier period ID`, uses `Compare periods`, and explains the business purpose in one sentence. The exact non-overlap, different-period and missing-is-not-zero rules remain in a default-closed `Comparison rules` disclosure. Empty and loading states use plain language; the same period guard, API reader, amounts, statuses, detail drill and Back context are unchanged. | `npm run test:authoritative-reports` exit 0; `npm run test:authoritative-full-shell` exit 0; `npm run test:authoritative-visual-parity` exit 0; `npm run build` exit 0; `git diff --check` exit 0. | ADDITIVE CANDIDATE — local Reports copy/progressive-disclosure correction only; authenticated comparison results and current QBO equivalence remain unverified. |

### Additive-control gaps still unverified

- Authenticated Render/OIDC/PostgreSQL behavior, populated queues, permitted controller commands and responsive full-page detail were not exercised in this local UI round.
- No QBO claim is made for AI accrual analysis, Insurance coverage extraction, amortization scheduling or WBS/AutoRec evidence; these remain REFS-specific, API-authoritative controls.

## Cumulative authoritative UI replay on core 252 (2026-08-20)

| Surface | Newly retained evidence | Cumulative REFS result | Status |
|---|---|---|---|
| Core period scope | Source audit on authoritative core `35ab443c` confirmed AP/AR list reads, Journal list/detail and financial statements already send and validate the configured `periodId`; the 252 control-total fix remains in the authoritative base. | This cumulative UI replay does not replace, duplicate or relax those API contracts. | VERIFIED LOCALLY — API/client contracts retained; authenticated Render remains unverified. |
| Desktop navigation panel | Authenticated QBO Standard Reports exposed a presentation-only Collapse action that removed the secondary panel while leaving the primary Reports rail available. No report or accounting action was used. | Multi-page REFS workspaces gain one labelled desktop Collapse/Expand control. The control fails closed without a handler; one-page direct routes and the mobile drawer remain unchanged. | PARTIAL — observed QBO behavior and local SSR/a11y contracts; authenticated REFS rendering unverified. |
| Chart of Accounts and Register | Authenticated QBO COA was inspected read-only at 900px and 525px. The 525px list reflowed account facts without document-level horizontal overflow. Populated rows used `View register`; opening one cash row showed a direct `Back to Chart of Accounts`, `Bank Register`, balances, Filter and the populated register columns. No Reconcile, Bank transactions, account CRUD, batch, print, export, settings or row mutation was used. | At ≤600px every authoritative account fact and `View register` reflows into labelled cards; filters use a compact three-row grid; the region has the width-neutral name `Chart of Accounts`. Register uses the stable `Account Register` title and a single direct Back action, without the redundant `Posted account activity` phrase, while retaining account name/code, scope, paging and exact Back. | PARTIAL — responsive structure, action vocabulary and direct Back hierarchy observed; QBO account-type title/columns/balances and populated authenticated REFS runtime remain unverified. |
| Chart of Accounts page capacity | Authenticated QBO COA showed `1 - 200`, an announced `Showing accounts 1 to 200`, and Previous/Next controls. The name/number filter and the displayed limit remained unchanged; no paging control, row action or account action was used. | The authoritative COA now defaults to 200 rows and offers 25/50/100/200 choices while retaining its contained table, result announcement, client-side filters and exact Register Back page-size token. Account Register remains 25/50/100 so a populated ledger does not create an overlong page. | PARTIAL — QBO 200-account range observed; QBO behavior beyond page one and authenticated populated REFS paging remain unverified. |
| Reports groups and shell | Authenticated QBO Standard Reports at 525px used level-two expandable groups in an internally scrolling fixed product frame. `Business overview` could collapse independently while other groups remained expanded. Favorite, More Options, Create and promotion actions were not used. | REFS uses compact vertical level-two group headings with shared icons, chevrons, ARIA state and collapsible active content. The authoritative shell is fixed to `100dvh`; central content owns contained scrolling. REFS deliberately keeps one active API workspace rather than claiming QBO multi-open equivalence. | PARTIAL — group/fixed-frame evidence observed; multi-open and authenticated REFS geometry remain unverified. |
| Statement of Cash Flows | Authenticated QBO exposed Statement of Cash Flows as an independent full page with Back, period/from-to and display-columns controls plus an Operating activities hierarchy. Cash/Accrual, Compare and row drill were not exercised. | `Cash activity` no longer aliases the formal statement. A separate search result and Common Reports shortcut open the approved-mapping reader as a full page with 25-row paging, Refresh, BLOCKED classification handling, row detail and nested exact Back. | PARTIAL — independent report structure observed; QBO basis/compare/row drill and authenticated REFS runtime remain unverified. |
| Trial Balance as-of heading | Authenticated QBO Standard Reports search returned Trial Balance and opened an independent full page with `Back to standard reports`, report-period/from/to controls, Cash/Accrual controls, Debit/Credit columns, a ready status, equal totals and the visible heading `As of August 20, 2026`. No basis, date, display-column, Customize, Save, Email, Print, Export or row action was used. | REFS now shows `As of <period_end>` only when every authoritative Trial Balance row supplies the same valid period-end date. Mixed or missing dates produce no inferred caption. Existing full-page Back, 25-row paging, Refresh, Debit/Credit/Balance, row detail and immutable scope remain unchanged. | PARTIAL — as-of structure observed and locally bound to API rows; QBO control behavior, row drill and authenticated populated REFS rendering remain unverified. |
| Balance Sheet as-of heading | Authenticated QBO opened Balance Sheet as an independent full page with Back, period/from/to, Cash/Accrual, display-columns and Compare controls, `As of August 20, 2026`, ready status and an Assets → Current Assets → Bank Accounts hierarchy. No control, Customize, Save, Email, Print, Export, Insights or row action was used. | The same fail-closed caption now applies to Balance Sheet: `As of <period_end>` renders only when every authoritative Balance Sheet row has one identical valid period end. Existing section rows, amounts, full-page Back, paging, Refresh and detail lineage remain unchanged. | PARTIAL — hierarchy and as-of structure observed; QBO controls/row drill and authenticated populated REFS rendering remain unverified. |
| Profit and Loss period and empty state | Authenticated QBO opened Profit and Loss as an independent full page with Back, report-period/from/to, Cash/Accrual, display-columns and Compare controls, a visible date-range heading and a ready empty-result alert. No control, Customize, Save, Email, Print, Export, Insights or row action was used. | REFS now renders the authoritative `period_start`–`period_end` range only when every Income Statement row agrees; mixed or missing dates produce no inferred caption. The full-page empty state uses concise guidance while explicitly preserving the rule that an empty response does not prove a zero balance. | PARTIAL — period/empty-state structure observed and locally bound to API rows; QBO control behavior, populated hierarchy/row drill and authenticated REFS rendering remain unverified. |
| General Ledger period and grouping | Authenticated QBO opened General Ledger as an independent full page with Back, report-period/from/to and Cash/Accrual controls, a visible date-range heading, ready status, account group headings, Beginning Balance and account totals. Visible columns were Distribution account, Transaction date, Transaction type, Num, Name, Description, Split, Amount and Balance. No control, Customize, Save, Email, Print, Export, More action or row action was used. | REFS now displays a General Ledger range only when every returned API row has one identical valid `period_start` and `period_end`; mixed or missing periods produce no inferred caption. Existing account/search filters, server paging, Date/Account/Journal/Member/Description/Currency/Debit/Credit columns, full-page lineage and exact Back remain unchanged. | PARTIAL — QBO range/group/column structure observed; transaction type/name/split, beginning/running/account totals, ad-hoc dates/basis and authenticated populated REFS rendering remain unimplemented or unverified. |
| A/R Aging Summary heading and empty state | Authenticated QBO opened A/R Aging Summary Report as an independent full page with Back, one as-of selector/date, `As of August 20, 2026`, ready status and a concise empty-result alert. No Customize, Save, Email, Print, Export, More action or date control was used. | The authoritative aging surface now uses the business title `Accounts receivable aging summary`, a readable as-of caption derived from the selected date, concise Refresh and empty-state language, and still states that an empty scoped response does not confirm a zero balance. Existing entity/period scope, immutable return validation, currency buckets and control totals remain unchanged. | PARTIAL — heading/as-of/empty structure observed; QBO customer-level rows, populated drill, control behavior and authenticated REFS rendering remain unimplemented or unverified. |
| Statement of Cash Flows period heading | Authenticated QBO opened Statement of Cash Flows as an independent full page with Back, report-period/from/to and display-columns controls, `January 1-August 20, 2026`, ready status, Account Name/Total columns, Operating Activities hierarchy, Net Income reconciliation, net cash change and beginning/ending cash rows. No control, Customize, Save, Email, Print, Export, More action, Insights or row action was used. | The separate approved-mapping Statement of Cash Flows now displays a readable range derived only from the authoritative runtime period start/end. Missing, invalid or reversed scope renders no inferred heading. Existing operating/investing/financing classification, BLOCKED handling, totals, 25-row paging, lineage and exact Back remain unchanged. | PARTIAL — QBO range/hierarchy observed; REFS classification reader does not yet expose QBO-style beginning/ending cash reconciliation, and authenticated populated rendering/control behavior remain unverified. |
| Reconcile first-screen language | Fresh authenticated QBO Reconcile again opened with the business heading `Match the books to the bank records`, concise benefits, and Connect now/Get started actions. None of those actions was used. | REFS keeps its existing authoritative statement and Controller workflows, but replaces the internal-looking `Match retained statements to the books` with concise `Match the books to bank records.` Reconciliation history and statement evidence remain unchanged. It does not import QBO connection marketing or expose a write action merely because no statement was found. | `npm run test:authoritative-bank` exit 0; `npm run test:authoritative-full-shell` exit 0; `npm run test:authoritative-visual-parity` exit 0; `npm run build` exit 0; `git diff --check` exit 0. | PARTIAL — QBO onboarding behavior was not operated; external connection/tutorial behavior is intentionally excluded and authenticated populated REFS reconciliation remains unverified. |
| Expenses narrow toolbar | Authenticated QBO Expenses at 525×599 showed Transaction Type, Filter and Dates on one compact row above a scoped empty state. Opening Filter without Apply exposed Status, Delivery method, Date, From, To and Reset. | At 421–720px REFS keeps Search full-width and compacts Status, Filter, Reset and count into the next row. Below 421px the existing single-column safety layout remains. Expenses now uses the observed visible `Filter` and `From` / `To` labels while retaining the authoritative `through` API value and return context. | PARTIAL — QBO geometry and filter/date-label vocabulary observed; Delivery method and exact popover equivalence remain unimplemented. |
| Bank Transactions first-screen hierarchy | Authenticated QBO Bank transactions showed a direct page title, horizontally arranged account cards, compact Pending/Posted/Excluded counts, Search, date and transaction-type filters, server paging, and a populated transaction table. Print, CSV export, settings, account linking/updating, AI suggestions, Match/Categorize and Post were visible but were not operated. | REFS keeps the direct `Bank transactions` title, concise account/date scope and Refresh. Its four existing authoritative counts now render in one compact, horizontally reachable strip instead of four large KPI cards; labels remain neutral because returned/matched/journal evidence is not equivalent to QBO Pending/Posted/Excluded states. Immutable API rows, direction/amount, full-page detail, pagination and exact Back are unchanged. | `npm run test:authoritative-bank` exit 0; `npm run test:authoritative-bank-responsive` exit 0; `npm run test:authoritative-full-shell` exit 0; `npm run test:authoritative-visual-parity` exit 0; `npm run build` exit 0; `git diff --check` exit 0. | PARTIAL — authenticated QBO structure observed; QBO account cards, true Pending/Posted/Excluded read model, transaction-type/search filters and populated-column parity remain unimplemented. External connection, mutation, AI recommendation, print and export remain intentionally excluded. |
| Bank Transactions spent/received columns | The authenticated QBO populated transaction list displayed separate `Spent` and `Received` columns. Individual rows populated one side according to the bank transaction direction. No transaction was opened or changed. | The REFS bank queue now presents its existing signed, four-decimal API amount in separate `Spent` and `Received` columns. Negative source amounts display as an absolute value under Spent; positive amounts display under Received; the opposite side remains blank. Full signed direction and amount remain visible in the independent detail page. | `npm run test:authoritative-bank` exit 0; `npm run test:authoritative-bank-batch-pipeline` exit 0; `npm run test:authoritative-bank-responsive` exit 0; `npm run test:authoritative-full-shell` exit 0; `npm run test:authoritative-visual-parity` exit 0; `npm run build` exit 0; `git diff --check` exit 0. | PARTIAL — QBO column structure observed and the signed amount mapping is covered locally; Bank description, attachment, From/To and populated runtime parity remain unverified or unavailable from the current REFS read model. |
| Reports common-list row density | Authenticated QBO Standard reports showed an expanded Favorites heading followed by compact name-only links for Accounts receivable aging summary, Balance Sheet, and Profit and Loss. Favorite toggles and More Options were visible but were not operated. Shortcuts and Favorites were independently expanded in the observed view. | REFS Common reports now uses the same concise name-led row hierarchy: each existing shared icon and report name remains, while repeated one-line descriptions are removed from this default list. Search-result descriptions remain available. Every report still opens its independent full-page reader and returns to the exact catalog context. | `npm run test:authoritative-reports` exit 0; `npm run test:authoritative-full-shell` exit 0; `npm run test:authoritative-visual-parity` exit 0; `npm run build` exit 0; `git diff --check` exit 0. | PARTIAL — authenticated QBO name-only density and independent expanded groups observed; Favorites mutation/More Options remain intentionally excluded, and exact QBO multi-open collapse behavior is not claimed equivalent. |
| Reports search prompt and suggestions | Authenticated QBO Standard reports used the placeholder `Type report name here`. Entering `Balance Sheet` opened a listbox with Balance Sheet, Balance Sheet Comparison, Balance Sheet Detail, and Balance Sheet Summary. No result was opened and the transient search changed no business data. | REFS now uses the observed concise search prompt while retaining its result-only catalog filtering and direct launch of supported authoritative readers. | `npm run test:authoritative-reports` exit 0; `npm run test:authoritative-full-shell` exit 0; `npm run test:authoritative-visual-parity` exit 0; `npm run build` exit 0; `git diff --check` exit 0. | PARTIAL — the QBO listbox interaction and Comparison/Detail/Summary variants remain unimplemented because their authoritative read models are not available; REFS does not fabricate these results. |
| Full report catalog Back label | Authenticated QBO Balance Sheet and General Ledger opened as independent report pages with the single explicit link `Back to standard reports`; no adjacent catalog label was shown. General Ledger also showed report-period/from/to, Cash/Accrual, a ready status and populated grouped rows. No control, Customize, Save As, Email, Print, Export, More action or row action was used. | REFS core financial statements and Statement of Cash Flows use one direct `Back to standard reports` action without repeating `Reports center` beside it, while preserving their opaque catalog return context, selected category/search/report, page, scroll and focus. | `npm run test:authoritative-reports` exit 0; `npm run test:authoritative-full-shell` exit 0; `npm run test:authoritative-visual-parity` exit 0; `npm run build` exit 0; `git diff --check` exit 0. | PARTIAL — Back wording/hierarchy and local restoration contract are covered; QBO report controls remain unimplemented unless backed by authoritative APIs, and customize/save/email/print/export/insights remain intentionally excluded. |
| Profit and Loss visible naming | Authenticated QBO Standard reports and its report reader use the visible name `Profit and Loss`. | REFS now uses `Profit and Loss` in Common reports, search results, full-page heading and summary accessibility label while retaining the internal `INCOME_STATEMENT` API type. `Income statement` remains a finder alias, so existing accounting terminology still locates the same reader. | `npm run test:authoritative-reports` exit 0; `npm run test:authoritative-full-shell` exit 0; `npm run test:authoritative-visual-parity` exit 0; `npm run build` exit 0; `git diff --check` exit 0. | PARTIAL — visible naming is aligned; current QBO populated hierarchy, basis/compare controls and authenticated REFS rendering remain unverified. |
| Mobile primary navigation semantics | Authenticated QBO Expenses at 525×599 opened its primary application list as a labelled `Navigation menu` dialog. Closing it returned the full width to Expenses; the dialog exposed direct application buttons and no prior secondary selection remained on the page. No Create or application action was used. | The existing REFS off-canvas drawer now announces `role=dialog`, `aria-modal=true` and `Navigation menu` only while open at ≤900px. Closed drawers remain inert, desktop navigation remains a normal sidebar, Escape/Close and opener focus restoration are unchanged. | PARTIAL — accessible modal behavior observed and locally verified; exact QBO menu content/order and authenticated REFS browser geometry remain unverified. |
| AP/AR missing values | No new QBO value-placeholder equivalence is claimed. Local UX audit found storage-oriented `Not retained` copy on otherwise user-facing Bill, Invoice and adjustment surfaces. | User-visible missing values now read `Not available`; IDs, revisions, period scope, lineage, paging and exact Back remain unchanged. | LOCAL UI CANDIDATE — not a QBO-equivalence claim. |

| Reports Center General Ledger entry | Authenticated QBO Standard reports search returned General Ledger and General Ledger List. Opening General Ledger produced an independent full page with `Back to standard reports`, report-period/from-to and Cash/Accrual controls, a ready status, grouped accounts, Beginning Balance and account totals. No control or report action was used. | General Ledger is now available in Common reports and result-only search, both routing to the existing authoritative POSTED GL GET reader. A Reports-supplied immutable parent context adds `Back to standard reports` and restores the exact catalog category/query/preview, scroll and focus; direct Accounting navigation shows no false Reports parent. | `npm run test:authoritative-reports` exit 0; `npm run test:authoritative-general-ledger` exit 0; `npm run test:authoritative-full-shell` exit 0; `npm run test:authoritative-visual-parity` exit 0; `npm run build` exit 0; `git diff --check` exit 0. | PARTIAL — General Ledger List, QBO ad-hoc date/basis controls, grouped totals and authenticated populated REFS rendering remain unimplemented or unverified. |
| General Ledger List discoverability | Authenticated QBO opened General Ledger List as a distinct full page with `Back to standard reports`, Report period/From/To, Cash/Accrual, and columns Distribution account, Transaction date, Description, Name, Transaction ID, Num, Balance, Debit, Credit. Its ready result grouped rows by account with a row count, Beginning Balance, account Total, and total including subaccounts where applicable. No control, row, note, or report action was used. | Reports search exposes General Ledger List as `Not available`, without a click action or alias to General Ledger. The existing paginated GL API does not return the complete server-calculated account groups, opening/running balances, totals, Name, Transaction ID, and Num required for this distinct report; REFS therefore does not calculate them from a partial browser page. | `npm run test:authoritative-reports` exit 0; `npm run test:authoritative-visual-parity` exit 0; `npm run build` exit 0; `git diff --check` exit 0. | UNIMPLEMENTED — an authoritative grouped General Ledger List read model and authenticated REFS runtime rendering are still required. |
| General Ledger visible hierarchy and audit IDs | Fresh authenticated QBO General Ledger showed one independent full page with `Back to standard reports`, report-period/From/To, Accrual selected, a date-range heading and columns beginning with Distribution account and Transaction date. Account groups, Beginning Balance and account totals were visible. No control, row, Customize, Save As, Email, Print, Export or More action was used; row-detail equivalence is not claimed. | The current authoritative GL list adopts only the directly compatible `Transaction date` and `Distribution account` labels. The independent posted-line page keeps Journal, line, ledger and source-document IDs, but places them in a collapsed `Audit identifiers` disclosure so the default page remains concise. API fields, POSTED scope, paging, full-page lineage and exact Back remain unchanged. | `npm run test:authoritative-general-ledger` exit 0 (4/4); `npm run test:authoritative-full-shell` exit 0; `npm run test:authoritative-visual-parity` exit 0; `npm run test:navigation-a11y` exit 0; `npm run build` exit 0; `npm test` exit 0 (visual verifier 64/64); `git diff --check` exit 0. | PARTIAL — QBO grouping, transaction type/name/split, opening/running/account totals, ad-hoc date/basis controls and authenticated populated REFS rendering remain unimplemented or unverified. |
| Journal line information density | A direct authenticated QBO Journal route opened the Journal Entry form rather than a list. Without entering or saving anything, the visible table showed Account, Debits, Credits, Description, Name and Class, plus Journal date/no., totals, Memo, Attachments and History. No field, row, attachment, Save, export or other action was used, and list/detail equivalence is not claimed. | REFS keeps its read-only Journal list and controlled server workflow. The independent Journal evidence page keeps Line, Account, Debit, Credit, Member, Dimensions and Description in the primary table, while Journal-line, ledger-line and source-document IDs remain in a collapsed `Audit identifiers` disclosure. This shortens the default table without deleting immutable evidence or changing exact Back. | `npm run test:authoritative-journals` exit 0; `npm run test:authoritative-full-shell` exit 0; `npm run test:authoritative-visual-parity` exit 0; `npm run test:navigation-a11y` exit 0; `npm run build` exit 0; `git diff --check` exit 0. | PARTIAL — QBO list/detail/history behavior was not observed; Name/Class/Memo/Attachments are not inferred from unavailable API fields, and authenticated populated REFS rendering remains unverified. |
| Trial Balance debit and credit basis | Fresh authenticated QBO Standard reports search returned Trial Balance and Adjusted Trial Balance. Opening Trial Balance produced an independent full page with `Back to standard reports`, Report period/From/To, Accrual selected, Display columns by, `As of August 20, 2026`, ready status, Account/Debit/Credit and a balanced TOTAL. No report control, row, Customize, Save As, Email, Print, Export, note or More action was used. | REFS Trial Balance now labels its authoritative columns `Debit` and `Credit` and renders API-returned `ending_debit` / `ending_credit`, which match the existing as-of period caption. Other financial statements continue to use period debit/credit activity. Balance, evidence detail, 25-row paging and exact Back remain unchanged. | `npm run test:authoritative-reports` exit 0; `npm run test:authoritative-full-shell` exit 0; `npm run test:authoritative-visual-parity` exit 0; `npm run test:navigation-a11y` exit 0; `npm run build` exit 0; `npm test` exit 0 (visual verifier 64/64); `git diff --check` exit 0. | PARTIAL — QBO Adjusted Trial Balance, ad-hoc period/basis/display-column controls, TOTAL row and authenticated populated REFS rendering remain unimplemented or unverified. |
| Adjusted Trial Balance discoverability | Fresh authenticated QBO Standard reports search opened Adjusted Trial Balance as a distinct full-page report with `Back to standard reports`, Report period/From/To, Cash/Accrual, an as-of heading, ready status, and grouped `Unadjusted Balance`, `Adjustments`, and `Adjusted Balance` Debit/Credit columns. No control, row, Customize, Save As, Email, Print, Export, note or More action was used. | REFS search now exposes Adjusted Trial Balance as an explicitly unavailable report rather than hiding it or routing to ordinary Trial Balance. The unavailable reason requires the three observed balance groups; no adjustments are inferred from the current financial-statement rows. | `npm run test:authoritative-reports` exit 0; `npm run test:authoritative-full-shell` exit 0; `npm run test:authoritative-visual-parity` exit 0; `npm run test:navigation-a11y` exit 0; `npm run build` exit 0; `npm test` exit 0 (visual verifier 64/64); `git diff --check` exit 0. | UNIMPLEMENTED — the authoritative API does not return unadjusted and adjustment columns, so no reader, drill or equivalence is claimed. |
| Balance Sheet Detail discoverability | Fresh authenticated QBO Standard reports search opened Balance Sheet Detail as a distinct full page with `Back to standard reports`, Report period/From/To, Cash/Accrual, a date-range heading and ready status. It grouped transactions by balance-sheet account with Beginning Balance and account Total rows, and exposed Transaction date/type, Num, Name, Item class, Description, Split account, Debit, Credit, Amount and Balance columns. No control, sort, row, Customize, Save As, Email, Print, Export, note or More action was used. | REFS search exposes Balance Sheet Detail as explicitly unavailable rather than routing to the aggregate Balance Sheet reader. The unavailable reason requires grouped transaction rows and running balances; the browser does not infer these from a paged GL or aggregate statement response. | `npm run test:authoritative-reports` exit 0; `npm run test:authoritative-full-shell` exit 0; `npm run test:authoritative-visual-parity` exit 0; `npm run test:navigation-a11y` exit 0; `npm run build` exit 0; `npm test` exit 0 (visual verifier 64/64); `git diff --check` exit 0. | UNIMPLEMENTED — an authoritative transaction-detail report read model, server-calculated grouping/balances and authenticated REFS rendering are required. |
| Profit and Loss Detail discoverability | Fresh authenticated QBO Standard reports search opened Profit and Loss Detail as a distinct full page with `Back to standard reports`, Report period/From/To, Cash/Accrual, `January 1-August 20, 2026`, ready status and the scoped alert `Your selection doesn’t have any info. Change your selection or start a new search.` No detail columns were observable in this empty scope. No control, row, Customize, Save As, Email, Print, Export, note or More action was used. | REFS search exposes Profit and Loss Detail as explicitly unavailable rather than routing to aggregate Profit and Loss. The unavailable reason requires a separate authoritative detail-report read model; REFS does not invent transaction rows or columns from the QBO empty state. | `npm run test:authoritative-reports` exit 0; `npm run test:authoritative-full-shell` exit 0; `npm run test:authoritative-visual-parity` exit 0; `npm run test:navigation-a11y` exit 0; `npm run build` exit 0; `npm test` exit 0 (visual verifier 64/64); `git diff --check` exit 0. | UNIMPLEMENTED — the separate detail API, populated QBO columns, authenticated REFS empty/ready states and drill behavior remain unavailable or unverified. |
| Balance Sheet Comparison discoverability | Fresh authenticated QBO Standard reports search opened Balance Sheet Comparison as a distinct full page with `Back to standard reports`, Report period/From/To, Cash/Accrual, Display columns by and `Compare to Previous year (PY)`. The ready report used an as-of heading, an Assets hierarchy and account/group totals, with `As of Aug 20, 2026` and `As of Aug 20, 2025 (PY)` value columns. No control, row, Customize, Save As, Email, Print, Export, note or More action was used. | REFS search exposes Balance Sheet Comparison as explicitly unavailable rather than routing to aggregate Balance Sheet or the generic manual period-ID comparison workbench. The unavailable reason identifies the missing authoritative previous-year period resolver and grouped comparison totals. | `npm run test:authoritative-reports` exit 0; `npm run test:authoritative-full-shell` exit 0; `npm run test:authoritative-visual-parity` exit 0; `npm run test:navigation-a11y` exit 0; `npm run build` exit 0; `npm test` exit 0 (visual verifier 64/64); `git diff --check` exit 0. | UNIMPLEMENTED — the generic comparison API does not resolve QBO-style previous-year scope or provide the observed grouped Balance Sheet hierarchy/totals; no equivalence is claimed. |
| Profit and Loss Comparison discoverability | Fresh authenticated QBO Standard reports search opened Profit and Loss Comparison as a distinct full page with `Back to standard reports`, Report period/From/To, Cash/Accrual, Display columns by and `Compare to Previous year (PY)`. The ready report used a date-range heading, Income/Gross Profit/Expenses/Net Operating Income/Net Income hierarchy and totals, with current-year-to-date and prior-year-to-date value columns. No control, row, Customize, Save As, Email, Print, Export, note or More action was used. | REFS search exposes Profit and Loss Comparison as explicitly unavailable rather than routing to aggregate Profit and Loss or the generic manual period-ID comparison workbench. The unavailable reason identifies the missing authoritative previous-year period resolver and grouped comparison totals. | `npm run test:authoritative-reports` exit 0; `npm run test:authoritative-full-shell` exit 0; `npm run test:authoritative-visual-parity` exit 0; `npm run test:navigation-a11y` exit 0; `npm run build` exit 0; `npm test` exit 0 (visual verifier 64/64); `git diff --check` exit 0. | UNIMPLEMENTED — the generic comparison API does not resolve QBO-style prior-year-to-date scope or provide the observed Profit and Loss hierarchy/totals; no equivalence is claimed. |
| Balance Sheet Summary discoverability | Fresh authenticated QBO Standard reports search opened Balance Sheet Summary as a distinct full page with `Back to standard reports`, Report period/From/To, Cash/Accrual, Display columns by and Compare to. The ready as-of report grouped Assets and Liabilities and Equity by account type, including Bank Accounts, Accounts Receivable, Other Current Assets, Accounts Payable and other category subtotals, with one Total value column. No control, row, Customize, Save As, Email, Print, Export, note or More action was used. | REFS search exposes Balance Sheet Summary as explicitly unavailable rather than routing to the account-level Balance Sheet reader. The unavailable reason requires server-defined account-type groups and subtotals; REFS does not infer those groups from account names or codes. | `npm run test:authoritative-reports` exit 0; `npm run test:authoritative-full-shell` exit 0; `npm run test:authoritative-visual-parity` exit 0; `npm run test:navigation-a11y` exit 0; `npm run build` exit 0; `npm test` exit 0 (visual verifier 64/64); `git diff --check` exit 0. | UNIMPLEMENTED — the financial-statement response lacks the authoritative account-type hierarchy and summary subtotal rows observed in QBO. |
| Quarterly Profit and Loss Summary discoverability | Fresh authenticated QBO Standard reports search returned Quarterly Profit and Loss Summary as a distinct full page with `Back to standard reports`, Report period/From/To, Cash/Accrual, Display columns by `Quarters` and Compare to. The report was ready for January 1-August 20, 2026 but the current scope showed `Your selection doesn’t have any info. Change your selection or start a new search.` No control, row, Customize, Save As, Email, Print, Export, note or More action was used; no populated columns or totals were observable. | REFS search exposes Quarterly Profit and Loss Summary as explicitly unavailable rather than routing to the aggregate Profit and Loss reader. The unavailable reason requires authoritative quarter columns and period totals; REFS does not fabricate quarterly values from an empty QBO result or split a period response in the browser. | `npm run test:authoritative-reports` exit 0; `npm run test:authoritative-full-shell` exit 0; `npm run test:authoritative-visual-parity` exit 0; `npm run test:navigation-a11y` exit 0; `npm run build` exit 0; `npm test` exit 0 (visual verifier 64/64); `git diff --check` exit 0. | UNIMPLEMENTED — the authoritative API lacks quarterly columns/totals, populated QBO structure and authenticated REFS runtime remain unverified; ordinary Profit and Loss Summary was not returned by this search and is not claimed. |
| Expenses by Vendor Summary discoverability | Fresh authenticated QBO Standard reports search returned Expenses by Vendor Summary and opened it as a distinct full page with `Back to standard reports`, Report period/From/To, Cash/Accrual, Display columns by and Compare to. The report was ready for January 1-August 20, 2026 but the current scope showed `Your selection doesn’t have any info. Change your selection or start a new search.` No control, row, Customize, Save As, Email, Print, Export, note or More action was used; no populated vendor rows, columns or totals were observable. | REFS search exposes Expenses by Vendor Summary as explicitly unavailable. The unavailable reason requires server-grouped vendor expense totals; REFS does not aggregate the paged invoice list, AI vendor findings or A/P aging control totals into a financial report. | `npm run test:authoritative-reports` exit 0; `npm run test:authoritative-full-shell` exit 0; `npm run test:authoritative-visual-parity` exit 0; `npm run test:navigation-a11y` exit 0; `npm run build` exit 0; `npm test` exit 0 (visual verifier 64/64); `git diff --check` exit 0. | UNIMPLEMENTED — the authoritative API lacks vendor-grouped expense totals, and populated QBO structure plus authenticated REFS runtime remain unverified. |
| Unpaid Bills report discoverability | Fresh authenticated QBO Standard reports search opened `Unpaid Bills Report` as a distinct full page with `Back to standard reports` and an as-of selector (`All Dates` in the observed scope). The populated table was grouped by vendor and exposed Date, Transaction type, Num, Due date, Past due, Amount and Open balance, with vendor subtotals and a grand total. Pivot and Chart were visible but not operated. No control, row, bill, payment, Customize, Save As, Email, Print, Export, note or More action was used; no real vendor, document number or amount is retained in this matrix. | REFS search exposes Unpaid Bills as explicitly unavailable rather than routing to the current Unpaid Bills queue or currency-only A/P aging control totals. The unavailable reason requires an immutable as-of, vendor-grouped open-balance snapshot; no document membership, historical balance or subtotal is inferred. | `npm run test:authoritative-reports` exit 0; `npm run test:authoritative-full-shell` exit 0; `npm run test:authoritative-visual-parity` exit 0; `npm run test:navigation-a11y` exit 0; `npm run build` exit 0; `npm test` exit 0 (visual verifier 64/64); `git diff --check` exit 0. | UNIMPLEMENTED — the authoritative API lacks vendor-grouped as-of open-bill rows, snapshot identity/hash and subtotals; Pivot/Chart, authenticated REFS runtime and populated drill/Back remain unverified. |
| Transaction List by Vendor discoverability | Fresh authenticated QBO Standard reports search opened Transaction List by Vendor as a distinct full page with `Back to standard reports`, Report period/From/To and ready status. The observed table schema was Date, Track 1099, Transaction type, Num, Posting (Y/N), Memo, Account full name, Item split account and Amount. The current period was empty. Pivot and Chart plus a Vendor Contact List notice were visible but not operated. No control, row, Customize, Save As, Email, Print, Export, note or More action was used. | REFS search exposes Transaction List by Vendor as explicitly unavailable. The unavailable reason requires one server-defined vendor-grouped transaction response carrying posting and split-account facts; REFS does not join Source Documents, GL lines or vendor metadata in the browser. | `npm run test:authoritative-reports` exit 0; `npm run test:authoritative-full-shell` exit 0; `npm run test:authoritative-visual-parity` exit 0; `npm run test:navigation-a11y` exit 0; `npm run build` exit 0; `npm test` exit 0 (visual verifier 64/64); `git diff --check` exit 0. | UNIMPLEMENTED — the authoritative API lacks vendor-grouped transaction rows, Track 1099, Posting Y/N and split-account facts; Pivot/Chart, populated QBO rows and authenticated REFS runtime remain unverified. |
| Vendor Balance Summary discoverability | Fresh authenticated QBO Standard reports search opened Vendor Balance Summary as a distinct full page with `Back to standard reports`, an as-of selector (`All Dates` in the observed scope), Compare to and ready status. The populated report showed one vendor row, a Total value column and a grand total. No control, row, Customize, Save As, Email, Print, Export, note or More action was used; no real vendor name or amount is retained in this matrix. | REFS search exposes Vendor Balance Summary as explicitly unavailable. The unavailable reason requires authoritative as-of vendor balances and totals; current Bills rows and currency-only A/P aging aggregates are not treated as vendor-balance membership or a historical snapshot. | `npm run test:authoritative-reports` exit 0; `npm run test:authoritative-full-shell` exit 0; `npm run test:authoritative-visual-parity` exit 0; `npm run test:navigation-a11y` exit 0; `npm run build` exit 0; `npm test` exit 0 (visual verifier 64/64); `git diff --check` exit 0. | UNIMPLEMENTED — the authoritative API lacks immutable as-of vendor balance rows, snapshot identity/hash and report totals; authenticated REFS runtime and populated drill/Back remain unverified. |
| Vendor Balance Detail discoverability | Fresh authenticated QBO Standard reports search opened Vendor Balance Detail Report as a distinct full page with `Back to standard reports`, an as-of selector (`All Dates` in the observed scope) and ready status. The populated report was grouped by vendor and exposed Date, Transaction type, Num, Due date, Amount, Open balance and running Balance; the observed group included Bill and Bill Payment rows, followed by a vendor total and grand total. No control, row, bill, payment, Customize, Save As, Email, Print, Export, note or More action was used; no real vendor, document number or amount is retained in this matrix. | REFS search exposes Vendor Balance Detail as explicitly unavailable. The unavailable reason requires one authoritative as-of vendor transaction ledger with running balances; current Bills rows and currency-only A/P aging aggregates are not joined or treated as historical vendor-ledger membership. | `npm run test:authoritative-reports` exit 0; `npm run test:authoritative-full-shell` exit 0; `npm run test:authoritative-visual-parity` exit 0; `npm run test:navigation-a11y` exit 0; `npm run build` exit 0; `npm test` exit 0 (visual verifier 64/64); `git diff --check` exit 0. | UNIMPLEMENTED — the authoritative API lacks immutable as-of vendor transaction rows, snapshot identity/hash, group totals and running balances; authenticated REFS runtime and populated drill/Back remain unverified. |
| Accounts payable aging summary discoverability | Fresh authenticated QBO Standard reports search opened A/P Aging Summary Report as a distinct full page with `Back to standard reports`, an as-of selector/date, `As of August 20, 2026` and ready status. The populated vendor row used CURRENT, 1–30, 31–60, 61–90, 91 AND OVER and Total columns, followed by a grand total. No control, row, vendor, Customize, Save As, Email, Print, Export, note or More action was used; no real vendor or amount is retained in this matrix. | REFS search exposes Accounts payable aging summary as explicitly unavailable. The current Accounts payable aging page remains available as currency aging/control-total evidence, but it is not labelled or routed as the vendor-level five-bucket summary. | `npm run test:authoritative-reports` exit 0; `npm run test:authoritative-full-shell` exit 0; `npm run test:authoritative-visual-parity` exit 0; `npm run test:navigation-a11y` exit 0; `npm run build` exit 0; `npm test` exit 0 (visual verifier 64/64); `git diff --check` exit 0. | UNIMPLEMENTED — the authoritative API lacks immutable as-of vendor bucket rows, snapshot identity/hash and grand totals; authenticated REFS runtime and Summary→Detail/document drill remain unverified. |
| Accounts payable aging detail discoverability | Fresh authenticated QBO Standard reports search opened A/P Aging Detail Report as a distinct full page with `Back to standard reports`, an as-of selector/date and ready status. The populated report was grouped by aging bucket and exposed Date, Transaction type, Num, Vendor display name, Due date, Past due, Amount and Open balance; the observed bucket included Bill and Bill Payment rows, followed by a bucket total and grand total. No control, sort, row, vendor, bill, payment, Customize, Save As, Email, Print, Export or More action was used; no real vendor, document number or amount is retained in this matrix. | REFS search exposes Accounts payable aging detail as explicitly unavailable. The unavailable reason requires an immutable as-of vendor-document snapshot grouped by aging bucket; current Bills rows and currency-only A/P aging control totals are not treated as historical document membership. | `npm run test:authoritative-reports` exit 0; `npm run test:authoritative-full-shell` exit 0; `npm run test:authoritative-visual-parity` exit 0; `npm run test:navigation-a11y` exit 0; `npm run build` exit 0; `npm test` exit 0 (visual verifier 64/64); `git diff --check` exit 0. | UNIMPLEMENTED — the authoritative API lacks immutable as-of vendor/document aging rows, snapshot identity/hash, bucket membership and totals; authenticated REFS runtime and Detail→document→Back remain unverified. |
| Accounts receivable aging detail discoverability | Fresh authenticated QBO Standard reports search opened A/R Aging Detail Report as a distinct full page with `Back to standard reports`, an as-of selector/date, `As of August 20, 2026`, ready status and a concise scoped-empty alert. The observed table schema was Date, Transaction type, Num, Customer full name, Due date, Amount and Open balance. Pivot and Chart were visible but not operated; no populated row or aging-bucket grouping was observed. No control, sort, row, customer, invoice, receipt, Customize, Save As, Email, Print, Export or More action was used. | REFS search exposes Accounts receivable aging detail as explicitly unavailable. The unavailable reason requires an immutable as-of customer-document snapshot; current Invoice rows and currency-only A/R aging control totals are not treated as historical document membership. | `npm run test:authoritative-reports` exit 0; `npm run test:authoritative-full-shell` exit 0; `npm run test:authoritative-visual-parity` exit 0; `npm run test:navigation-a11y` exit 0; `npm run build` exit 0; `npm test` exit 0 (visual verifier 64/64); `git diff --check` exit 0. | UNIMPLEMENTED — the authoritative API lacks immutable as-of customer/document aging rows and snapshot identity/hash; populated QBO grouping, Pivot/Chart, authenticated REFS runtime and Detail→document→Back remain unverified. |
| Accounts receivable aging summary discoverability | Fresh authenticated QBO Standard reports search opened A/R Aging Summary Report as a distinct full page with `Back to standard reports`, an as-of selector/date, `As of August 20, 2026`, ready status and a concise scoped-empty alert. No populated customer row, bucket column or total was observable in this scope. No control, row, customer, Customize, Save As, Email, Print, Export, note or More action was used. | REFS search exposes Accounts receivable aging summary as explicitly unavailable. The current Accounts receivable aging page remains available as currency aging/control-total evidence, but it is not labelled or routed as the customer-level Summary. | `npm run test:authoritative-reports` exit 0; `npm run test:authoritative-full-shell` exit 0; `npm run test:authoritative-visual-parity` exit 0; `npm run test:navigation-a11y` exit 0; `npm run build` exit 0; `npm test` exit 0 (visual verifier 64/64); `git diff --check` exit 0. | UNIMPLEMENTED — the authoritative API lacks immutable as-of customer aging rows and snapshot identity/hash; populated QBO buckets/totals, authenticated REFS runtime and Summary→Detail/document drill remain unverified. |
| Customer Balance Summary discoverability | Fresh authenticated QBO Standard reports search opened Customer Balance Summary as a distinct full page with `Back to standard reports`, an as-of selector (`All Dates` in the observed scope), Compare to, ready status and a concise scoped-empty alert. No populated customer row, value column or total was observable. No control, row, customer, Customize, Save As, Email, Print, Export, note or More action was used. | REFS search exposes Customer Balance Summary as explicitly unavailable. The unavailable reason requires authoritative as-of customer balances and totals; current Invoice rows and currency-only A/R aging aggregates are not treated as customer-balance membership or a historical snapshot. | `npm run test:authoritative-reports` exit 0; `npm run test:authoritative-full-shell` exit 0; `npm run test:authoritative-visual-parity` exit 0; `npm run test:navigation-a11y` exit 0; `npm run build` exit 0; `npm test` exit 0 (visual verifier 64/64); `git diff --check` exit 0. | UNIMPLEMENTED — the authoritative API lacks immutable as-of customer balance rows, snapshot identity/hash and report totals; populated QBO columns, authenticated REFS runtime and drill/Back remain unverified. |
| Customer Balance Detail discoverability | Fresh authenticated QBO Standard reports search opened Customer Balance Detail Report as a distinct full page with `Back to standard reports`, an as-of selector (`All Dates` in the observed scope), ready status and a concise scoped-empty alert. The observed table schema was Date, Transaction type, Num, Due date, Amount, Open balance and running Balance. No populated row, customer grouping or total was observable. No control, sort, row, customer, invoice, receipt, Customize, Save As, Email, Print, Export, note or More action was used. | REFS search exposes Customer Balance Detail as explicitly unavailable. The unavailable reason requires one authoritative as-of customer transaction ledger with running balances; current Invoice rows and currency-only A/R aging aggregates are not joined or treated as historical customer-ledger membership. | `npm run test:authoritative-reports` exit 0; `npm run test:authoritative-full-shell` exit 0; `npm run test:authoritative-visual-parity` exit 0; `npm run test:navigation-a11y` exit 0; `npm run build` exit 0; `npm test` exit 0 (visual verifier 64/64); `git diff --check` exit 0. | UNIMPLEMENTED — the authoritative API lacks immutable as-of customer transaction rows, snapshot identity/hash and running balances; populated QBO grouping/totals, authenticated REFS runtime and drill/Back remain unverified. |
| Transaction List by Customer discoverability | Fresh authenticated QBO Standard reports search opened Transaction List by Customer as a distinct full page with `Back to standard reports`, Report period/From/To and ready status. The current period was empty, while the visible table schema was Date, Transaction type, Num, Posting (Y/N), Memo, Account full name and Amount. Pivot and Chart were visible but not operated. No control, sort, row, customer, invoice, receipt, Customize, Save As, Email, Print, Export, note or More action was used. | REFS search exposes Transaction List by Customer as explicitly unavailable. The unavailable reason requires one server-defined customer-grouped transaction response carrying posting facts; REFS does not join Invoices, Source Documents, GL lines or customer metadata in the browser. | `npm run test:authoritative-reports` exit 0; `npm run test:authoritative-full-shell` exit 0; `npm run test:authoritative-visual-parity` exit 0; `npm run test:navigation-a11y` exit 0; `npm run build` exit 0; `npm test` exit 0 (visual verifier 64/64); `git diff --check` exit 0. | UNIMPLEMENTED — the authoritative API lacks customer-grouped transaction rows and Posting Y/N facts; populated QBO rows, Pivot/Chart, authenticated REFS runtime and drill/Back remain unverified. |
| Expenses secondary filter Apply boundary | Authenticated QBO Expenses showed Transaction Type and a collapsed `Filter` control. Opening Filter exposed Status, Delivery method, Date, From, To, plus separate Reset and Apply actions; the visible list was empty with `No expenses found` and `Try to change some filters to see more results.` No filter value or action was operated. | REFS keeps Search and Status compact, stages From/To/Vendor/Category inside Filter, and changes the displayed result only when Apply is selected. Reset clears the staged secondary values; the applied-filter count and exact list/Back scope remain visible while the disclosure stays collapsed by default. Delivery method is omitted because REFS has no delivery workflow in this read-only accounting scope. | `npm run test:authoritative-documents` exit 0; `npm run test:payables-responsive` exit 0; `npm run test:authoritative-visual-parity` exit 0; `npm run build` exit 0; `git diff --check` exit 0. | PARTIAL — QBO Reset/Apply behavior was not operated, and authenticated REFS responsive/runtime interaction remains unverified. |
| Expenses Payee filter vocabulary | The same authenticated QBO Filter popover visibly labelled the counterparty selector `Payee`, followed by `Category` and `Project`. No selector was opened or changed. | The AP presentation filter and its applied-scope summary now use `Payee` / `All payees`, while retaining the existing authoritative vendor-backed counterparty value and exact Back context. Bill tables and document evidence continue to use `Vendor`, because those are document facts rather than the broader Expenses filter label. Project remains absent because the current authoritative AP list response does not provide a project filter contract. | `npm run test:authoritative-documents` exit 0; `npm run test:payables-responsive` exit 0; `npm run test:authoritative-browser-acceptance-preflight` exit 0; `npm run build` exit 0; `git diff --check` exit 0. | PARTIAL — QBO Payee/Category/Project selection behavior and authenticated populated REFS runtime remain unverified; Project is unimplemented. |
| Chart of Accounts heading and search vocabulary | Authenticated QBO Chart of accounts rendered a concise `Chart of accounts` heading and a text field labelled `Filter by name or number`. The loaded table showed Name, Account type, QuickBooks balance, Bank balance and Action, with a visible `1 - 200` range. No filter, row, action, pagination, batch, report, export, print or account command was used. | REFS uses the same visible `Chart of accounts` sentence-case name in the page heading, Register Back action and retained-filter disclosure. It keeps the concise `ACCOUNTING` context, `Review accounts and posted balances.` subtitle and `Filter by name or number` search. Authoritative Status/Currency filters, 200-row page, four-decimal balances, member rules, posted-line counts, View register and exact Back remain unchanged. | `npm run test:authoritative-coa-register` exit 0; `npm run test:authoritative-full-shell` exit 0; `npm run test:authoritative-visual-parity` exit 0; `npm run build` exit 0; `git diff --check` exit 0. | PARTIAL — QBO account-type filtering/sorting, conditional Run report versus View register actions, populated authenticated REFS rendering and responsive runtime remain unverified or unimplemented. |
| Reports common-entry priority | Authenticated QBO Standard reports showed the expanded Favorites group beginning with Accounts receivable aging summary, Balance Sheet, and Profit and Loss. Favorite and More Options controls were visible but were not operated; no report was opened in this observation. | REFS Common reports now leads with its existing read-only Accounts receivable aging control-total reader, Balance Sheet, and Profit and Loss entries. It uses a neutral `Common reports` accessible name and `authoritative-common-*` focus IDs rather than implying favorites state. Existing independent full-page readers, exact Back context, API scope and all excluded favorite/customize/share/export actions are unchanged. | `npm run test:authoritative-reports` exit 0; `npm run test:authoritative-full-shell` exit 0; `npm run test:authoritative-visual-parity` exit 0; `npm run build` exit 0; `git diff --check` exit 0. | PARTIAL — the REFS A/R Aging entry remains a currency control-total reader, not QBO customer aging summary/detail; favorite mutation, More Options and authenticated populated REFS runtime remain excluded or unverified. |
| Standard reports page naming | The authenticated QBO report catalog used the visible heading and Reports & Analytics navigation label `Standard reports`; reports opened from it use `Back to standard reports`. Custom reports, Management reports, KPIs, Dashboards and other sibling destinations were visible but not operated. | The authoritative catalog heading and its Reports secondary-navigation item now both use `Standard reports`, matching the existing full-page Back destination. The primary rail remains `Reports`, and its separate Accounting Analysis Report child means the secondary menu remains necessary. Report routes, API readers, catalog context, full-page drill and exact Back are unchanged. | `npm run test:authoritative-reports` exit 0; `npm run test:authoritative-full-shell` exit 0; `npm run test:authoritative-visual-parity` exit 0; `npm run build` exit 0; `git diff --check` exit 0. | PARTIAL — catalog naming/hierarchy is aligned; QBO sibling report products, authenticated populated REFS rendering and catalog controls remain unverified or intentionally excluded. |
| Aging aggregate naming boundary | Authenticated QBO labels its customer report `Accounts receivable aging summary`. Separate prior observations showed customer/vendor rows and aging buckets, while the current REFS API returns only currency aggregates and control totals. | The REFS full-page reader no longer calls its currency aggregate an aging `summary`; it uses the neutral `Accounts receivable aging` / `Accounts payable aging` title and states `Review currency aging and control totals.` Scope, as-of date, GET refresh, balances, empty/blocked states and exact Back remain unchanged. | `npm run test:authoritative-aging` exit 0; `npm run test:authoritative-reports` exit 0; `npm run test:authoritative-full-shell` exit 0; `npm run build` exit 0; `git diff --check` exit 0. | PARTIAL — authoritative customer/vendor Summary and Detail rows, immutable snapshot membership and document drill remain unimplemented; currency aggregates are control evidence only. |
| Account Register row evidence drill | Authenticated QBO opened a cash Bank Register as an independent page with `Back to Chart of Accounts`, account selection, Reconcile, Bank transactions, ending/bank balances, reconciliation cutoff, paging and a transaction grid. Observed columns were Date, Ref No., Payee, Memo, Class, Payment, Deposit, Clear, Balance, Type, Account and Location. No row, Add check, filter, print, export, settings, Reconcile or Bank transactions action was operated. | Every existing REFS Register row now has an explicit `View details` action that opens the shared full-page read-only GL→Journal→Source lineage drill using exact entity, period, journal-entry, journal-line and ledger-line IDs. Back restores Register query/page state, table scroll, page scroll and opener focus before the outer Back restores Chart of Accounts. | `npm run test:authoritative-coa-register` exit 0; `npm run test:authoritative-lineage-drill` exit 0; `npm run test:authoritative-full-shell` exit 0; `npm run test:authoritative-visual-parity` exit 0; `npm run build` exit 0; `git diff --check` exit 0. | PARTIAL — QBO Ref No., Payee, Class, Location, clear status, transaction type and account-specific columns are not returned by the current Register API and are not inferred; authenticated populated REFS drill/Back remains unverified. |
| Reconcile entry copy and product boundary | Authenticated QBO Reconcile showed `Match the books to the bank records`, a `Connect now` promotion, three benefit bullets and `Get started`. No account, button or reconciliation action was operated. | REFS uses the observed concise description while retaining its existing authoritative reconciliation history, retained statement selection and role-controlled workflow. It does not add the QBO connection promotion or generic Get started action. | `npm run test:authoritative-bank` exit 0; `npm run test:authoritative-bank-responsive` exit 0; `npm run test:authoritative-full-shell` exit 0; `npm run build` exit 0; `git diff --check` exit 0. | PARTIAL — QBO onboarding actions were not operated and are intentionally excluded; authenticated populated REFS reconciliation runtime remains unverified. |
| Open Invoices report discoverability | Fresh authenticated QBO Standard reports search opened Open Invoices Report as a distinct full page with `Back to standard reports`, an as-of selector (`Today`, 08/20/2026), ready status and a concise scoped-empty alert. The observed table schema was Date, Transaction type, Num, Term, Due date and Open balance. No control, sort, row, customer, invoice, payment, Customize, Save As, Email, Print, Export, note or More action was used. | REFS search exposes Open Invoices as explicitly unavailable. The unavailable reason requires one immutable as-of customer open-balance snapshot; current Invoice queue rows and currency-only A/R aging control totals are not treated as historical document membership. | `npm run test:authoritative-reports` exit 0; `npm run test:authoritative-full-shell` exit 0; `npm run test:authoritative-visual-parity` exit 0; `npm run test:navigation-a11y` exit 0; `npm run build` exit 0; `npm test` exit 0 (visual verifier 64/64); `git diff --check` exit 0. | UNIMPLEMENTED — the authoritative API lacks immutable as-of customer/document membership, snapshot identity/version/hash and open-balance rows; populated QBO rows and authenticated REFS runtime/drill remain unverified. |
| Collections Report discoverability | Fresh authenticated QBO Standard reports search opened Collections Report as a distinct full page with `Back to standard reports`, an as-of selector (`Today`, 08/20/2026), ready status and a concise scoped-empty alert. The observed table schema was Date, Transaction type, Num, Due date, Past due, Amount and Open balance. Pivot and Chart were visible but not operated. No control, sort, row, customer, invoice, payment, Customize, Save As, Email, Print, Export, note or More action was used. | REFS search exposes Collections Report as explicitly unavailable. The unavailable reason requires one immutable as-of customer collections snapshot; current Invoice queue rows and currency-only A/R aging control totals are not treated as historical customer/document membership. | `npm run test:authoritative-reports` exit 0; `npm run test:authoritative-full-shell` exit 0; `npm run test:authoritative-visual-parity` exit 0; `npm run test:navigation-a11y` exit 0; `npm run build` exit 0; `npm test` exit 0 (visual verifier 64/64); `git diff --check` exit 0. | UNIMPLEMENTED — the authoritative API lacks immutable as-of collections snapshot identity/version/hash, customer/document membership and past-due rows; populated QBO rows, Pivot/Chart and authenticated REFS runtime/drill remain unverified. |
| Invoice List by Date report | Fresh authenticated QBO Standard reports search opened `Invoice List by Date` as a distinct full page with `Back to standard reports`, Report period (`This month to date`), From/To dates, ready status and a scoped-empty result. The visible columns were Date, Transaction type, Num, Name, Memo, Due date, Amount and Open balance. Pivot and Chart were visible but not operated. No control, sort, row, customer, invoice, payment, Customize, Save As, Email, Print, Export, note or More action was used. | REFS search now opens a full-page, GET-only Invoice List reader backed by the existing authoritative A/R Invoice response. It preserves the configured entity/accounting-period scope, uses the returned invoice date, number, customer, description, due date, amount and open balance, labels the validated A/R document kind as `Invoice`, pages at 25 rows and returns exactly to the report catalog. The scoped empty state does not claim a zero receivables balance. | `npm run test:authoritative-documents` exit 0; `npm run test:authoritative-reports` exit 0; `npm run test:authoritative-full-shell` exit 0; `npm run test:authoritative-visual-parity` exit 0; `npm run test:navigation-a11y` exit 0; `npm run build` exit 0; `npm test` exit 0 (visual verifier 64/64); `git diff --check` exit 0. | PARTIAL — REFS uses the configured accounting period rather than exposing QBO ad-hoc period/From/To controls; populated QBO rows, row drill behavior, Pivot/Chart and authenticated populated REFS runtime remain unverified. |
| Journal report | Fresh authenticated QBO Standard reports search opened `Journal` as a distinct full page with `Back to standard reports`, Report period (`This month to date`), From/To dates, ready status and a scoped-empty result. The visible columns were Transaction date, Transaction type, Num, Name, Description, Account Name, Debit and Credit. Pivot and Chart were visible but not operated. No control, sort, row, journal, Customize, Save As, Email, Print, Export, note or More action was used. | REFS search now opens a full-page, GET-only Journal report backed by the existing period-scoped POSTED General Ledger line response. It shows only API-proven Transaction date, journal Num, Description, Account Name/code, Debit and Credit, uses server pagination at 25 rows and returns exactly to the report catalog. A concise disclosure says Transaction type and Name are not returned and are not inferred. The scoped empty state does not claim a zero ledger balance. | `npm run test:authoritative-general-ledger` exit 0; `npm run test:authoritative-reports` exit 0; `npm run test:authoritative-full-shell` exit 0; `npm run test:authoritative-visual-parity` exit 0; `npm run test:navigation-a11y` exit 0; `npm run build` exit 0; `npm test` exit 0 (visual verifier 64/64); `git diff --check` exit 0. | PARTIAL — the authoritative reader is configured-period and POSTED-only; QBO Transaction type, Name, ad-hoc period/From/To, populated rows, row drill behavior, Pivot/Chart and authenticated populated REFS runtime remain unverified or unavailable. |
| Account List report | Fresh authenticated QBO Standard reports search opened `Account List` as a distinct full page with `Back to standard reports` and ready status. No Report period/From/To control was visible. The populated table columns were Account Name, Type, Detail type, Description and Total balance. No control, sort, row, account, Customize, Save As, Pivot, Chart, refresh, Email, Print, Export, note or More action was used; no real account name or balance is retained in this matrix. | REFS search now opens a full-page, GET-only Account List backed by the existing authoritative Chart of Accounts response. It shows only API-proven account name/code, member rule, active status, currency and configured-period ending balance, pages at 25 rows and returns exactly to the report catalog. A concise disclosure says Type, Detail type and Description are unavailable and are not inferred. The scoped empty state does not claim a zero ledger balance. | `npm run test:authoritative-coa-register` exit 0; `npm run test:authoritative-reports` exit 0; `npm run test:authoritative-full-shell` exit 0; `npm run test:authoritative-visual-parity` exit 0; `npm run test:navigation-a11y` exit 0; `npm run build` exit 0; `npm test` exit 0 (visual verifier 64/64); `git diff --check` exit 0. | PARTIAL — QBO Type, Detail type, Description, sorting and row drill are unavailable or unverified; REFS balances are configured-period facts, and authenticated populated REFS runtime remains unverified. |
| Transaction Detail by Account discoverability | Fresh authenticated QBO Standard reports search opened `Transaction Detail by Account` as a distinct full page with `Back to standard reports`, Report period (`This month to date`), From/To, Cash/Accrual and ready status. The current scope was empty; the visible columns were Transaction date, Transaction type, Num, Name, Class full name, Description, Split, Amount and Balance. No control, sort, row, account, Customize, Save As, Pivot, Chart, refresh, Email, Print, Export, note or More action was used. | REFS search exposes Transaction Detail by Account as explicitly unavailable. The current General Ledger response proves posted date, journal number, account, description, debit/credit and lineage, but it does not provide transaction type, Name, Class, Split, authoritative account grouping or running balances; REFS does not derive those facts in the browser or alias this report to General Ledger. | `npm run test:authoritative-reports` exit 0; `npm run test:authoritative-full-shell` exit 0; `npm run test:authoritative-visual-parity` exit 0; `npm run test:navigation-a11y` exit 0; `npm run build` exit 0; `npm test` exit 0 (visual verifier 64/64); `git diff --check` exit 0. | UNIMPLEMENTED — a dedicated authoritative transaction-detail read model with account grouping, transaction metadata, split facts and running balances is missing; populated QBO rows, row drill, responsive behavior and authenticated REFS runtime remain unverified. |
| Transaction List by Date discoverability | Fresh authenticated QBO Standard reports search opened `Transaction List by Date` as a distinct full page with `Back to standard reports`, Report period (`This month to date`), From/To and ready status. The current scope was empty; the visible columns were Date, Transaction type, Num, Posting (Y/N), Name, Memo, Account name, Split and Amount. Pivot and Chart were visible but not operated. No control, sort, row, transaction, Customize, Save As, refresh, Email, Print, Export, note or More action was used. | REFS search exposes Transaction List by Date as explicitly unavailable. The current General Ledger response is POSTED-only and does not provide authoritative transaction type, Posting Y/N, Name, Memo or Split facts; REFS does not alias the report to GL or join independent browser reads. | `npm run test:authoritative-reports` exit 0; `npm run test:authoritative-full-shell` exit 0; `npm run test:authoritative-visual-parity` exit 0; `npm run test:navigation-a11y` exit 0; `npm run build` exit 0; `npm test` exit 0 (visual verifier 64/64); `git diff --check` exit 0. | UNIMPLEMENTED — a complete authoritative transaction-list read model with posting, name, memo, account and split facts is missing; populated QBO rows, row drill, responsive behavior and authenticated REFS runtime remain unverified. |
| Transaction List with Splits discoverability | Fresh authenticated QBO Standard reports search opened `Transaction List with Splits` as a distinct full page with `Back to standard reports`, Report period (`This month to date`), From/To and ready status. The current scope was empty; the visible columns were Transaction date, Transaction type, Num, Posting (Y/N), Name, Description, Account Name and Amount. Pivot and Chart were visible but not operated. No control, sort, row, transaction, Customize, Save As, refresh, Email, Print, Export, note or More action was used. | REFS search exposes Transaction List with Splits as explicitly unavailable. The current General Ledger response contains posted ledger lines but does not carry the complete transaction identity, Posting Y/N, counterparty Name or QBO split-line contract; REFS does not alias this report to GL or reconstruct splits across independent reads. | `npm run test:authoritative-reports` exit 0; `npm run test:authoritative-full-shell` exit 0; `npm run test:authoritative-visual-parity` exit 0; `npm run test:navigation-a11y` exit 0; `npm run build` exit 0; `npm test` exit 0 (visual verifier 64/64); `git diff --check` exit 0. | UNIMPLEMENTED — a complete authoritative transaction-with-splits read model is missing; populated QBO rows, split grouping, row drill, responsive behavior and authenticated REFS runtime remain unverified. |
| Recent Transactions discoverability | Fresh authenticated QBO Standard reports search selected `Recent Transactions`, which opened a distinct full page titled `Transaction Report` with `Back to standard reports`, Report period `All Dates` and ready status. The current scope was empty; the visible columns were Date, Num, Posting (Y/N), Name, Memo, Account full name and Amount. Pivot and Chart were visible but not operated. No period control, sort, row, transaction, Customize, Save As, refresh, Email, Print, Export, note or More action was used. | REFS search exposes the catalog label Recent Transactions as explicitly unavailable and records the observed QBO catalog/title mismatch. The current General Ledger reader is configured-period and POSTED-only, and it lacks Posting Y/N, Name and Memo; REFS does not invent a recent/all-date scope or alias the result to GL. | `npm run test:authoritative-reports` exit 0; `npm run test:authoritative-full-shell` exit 0; `npm run test:authoritative-visual-parity` exit 0; `npm run test:navigation-a11y` exit 0; `npm run build` exit 0; `npm test` exit 0 (visual verifier 64/64); `git diff --check` exit 0. | UNIMPLEMENTED — authoritative recent/all-date scope semantics and a complete transaction row model are missing; populated QBO rows, row drill, responsive behavior and authenticated REFS runtime remain unverified. |
| Invalid Journal Transactions discoverability | Fresh authenticated QBO Standard reports search opened `Invalid Journal Transactions` as a distinct full page with `Back to standard reports`, Report period (`This year to date`), From/To, Cash/Accrual and ready status. The current scope was empty; the visible columns were Transaction date, Transaction type, Num, Line order, Customer full name, Vendor display name, Account Name, Line currency, Exchange rate and Amount. No control, sort, row, journal, Customize, Save As, refresh, Email, Print, Export, note or More action was used. | REFS search exposes Invalid Journal Transactions as explicitly unavailable. The ordinary Journal reader proves POSTED lines and immutable lineage but does not return QBO's invalid classification, customer/vendor, line currency or exchange-rate facts; the browser does not classify ordinary journal rows or alias this report to Journal/Exception Center. | `npm run test:authoritative-reports` exit 0; `npm run test:authoritative-full-shell` exit 0; `npm run test:authoritative-visual-parity` exit 0; `npm run test:navigation-a11y` exit 0; `npm run build` exit 0; `npm test` exit 0 (visual verifier 64/64); `git diff --check` exit 0. | UNIMPLEMENTED — an authoritative invalid-journal report contract with server classification, counterparties and FX facts is missing; populated QBO rows, invalid-reason semantics, drill, responsive behavior and authenticated REFS runtime remain unverified. |
| 1099 Transaction Detail Report discoverability | Fresh authenticated QBO Standard reports search opened the classic `1099 Transaction Detail Report`. A read-only Run report loaded a populated, vendor-grouped result with `Back to report list`, Report period `All Dates`, Cash/Accrual, and columns Date, Transaction Type, Num, Memo/Description, 1099 Box, Account, Split, Amount, Balance and Tax ID. A visible deprecation alert directed users to Transaction List by Vendor for transaction detail and Vendor Contact List for W-9 information. No filter value, row, vendor, transaction, tax setting, Customize, Save customization, note, Email, Print, Export or Settings action was used; no real vendor, tax ID, document number or amount is retained here. | REFS search exposes 1099 Transaction Detail Report as explicitly unavailable. Current AP/vendor and GL readers do not return authoritative vendor tax identity, approved 1099-box mappings or the classic report's vendor grouping/running balances; REFS does not infer these sensitive facts or substitute Transaction List by Vendor. | `npm run test:authoritative-reports` exit 0; `npm run test:authoritative-full-shell` exit 0; `npm run test:authoritative-visual-parity` exit 0; `npm run test:navigation-a11y` exit 0; `npm run build` exit 0; `npm test` exit 0 (visual verifier 64/64); `git diff --check` exit 0. | UNIMPLEMENTED — authoritative 1099 vendor identity/box-mapping read models and a replacement report contract are missing; responsive behavior, tax-permission visibility, row drill and authenticated REFS runtime remain unverified. |
| Vendor Contact List discoverability | Fresh authenticated QBO Standard reports search opened `Vendor Contact List` as a distinct full page with `Back to standard reports` and ready status. No report-period control was visible. The populated table columns were Vendor, Company, Phone numbers, Email, Full name, Billing address, Account #, Tax ID and Track 1099. No control, sort, row, vendor, contact, tax setting, Customize, Save As, refresh, Email, Print, Export, note or More action was used; no real vendor, contact, address, account number or tax identity is retained here. | REFS search exposes Vendor Contact List as explicitly unavailable. Existing AP document responses prove a transaction counterparty only; they do not constitute a permission-scoped vendor master, contact directory, tax-identity source or 1099-status reader. REFS does not infer or expose these sensitive facts. | `npm run test:authoritative-reports` exit 0; `npm run test:authoritative-full-shell` exit 0; `npm run test:authoritative-visual-parity` exit 0; `npm run test:navigation-a11y` exit 0; `npm run build` exit 0; `npm test` exit 0 (visual verifier 64/64); `git diff --check` exit 0. | UNIMPLEMENTED — an authoritative permission-scoped vendor master/contact/tax read model is missing; field-level permissions, drill, responsive behavior and authenticated REFS runtime remain unverified. |
| Transaction List by Tag Group discoverability | Fresh authenticated QBO Standard reports search opened `Transaction List by Tag Group` as a distinct full page with `Back to standard reports`, Report period (`This month to date`), From/To and ready status. The current scope was empty; the visible columns were Date, Transaction type, Num, Posting (Y/N), Name, Memo, Account name, Account full name (split account) and Amount. Pivot and Chart were visible but not operated. No control, sort, row, tag, transaction, Customize, Save As, refresh, Email, Print, Export, note or More action was used. | REFS search exposes Transaction List by Tag Group as explicitly unavailable. Current ledger and document readers do not return authoritative tag-group membership or the complete transaction, posting, counterparty, memo and split-account facts required by this report; REFS does not infer tag membership or assemble the report from independent browser reads. | `npm run test:authoritative-reports` exit 0; `npm run test:authoritative-full-shell` exit 0; `npm run test:authoritative-visual-parity` exit 0; `npm run test:navigation-a11y` exit 0; `npm run build` exit 0; `npm test` exit 0 (visual verifier 64/64); `git diff --check` exit 0. | UNIMPLEMENTED — an authoritative tag-group transaction read model is missing; populated QBO rows, tag grouping, row drill, responsive behavior and authenticated REFS runtime remain unverified. |
| Transaction Drilldown Report discoverability | Fresh authenticated QBO reports search opened `Transaction Drilldown Report` as a populated saved custom report (`type=user`) with `Back to custom reports`, Custom dates/From/To, Cash/Accrual and ready status. It grouped rows by account hierarchy and showed Beginning Balance, transaction rows, running Balance, account totals and parent totals. The visible columns were Transaction date, Transaction type, Num, Name, Description, Account full name (distribution account), Item split account, Amount and Balance. No control, sort, row, transaction, Customize, Save, refresh, Pivot, Chart, Email, Print, Export, note or More action was used; no real account, counterparty, document, description or amount is retained here. | REFS search exposes Transaction Drilldown Report as explicitly unavailable. The observed result depends on a saved custom-report definition plus server-grouped transaction rows and running balances. Current POSTED General Ledger rows do not prove that custom definition, transaction type/name/split facts or the grouping and balance calculations; REFS does not silently alias it to GL. | `npm run test:authoritative-reports` exit 0; `npm run test:authoritative-full-shell` exit 0; `npm run test:authoritative-visual-parity` exit 0; `npm run test:navigation-a11y` exit 0; `npm run build` exit 0; `npm test` exit 0 (visual verifier 64/64); `git diff --check` exit 0. | UNIMPLEMENTED — an immutable custom-report definition and authoritative grouped transaction/running-balance read model are missing; row drill, responsive behavior and authenticated REFS runtime remain unverified. |
| Profit and Loss by Customer discoverability | Fresh authenticated QBO Standard reports search opened `Profit and Loss by Customer` as a standard full-page report with `Back to standard reports`, Report period (`This year to date`), From/To, Cash/Accrual, Display columns by Customer, Compare to and ready status. The current scope was empty, so no populated customer columns, account hierarchy or totals were observed. No control, customer, row, Customize, Save As, refresh, Email, Print, Export, note or More action was used. | REFS search exposes Profit and Loss by Customer as explicitly unavailable. Current Income Statement rows are aggregate account facts, and the existing property/project/unit/lot profitability reader proves only its selected dimension; neither invoices nor AR aging totals establish customer-dimension statement columns. REFS does not substitute another dimension or join independent browser reads. | `npm run test:authoritative-reports` exit 0; `npm run test:authoritative-full-shell` exit 0; `npm run test:authoritative-visual-parity` exit 0; `npm run test:navigation-a11y` exit 0; `npm run build` exit 0; `npm test` exit 0 (visual verifier 64/64); `git diff --check` exit 0. | UNIMPLEMENTED — an authoritative customer-dimension P&L read model with server-defined customer columns and totals is missing; populated QBO hierarchy/totals, comparison behavior, row drill, responsive behavior and authenticated REFS runtime remain unverified. |
| Profit and Loss by Class discoverability | Fresh authenticated QBO Standard reports search opened `Profit and Loss by Class` as a standard full-page report with `Back to standard reports`, Report period (`This year to date`), From/To, Cash/Accrual, Display columns by Class, Compare to and ready status. The current scope was empty, so no populated class columns, account hierarchy or totals were observed. No control, class, row, Customize, Save As, refresh, Email, Print, Export, note or More action was used. | REFS search exposes Profit and Loss by Class as explicitly unavailable. Current Income Statement rows are aggregate account facts, while the existing property/project/unit/lot profitability reader proves only its selected dimension; no authoritative class-dimension statement response exists. REFS does not substitute another dimension or derive class columns from ledger lines in the browser. | `npm run test:authoritative-reports` exit 0; `npm run test:authoritative-full-shell` exit 0; `npm run test:authoritative-visual-parity` exit 0; `npm run test:navigation-a11y` exit 0; `npm run build` exit 0; `npm test` exit 0 (visual verifier 64/64); `git diff --check` exit 0. | UNIMPLEMENTED — an authoritative class-dimension P&L read model with server-defined class columns and totals is missing; populated QBO hierarchy/totals, comparison behavior, row drill, responsive behavior and authenticated REFS runtime remain unverified. |
| Profit and Loss by Tag Group discoverability | Fresh authenticated QBO Standard reports search opened `Profit and Loss By Tag Group` as a standard full-page report with `Back to standard reports`, Report period (`This year to date`), From/To, Cash/Accrual, Display columns by `Ungrouped Tags`, Compare to and ready status. The current scope was empty, so no populated tag columns, account hierarchy or totals were observed. No control, tag, row, Customize, Save As, refresh, Email, Print, Export, note or More action was used. | REFS search exposes Profit and Loss by Tag Group as explicitly unavailable. Current Income Statement rows are aggregate account facts, and Transaction List by Tag Group is also unavailable because no authoritative tag membership exists. REFS does not infer tag columns from ledger/document rows, substitute property/project dimensions or reuse the aggregate P&L. | `npm run test:authoritative-reports` exit 0; `npm run test:authoritative-full-shell` exit 0; `npm run test:authoritative-visual-parity` exit 0; `npm run test:navigation-a11y` exit 0; `npm run build` exit 0; `npm test` exit 0 (visual verifier 64/64); `git diff --check` exit 0. | UNIMPLEMENTED — an authoritative tag-group P&L read model with immutable tag membership, server-defined tag columns and totals is missing; populated QBO hierarchy/totals, comparison behavior, row drill, responsive behavior and authenticated REFS runtime remain unverified. |
| Profit and Loss as % of total income discoverability | Fresh authenticated QBO Standard reports search exposed `Profit and Loss as % of total income`; opening it produced a standard full page titled `Profit and Loss % of Total Income` with `Back to standard reports`, Report period (`This year to date`), From/To, Cash/Accrual, Display columns, Compare to, a `% of Income` control and ready status. The current scope was empty, so no populated percentage columns, account hierarchy, denominator or totals were observed. No control, row, Customize, Save As, refresh, Email, Print, Export, note or More action was used. | REFS search exposes the percentage report as explicitly unavailable. The existing Income Statement response carries aggregate account amounts but no authoritative percent-of-income column, denominator contract, rounding policy or report totals. REFS does not calculate and present a new report in the browser from those rows. | `npm run test:authoritative-reports` exit 0; `npm run test:authoritative-full-shell` exit 0; `npm run test:authoritative-visual-parity` exit 0; `npm run test:navigation-a11y` exit 0; `npm run build` exit 0; `npm test` exit 0 (visual verifier 64/64); `git diff --check` exit 0. | UNIMPLEMENTED — a server-calculated percent-of-income P&L read model with denominator, rounding, grouped rows and totals is missing; populated QBO columns/hierarchy, comparison behavior, row drill, responsive behavior and authenticated REFS runtime remain unverified. |
| Balance Sheet by Class search boundary | During an authenticated QBO Standard reports session, the exact search `Balance Sheet by Class` returned no report option or result list. No report was opened and no control, row, Customize, Save, refresh, Email, Print, Export or mutation was used. Authentication expired during a later unrelated search, so absence is recorded only for this observed catalog response and is not a product-wide claim. | REFS returns `No reports found` for the same exact query. It neither aliases the request to aggregate Balance Sheet nor creates a speculative unavailable entry; no class-dimension balance sheet is inferred from GL or class-tagged transactions. | `npm run test:authoritative-reports` exit 0; `npm run test:authoritative-full-shell` exit 0; `npm run test:authoritative-visual-parity` exit 0; `npm run test:navigation-a11y` exit 0; `npm run build` exit 0; `npm test` exit 0 (visual verifier 64/64); `git diff --check` exit 0. | NOT OBSERVED — the current authenticated QBO catalog returned no result; subscription/feature availability, direct route, responsive behavior and authenticated REFS runtime remain unverified, so no equivalence or unimplemented-function claim is made. |

| Persisted AI decision workflow | Migration260 and its scoped no-store decision reader are owned by the AI/server integration line and were not present in this UI replay base. Existing analysis GETs are not treated as a persisted decision queue. | The QB/UI cumulative candidate does not invent an endpoint, DTO, browser queue, localStorage state or inferred capability. Human ACCEPT/REJECT and standard Draft actions must remain unavailable from this surface until the permission-closed persisted reader proves the decision, human outcome, Draft/Journal state and DB-derived capabilities. | BLOCKED — waiting for the clean migration260/API contract; no browser fallback and no production-equivalence claim. |
| Invoices and Received Payments discoverability | Fresh authenticated QBO Standard reports at a 525px viewport exposed `Invoices and Received Payments` under the expanded `Who owes you` group. Opening the read-only report produced a distinct full page with `Back to standard reports`, Report period (`This year to date`), From/To, ready status and a scoped empty alert. The visible columns were Date, Transaction type, Memo/Description, Transaction number and Amount. No control, row, invoice, payment, Customize, Save As, refresh, Email, Print, Export, note or More action was used. | REFS search exposes Invoices and Received Payments as explicitly unavailable. The authoritative Invoice list proves document facts only and does not return received-payment rows or immutable invoice-to-payment links; REFS therefore does not alias this report to Invoice List or join browser reads. | `npm run test:authoritative-reports` exit 0; `npm run test:authoritative-full-shell` exit 0; `npm run test:authoritative-visual-parity` exit 0; `npm run test:navigation-a11y` exit 0; `npm run build` exit 0; `npm test` exit 0 (visual verifier 64/64); `git diff --check` exit 0. | UNIMPLEMENTED — an authoritative linked invoice-and-received-payment report response is missing; populated QBO rows, row drill, responsive behavior beyond the observed 525px catalog/full-page shell, and authenticated REFS runtime remain unverified. |
| Statement List discoverability | Fresh authenticated QBO Standard reports exposed `Statement List` under the expanded `Who owes you` group. Opening it produced a distinct classic full page with `Back to standard reports`, Group by, read-only Run report, ready shell, Statement Date (`August 1-23, 2026`) and the explicit empty state `This report does not contain any data.` Customize, Save customization, Add notes, Email, Print, Export and Settings were visible but not operated. Because this scope was empty, no populated row or column structure was observed. | REFS search exposes Statement List as explicitly unavailable. The current Invoice list proves individual document facts but not an immutable customer-statement snapshot, statement membership, opening/closing balances or grouped statement rows; REFS does not construct statements in the browser. | `npm run test:authoritative-reports` exit 0; `npm run test:authoritative-full-shell` exit 0; `npm run test:authoritative-visual-parity` exit 0; `npm run test:navigation-a11y` exit 0; `npm run build` exit 0; `npm test` exit 0 (visual verifier 64/64); `git diff --check` exit 0. | UNIMPLEMENTED — an authoritative customer-statement snapshot/list response is missing; populated columns, statement drill, grouping semantics, responsive behavior and authenticated REFS runtime remain unverified. |
| Terms List discoverability | Fresh authenticated QBO Standard reports opened `Terms List` as a distinct full page with `Back to standard reports`, ready status and populated terms. No report-period control was visible. The table columns were Terms, Type, Net due days, Day of month due, Discount %, Discount days, Discount on day of month and Minimum days to pay. No sort, row, term, Customize, Save As, refresh, Email, Print, Export, note or More action was used; individual term values were not retained in this matrix. | REFS search exposes Terms List as explicitly unavailable. Invoice dates and due dates do not prove a permission-scoped payment-terms master, term type, due-day calculation or discount schedule; REFS does not reverse-engineer master data from documents. | `npm run test:authoritative-reports` exit 0; `npm run test:authoritative-full-shell` exit 0; `npm run test:authoritative-visual-parity` exit 0; `npm run test:navigation-a11y` exit 0; `npm run build` exit 0; `npm test` exit 0 (visual verifier 64/64); `git diff --check` exit 0. | UNIMPLEMENTED — an authoritative payment-terms master/read model and permission contract are missing; row drill, responsive behavior and authenticated REFS runtime remain unverified. |
| Deposit Detail discoverability | Fresh authenticated QBO Standard reports search opened `Deposit Detail` as a distinct full page with `Back to standard reports`, Report period (`This month to date`), From/To, ready status and a scoped empty alert. The visible columns were Transaction date, Transaction type, Num, Customer full name, Vendor, Description, Cleared and Amount. Pivot and Chart were visible but not used. No control, sort, row, deposit, Customize, Save As, refresh, Email, Print, Export, note or More action was used. | REFS search exposes Deposit Detail as explicitly unavailable. Authoritative Bank Transaction rows are external bank evidence and do not prove a posted deposit document, its accounting lines, transaction type, customer/vendor classification or cleared state; REFS does not alias the bank queue to this accounting report. | `npm run test:authoritative-reports` exit 0; `npm run test:authoritative-full-shell` exit 0; `npm run test:authoritative-visual-parity` exit 0; `npm run test:navigation-a11y` exit 0; `npm run build` exit 0; `npm test` exit 0 (visual verifier 64/64); `git diff --check` exit 0. | UNIMPLEMENTED — an authoritative posted-deposit detail read model and scope contract are missing; populated QBO rows, row drill, Pivot/Chart, responsive behavior and authenticated REFS runtime remain unverified. |
| Check Detail discoverability | Fresh authenticated QBO Standard reports search opened `Check Detail` as a distinct full page titled `Check Detail Report`, with `Back to standard reports`, Report period (`This month to date`), From/To, ready status and a scoped empty alert. The visible columns were Transaction date, Transaction type, Num, Name, Description, Cleared and Amount. Pivot and Chart were visible but not used. No control, sort, row, check, Customize, Save As, refresh, Email, Print, Export, note or More action was used. | REFS search exposes Check Detail as explicitly unavailable. Bank Transaction and retained payment evidence do not prove a posted check document, its accounting lines, transaction type, payee/name or cleared state; REFS does not alias those evidence surfaces to this accounting report. | `npm run test:authoritative-reports` exit 0; `npm run test:authoritative-full-shell` exit 0; `npm run test:authoritative-visual-parity` exit 0; `npm run test:navigation-a11y` exit 0; `npm run build` exit 0; `npm test` exit 0 (visual verifier 64/64); `git diff --check` exit 0. | UNIMPLEMENTED — an authoritative posted-check detail read model and scope contract are missing; populated QBO rows, row drill, Pivot/Chart, responsive behavior and authenticated REFS runtime remain unverified. |

### Remaining explicit gaps on this cumulative candidate

- Authenticated `refs-app.onrender.com` OIDC/API/PostgreSQL rendering was not exercised; local tests and build are not production acceptance.
- QBO Expense, Bill payment, Check, Purchase order, Recently paid, Item Receipt and receipt-reminder list readers are not claimed. REFS does not fabricate these without an authoritative read model.
- QBO report groups can remain independently open; this REFS candidate intentionally retains one active report workspace and is therefore PARTIAL.
- QBO customize, favorites, save, share, email, print, export, external connections, promotions and accounting mutations remain excluded.
